interface:
  display_name: "Script Promote"
  short_description: "Promote a verification script into hardened final scripts."
  default_prompt: "Use $script-promote for the current workspace task."

policy:
  allow_implicit_invocation: true
