interface:
  display_name: "Paper Referee"
  short_description: "Run the paper referee fix and review loop."
  default_prompt: "Use $paper-referee for the current workspace task."

policy:
  allow_implicit_invocation: true
