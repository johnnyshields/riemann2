interface:
  display_name: "Trifecta"
  short_description: "Synthesize recent work from three complementary angles."
  default_prompt: "Use $trifecta for the current workspace task."

policy:
  allow_implicit_invocation: true
