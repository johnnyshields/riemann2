interface:
  display_name: "Research Audit"
  short_description: "Audit paper sections under proved conditional missing bins."
  default_prompt: "Use $research-audit for the current workspace task."

policy:
  allow_implicit_invocation: true
