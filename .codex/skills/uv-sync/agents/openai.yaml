interface:
  display_name: "Uv Sync"
  short_description: "Reconcile rem:wip labels with UV ledger entries."
  default_prompt: "Use $uv-sync for the current workspace task."

policy:
  allow_implicit_invocation: true
