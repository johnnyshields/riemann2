interface:
  display_name: "Findings Prune"
  short_description: "Prune oversized findings ledgers while preserving provenance."
  default_prompt: "Use $findings-prune for the current workspace task."

policy:
  allow_implicit_invocation: true
