interface:
  display_name: "Research Attack"
  short_description: "Run a focused research push on one UV or target."
  default_prompt: "Use $research-attack for the current workspace task."

policy:
  allow_implicit_invocation: true
