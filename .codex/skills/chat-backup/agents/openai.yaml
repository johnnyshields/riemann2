interface:
  display_name: "Chat Backup"
  short_description: "Capture the current session summary into the appropriate team directory."
  default_prompt: "Use $chat-backup for the current workspace task."

policy:
  allow_implicit_invocation: true
