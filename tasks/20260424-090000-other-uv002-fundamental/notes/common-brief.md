# Common Brief

Use this file as the base brief for subsequent agents on the current RH finite-core program.

## Queue

Primary queue is `UV-002`-first.

Current theorem stack:

1. Local support theorem on the mixed/two-point side:
   - strongest local support candidate remains the corrected edge-law theorem on good patches, or the exact fixed-shear package closure on the residual corner.
2. Exact fixed-shear / quotient-diagonal package theorem:
   - local target is package-level coincidence on the residual involutive branch, sufficient to imply swap invariance and diagonal vanishing of the actual corrected cubic/quintic defects.
3. Provenance-sensitive bridge theorem:
   - best current shape is reduced-`\widehat\Psi` coincidence / same reduced image germ at coincidence / collision-functoriality for the actual corrected two-atom package.
4. Hidden extraction theorem:
   - package-side control must identify the first surviving odd coefficient of `H_m`.
5. Finite-core contradiction theorem:
   - formulate the genuinely finite-core branch by the first nonzero odd jet and hand off to the existing `\Xi_\zeta^{(N)}` extractor machinery.

## Active warnings

- Do not drift into pair-like quantitative cleanup (`UV-009 -> UV-001 -> UV-008`) as the main queue.
- Do not search for another primitive explicit pointwise highest-new generator.
- Treat local 2-point / mixed 4-point theorems as support/diagnostic unless they clearly collapse one of the fundamental fronts above.
- The draft itself warns that any still-live obstruction may be genuinely relational / provenance-sensitive or non-finite-order.

## Key current findings

- Residual exact fixed-shear transport is exhausted up to package-level data.
- The zeta-side odd-jet extractor is already complete; the missing bridge is package-to-jet.
- No current theorem-shaped route to universal pair-like exhaustion is visible.
- Current local Wronskian/detector formulas are too flexible to force a positive `UV-004` theorem from present local formulas alone.

## Best current package theorem candidate

The cleanest fundamental theorem candidate is a reduced-`\widehat\Psi` coincidence / collision-functoriality theorem for the actual corrected two-atom package, strong enough to make the order-7 quotient-septic collapse formal and to feed the hidden extraction theorem.

## Provenance refs

- `tasks/20260424-090000-other-uv002-fundamental/reports/coordinator-current-convergence.md`
- `paper/findings.md`
- `paper/unverified.tex`
- `paper/chats/` for prior agent attempts and theorem sketches.
