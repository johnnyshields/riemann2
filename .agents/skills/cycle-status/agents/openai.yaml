interface:
  display_name: "Cycle Status"
  short_description: "Snapshot active research state, teams, UVs, and recent lore."
  default_prompt: "Use $cycle-status for the current workspace task."

policy:
  allow_implicit_invocation: true
