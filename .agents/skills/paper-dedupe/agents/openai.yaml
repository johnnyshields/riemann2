interface:
  display_name: "Paper Dedupe"
  short_description: "Remove duplicate paper content while preserving unique claims."
  default_prompt: "Use $paper-dedupe for the current workspace task."

policy:
  allow_implicit_invocation: true
