interface:
  display_name: "Paper Rewrite"
  short_description: "Compact and tighten paper prose without changing math."
  default_prompt: "Use $paper-rewrite for the current workspace task."

policy:
  allow_implicit_invocation: true
