interface:
  display_name: "Research Resume"
  short_description: "Resume adaptive research-team workflow inside an existing team directory."
  default_prompt: "Use $research-resume for the current workspace task."

policy:
  allow_implicit_invocation: true
