interface:
  display_name: "Research Capture"
  short_description: "Append a single provenance-backed research finding."
  default_prompt: "Use $research-capture for the current workspace task."

policy:
  allow_implicit_invocation: true
