interface:
  display_name: "Paper Cut"
  short_description: "Move a paper passage to cut-for-time with provenance."
  default_prompt: "Use $paper-cut for the current workspace task."

policy:
  allow_implicit_invocation: true
