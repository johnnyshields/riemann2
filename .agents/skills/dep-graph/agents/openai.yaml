interface:
  display_name: "Dep Graph"
  short_description: "Validate UV dependency graphs and identify critical blockers."
  default_prompt: "Use $dep-graph for the current workspace task."

policy:
  allow_implicit_invocation: true
