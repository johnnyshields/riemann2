interface:
  display_name: "Session Handoff"
  short_description: "Write an end-of-session handoff into lore."
  default_prompt: "Use $session-handoff for the current workspace task."

policy:
  allow_implicit_invocation: true
