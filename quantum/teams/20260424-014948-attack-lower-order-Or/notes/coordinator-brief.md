# Coordinator Brief

Focused topic: lower-order Veronese complements on the value-channel-free `O_r`
side.

Question:
- Since `A_r = span{psi} ⊕ O_r` and we now have an exact lower-order complement
  theorem for `A_r`, can we write the corresponding `O_r` complement theorem
  cleanly for all `(n,r)`?

Targets:
1. State the exact complement formula.
2. Keep it as a side corollary unless it clearly sharpens the note.
