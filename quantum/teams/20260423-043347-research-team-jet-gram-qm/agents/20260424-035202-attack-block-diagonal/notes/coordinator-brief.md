# Coordinator Brief

Focused topic: block-diagonal criterion for the `A_r` / `O_r` complement
relation.

Question:
- When does the complement comparison for `O_r` split into a direct sum of the
  value-channel line and the `A_r^\perp` comparison, so that the `A_r` and `O_r`
  data separate cleanly?

Targets:
1. State the strongest exact criterion.
2. Keep it subspace/complement-level.
