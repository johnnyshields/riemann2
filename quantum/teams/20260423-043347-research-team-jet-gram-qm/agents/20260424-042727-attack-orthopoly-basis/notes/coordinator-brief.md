# Coordinator Brief

Focused topic: canonical orthogonal-polynomial / Lanczos basis in the exact
unitary setting.

Question:
- Given the exact unitary/Krylov specialization, does the spectral measure of
  `(H, psi_0)` canonically determine an orthonormal basis of `A_r`, and perhaps
  a centered analogue for `O_r`, strong enough to yield a more canonical matrix
  package in this specialization?

Targets:
1. State the strongest exact theorem.
2. Distinguish the `A_r` and `O_r` cases.
3. Keep the statement explicitly specialized to the exact unitary/Krylov setup.
