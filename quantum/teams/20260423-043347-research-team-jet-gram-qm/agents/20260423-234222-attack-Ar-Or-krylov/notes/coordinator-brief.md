# Coordinator Brief

Focused topic: `A_r` versus `O_r` under exact unitary/Krylov dynamics.

Question:
- Does the exact Krylov specialization give a sharper principled criterion for
  when `A_r` or `O_r` is the natural object?

Targets:
1. State the strongest exact role split supported by the dynamical picture.
2. Avoid overclaiming uniqueness if it is still context-dependent.
