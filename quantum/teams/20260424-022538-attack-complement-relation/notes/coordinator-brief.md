# Coordinator Brief

Focused topic: general complement-level relation between `A_r` and `O_r`.

Question:
- Can the exact relation `A_r = span{psi} ⊕ O_r` be turned into a clean two-point
  formula relating the principal-angle data of `A_r` and `O_r` through a small
  complement matrix?

Targets:
1. State the strongest exact relation.
2. Keep it at the subspace/complement level.
3. See whether it explains the benchmark behavior conceptually.
