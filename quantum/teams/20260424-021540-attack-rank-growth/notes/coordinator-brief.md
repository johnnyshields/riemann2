# Coordinator Brief

Focused topic: growth and stabilization of `A_r` and `O_r` along exact
unitary/Krylov orbits.

Question:
- If the cyclic/Krylov subspace generated by `(H, psi_0)` has finite dimension
  `m`, what are the exact dimensions of `A_r` and `O_r` as functions of `r`?

Targets:
1. State the exact dimension formulas.
2. Keep it finite-dimensional / finite-cyclic if needed.
