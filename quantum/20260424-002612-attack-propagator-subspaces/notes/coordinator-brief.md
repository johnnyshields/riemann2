# Coordinator Brief

Focused topic: exact two-point propagator formulas for unitary subspace
invariants.

Question:
- For exact unitary orbits, can the two-point subspace invariants be written
  directly as compressions of the propagator `e^{-i(t-s)H}` onto the initial
  Krylov / compressed Krylov spaces?

Targets:
1. State the exact formula.
2. Distinguish `A_r` and `O_r`.
3. Decide whether it is worth adding as a short corollary.
