# Coordinator Brief

Focused topic: `A_r` versus `O_r` as the canonical subspace.

Question:
- Which subspace is the better canonical object for the note?
  `A_r = span{psi, jets}` includes the value channel.
  `O_r = span{projected jets}` excludes it.

Targets:
1. Determine the exact relation between `A_r` and `O_r`.
2. Identify which one better matches the existing first-order and higher-order
   results.
3. Give the strongest safe recommendation for the note.

Source files:
- `quantum/paper/jet_gram_quantum_note.md`
- `quantum/findings.md`
- `quantum/20260423-140111-attack-transport-free-subspaces/reports/*.md`
- `quantum/20260423-141619-attack-projector-operator/reports/*.md`

Non-goals:
- No edits outside `quantum/`.
- No claim that one choice is uniquely correct unless proved.
