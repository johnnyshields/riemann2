# Coordinator Brief

Focused topic: higher-order gauge-covariant jet hierarchy.

Question:
- Is there a clean higher-order covariant derivative hierarchy whose iterates are
  gauge-covariant and span the same higher-jet subspaces as the raw projected
  jets?

Targets:
1. State the strongest exact theorem if it exists.
2. Compare the resulting subspaces with the raw `O_r` / `A_r` ones.
3. Do not overclaim matrix canonicity if the result is only subspace-level.
