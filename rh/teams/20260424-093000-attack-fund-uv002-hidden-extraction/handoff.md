# Handoff

Date: 2026-04-24
Team dir: `paper/teams/20260424-093000-attack-fund-uv002-hidden-extraction/`
Scope: RH main paper, UV-002-first, fundamental/package-side queue

## 1. Executive Summary

The session did **not** close RH or `UV-002`, but it did materially sharpen the finite-core queue.

The main outcome is that the active queue has collapsed to:

\[
\boxed{C \to D \to \text{finite-core contradiction}}
\]

where

- `C` is the **corrected reduced-package diagonal-collapse / collision-functoriality theorem**
  \[
  \widetilde\Psi^{\corr}_{\mathrm{red}}(m,\kappa,0)=\widehat\Psi(m),
  \]
  on the collision/cancellation blow-up chart;

- `D` is the **hidden-state / package-to-transform theorem**
  saying the first surviving odd order and leading coefficient of `H_m`, equivalently the first nonzero localized transform `\Xi_{\zeta,\le H}^{(N)}`, are determined by the corrected coincidence package modulo `\Phi_K`-invisible directions;

- the final theorem is the **first-nonzero-odd-jet finite-core contradiction theorem**.

All local lane work is now best treated as support or diagnostics beneath `C`, not as the main queue.

## 2. Authoritative State

Per the latest `CLAUDE.md`, authoritative in-cycle state should live in the latest team dir under `paper/teams/...`.

This session created the first canonical team dir:

- `paper/teams/20260424-093000-attack-fund-uv002-hidden-extraction/`

Use this dir, not the earlier ad hoc `paper/tasks/...` note bundle, as the main carrier of state.

Key files in the team dir:

- `findings.md`
- `uv.md`
- `collation.md`
- `dispatch.md`
- `summary.md`
- `handoff.md` (this file)
- `agents/.../report.md` for per-agent deposits

There are **no** prior `paper/teams/` dirs in the repo before this one.

## 3. Biggest Workflow Fixes This Session

1. We stopped treating local theorem-shaping as the main queue and put `UV-002` back in charge.
2. We corrected the task/provenance workflow to align with the current `CLAUDE.md`:
   agent work is now being deposited under `paper/teams/.../agents/.../report.md`.
3. We updated `CLAUDE.md` to use `paper/tasks/` for ad hoc task tracking, but the latest authoritative policy is actually `paper/teams/...` for research cycles. The active cycle is now in `paper/teams/...` and should stay there.
4. We updated `paper/findings.md` with several structurally important new findings, especially around the exact fixed-shear residual side and the extractor/package split.

## 4. What Is Actually Proved Already

These are real structural facts already in the draft and should not be re-derived as if they were still open:

### 4.1 Zeta-side extractor is complete

The draft already has:

- corrected transverse scalar `H_m(s)`;
- odd holomorphic expansion
  \[
  H_m(z/Q^2)=\sum_{r\ge 0} c_{2r+1}(m) z^{2r+1} Q^{-(2r+4)};
  \]
- exact `N`-point extractor `\Xi_\zeta^{(N)}`;
- exact surviving-tail expansion showing `\Xi_\zeta^{(N)}` isolates the first surviving odd coefficient;
- finite-core localization for both `c_{2N-1}(m)` and `\Xi_{\zeta,\le H}^{(N)}`.

Key refs:

- `paper/proof_of_rh.tex:2214-2307`
- `paper/proof_of_rh.tex:2953-2969`
- `paper/proof_of_rh.tex:3984-4190`

Consequence: the extractor side is **not** the blocker anymore.

### 4.2 Canonical order-7 package is closed

The draft already canonically closes the quotient-septic order-7 package:

- `A_7^{\mathfrak f}` matters only modulo the quintic line;
- equivalently `\Delta_7` is canonical.

Key refs:

- `paper/proof_of_rh.tex:7004-7062`
- `paper/proof_of_rh.tex:7892-8001`

Consequence: the order-7 side is no longer blocked by raw representative ambiguity alone.

### 4.3 Explicit highest-new pointwise package is exhausted

All currently visible explicit highest-new pointwise fields lie in the same tower generated by

\[
r=q^{(7)}.
\]

There is no second primitive explicit pointwise highest-new generator.

Key refs:

- `paper/proof_of_rh.tex:21142-21329`

Consequence: stop searching for new local pointwise generators.

### 4.4 Residual exact fixed-shear transport is exhausted up to package data

On the residual exact fixed-shear side:

- repeated-value branch is involutive;
- finite quotient-visible transport jets descend to `u=t^2`;
- on the quartic--sextic rung every finite quotient-visible transport state is rank one in `Q`;
- no finite-order hidden reset remains beyond the involution.

Key refs:

- `paper/proof_of_rh.tex:21736-22529`
- `paper/proof_of_rh.tex:22966-23109`

Consequence: any still-live obstruction is package-level / provenance-sensitive / non-finite-order, not another transport-jet classification issue.

## 5. What Is Still Open

### 5.1 Top-level open UVs

- `UV-002`: pair-like vs genuine finite-core endgame
- `UV-003`: corrected edge-law / local good-patch theorem
- `UV-004`: explicit-pointwise bridge to the local detector
- `UV-007`: current endgame status; depends on UV-002

`UV-001`, `UV-008`, `UV-009` remain open but should be treated as downstream pair-like/effective cleanup, not the main queue.

## 6. Queue Lock

The queue should currently be treated as:

1. `C`: corrected reduced-package diagonal-collapse theorem
2. `D`: hidden-state / package-to-transform factorization theorem
3. finite-core contradiction theorem

Support/diagnostic lanes still active beneath that:

- exact fixed-shear local package theorem (`B`)
- corrected edge-law theorem on good patches (`A`)
- `UV-004` bridge theorem (negative-leaning / diagnostic)

Do **not** let the queue drift back to:

- pair-like quantitative cleanup (`UV-009 -> UV-001 -> UV-008`) as main queue
- another pointwise generator hunt
- pure planar / endpoint-gap / three-run overlap work unless directly serving `C` or `D`

## 7. Bottleneck C: Final Current Shape

### The theorem

The sharp current statement is:

\[
\widetilde\Psi^{\corr}_{\mathrm{red}}(m,\kappa,0)=\widehat\Psi(m).
\]

Interpretation:

- work in the collision/cancellation blow-up chart with `2\omega=\kappa\delta`;
- define the actual corrected reduced two-atom package `\widetilde\Psi^{\corr}_{\mathrm{red}}`;
- extend it analytically to the exceptional divisor `\delta=0`;
- prove the resulting value is independent of `\kappa` and equals the one-pair datum `\widehat\Psi(m)`.

This is stronger and sharper than the old slogan “same reduced image germ at coincidence.”

### Exact blocker

Current hypotheses still allow a free analytic exceptional-divisor term

\[
B(m,\kappa)=\widetilde\Psi^{\corr}_{\mathrm{red}}(m,\kappa,0).
\]

Nothing in the current draft kills this term except a genuine diagonal-merger / collision-functoriality theorem for the actual corrected two-atom package.

### What is needed to prove C

Minimal obligations:

1. define the corrected reduced two-atom package cleanly;
2. prove analytic extension to the exceptional divisor;
3. prove the exceptional-divisor value is `\kappa`-independent;
4. identify that common value with `\widehat\Psi(m)`.

### Immediate consequence if C lands

`B` becomes formal: the exact fixed-shear package theorem reduces to diagonal coincidence / diagonal merger, which is then automatic.

## 8. Bottleneck B: Exact Fixed-Shear Package Theorem

### Sharp current statement

The residual exact fixed-shear corner already has a formal closure theorem once the package inputs are known.

Best current statement:

- along the involutive branch
  \[
  (h_1,h_2)=(h(t),h(-t))
  \]
  the actual corrected cubic/quintic defects are swap-even and vanish on coincidence;
- then the already-written local theorem excludes the residual bad branch on any compact good patch.

### Exact blocker

The exact blocker is the **diagonal merger law** for the actual corrected two-atom package:

\[
\mathfrak P_2(a_1,h;a_2,h)=(a_1+a_2)F_h.
\]

This is equivalent to the diagonal coincidence statement behind

\[
E_{12}^{(3)}(0)=E_{12}^{(5)}(0)=0.
\]

### Important refinement

On the quartic--sextic rung, if state-locality through the descended transport state `\mathcal T_N` were proved, swap-evenness would be automatic; only diagonal coincidence would still need to be checked.

## 9. Bottleneck D: Hidden Extraction Theorem

### Final current form

The sharp theorem is no longer coefficient-level but transform-level:

The first nonzero finite-core transformed scalar `\Xi_{\zeta,\le H}^{(N)}` and hence the first surviving odd order and leading coefficient of `H_m` are determined by the corrected coincidence package modulo `\Phi_K`-invisible directions.

Equivalent strong form:

\[
H_{\core}=\mathfrak H(\mathcal P_{\mathrm{red}})
\]

or at minimum:

equal package fibers imply the same first surviving odd order and the same leading coefficient.

### What failed

Equal reduced-`\widehat\Psi` fibers are **not** enough.

Reason: the septic gauge direction

\[
A_7^{\mathfrak f}\mapsto A_7^{\mathfrak f}+\chi A_5^{\mathfrak f}
\]

is generically `\Phi_K`-visible because

\[
\Phi_K(A_5^{\mathfrak f})=2v_5.
\]

So the reduced package must remember at least one extra visible scalar.

### Best current enlarged proxy

The sharpest current diagnostic package is

\[
\widetilde\Psi_{\min}=(x,Y,S,T)
=
\left(\frac{v_5}{c},\frac{u_5}{c},\frac{\Delta_7}{cv_5},\frac{v_7}{c}\right).
\]

This is **not** yet the canonical theorem object, but it is the best current proxy for the hidden visible scalar.

### Exact blocker after latest convergence

The hidden-state theorem has been reduced to:

equal corrected package fibers force the corrected block difference to lie in

\[
\ker\Phi_K=\mathbf C I\oplus\mathbf C D\oplus\mathbf C K
\]

through the first surviving odd order.

That is the single exact theorem blocker still missing.

## 10. What the GRH Subtree Gave Us

The `grh/20260423-050630-research-team-grh-dirichlet-tau/` subtree is useful, but mostly for **source-boundary hygiene**, not for closing `UV-002`.

Main useful contribution:

- the RH paper should probably contain a canonical zeta-source package subsection clarifying `q`, `B_zeta`, `S`, and the strip-edge kernel.

This helps provenance and dependency clarity around the zeta-side decomposition, but it does **not** close the finite-core fork.

## 11. What Should Be Reused From Prior Chats

### Clean-branch scalar descent

The archive does contain a stronger theorem sketch than the current paper text around the centered clean branch, especially:

- a canonical scalar extraction from the cubic line,
- a `\Sigma`-invariance argument,
- a conditional derivation of
  \[
  c(-s)=c(s),\qquad C_1=C_3=C_5=0.
  \]

But it is still conditional on the reflection symmetry hypothesis `R_{-s}=\Sigma(R_s)`. So that local line is useful support, but it is not the present main queue.

### Exact fixed-shear package theorem

Prior chats supplied stronger conditional statements (state-locality on the descended transport packet, stronger merger-law collapse), but not an unconditional theorem for the actual corrected two-atom package.

### UV-004 bridge

Prior chats supplied stronger detector-slice scaffolding, especially the section-corrected quintic-gap detector `\delta\Gamma` and the source detector matrix, but not a closed positive or negative theorem. The current draft still only supports a conditional dichotomy there.

## 12. Practical Restart Instructions For Another Team Lead

If another lead continues from here, the most productive next actions are:

1. Stay in the current team dir:
   `paper/teams/20260424-093000-attack-fund-uv002-hidden-extraction/`

2. Read in this order:
   - `dispatch.md`
   - `findings.md`
   - `uv.md`
   - `collation.md`
   - `summary.md`
   - this `handoff.md`
   - relevant deposited `agents/*/report.md`

3. Keep the active queue as:
   - `C` (diagonal-collapse / collision-functoriality)
   - `D` (hidden-state / package-to-transform)
   - contradiction

4. Keep local lanes only as support beneath that.

5. Require new agents to self-deposit under `paper/teams/.../agents/<slug>/report.md` before accepting their conclusions.

## 13. Shortest Current Handoff Statement

We are converging on a theorem, but not yet on the final theorem.

The exact current obstruction is no longer “more local analysis.” It is:

1. define the actual corrected reduced two-atom package cleanly enough;
2. prove its diagonal-collapse / collision-functoriality theorem at the exceptional divisor;
3. prove that package fibers determine the first surviving odd transform / odd jet up to `\Phi_K`-invisible directions;
4. then the existing `\Xi_\zeta^{(N)}` machinery should finish the finite-core contradiction.

That is the current sharpest possible picture.
