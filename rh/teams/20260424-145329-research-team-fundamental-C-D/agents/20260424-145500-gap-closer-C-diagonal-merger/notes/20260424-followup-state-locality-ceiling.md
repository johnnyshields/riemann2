Second follow-up on C2a after mining the fixed-shear package-collapse apparatus.

Main extraction:

1. The fixed-shear/involutive package gives a strong conditional implication:
   state-locality through a descended finite transport state + merger law => package collapse on that branch.
2. But the same section explicitly records the ceiling: it does NOT prove the actual corrected package is state-local in that descended state, and if any obstruction remains it must come from genuinely relational/provenance-sensitive two-atom structure or non-finite-order dependence.
3. Therefore the next clean split is not merely
   C2a = factor through descended state,
   C2b = diagonal coincidence,
   but
   - C2a(qv): factor through finite quotient-visible descended transport state;
   - C2a(rel): identify the first extra relational/provenance-sensitive state beyond that quotient-visible data, if C2a(qv) fails;
   - C2b: diagonal coincidence on the correct state space.

So the present draft does more than suggest a route. It rules out, from the fixed-shear tested scope alone, the hope that another local quotient-visible finite-order transport scalar will kill B(m,kappa). Any successful C-theorem must either:
- prove the actual corrected package is already state-local in the descended quotient-visible state after all, or
- identify the minimal extra relational/provenance-sensitive two-atom state needed, or
- use genuinely non-finite-order dependence.

This is the current ceiling/redirect for the C attack.