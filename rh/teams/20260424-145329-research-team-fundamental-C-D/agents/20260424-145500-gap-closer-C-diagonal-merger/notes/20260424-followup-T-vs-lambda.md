Fourth follow-up on C: test whether the patchwise candidate extra state should be written as T=v_7/c or instead by the canonical good-patch lift coordinate lambda/c.

Readouts:

1. On v_5\neq0, the paper's canonical local gauge-fixing sets v_7^{new}=0 and u_7^{new}=\Delta_7/v_5. This gives the canonical good-patch lift coordinate
   \[
   \frac{\lambda}{c}=\frac{\Delta_7}{c v_5}.
   \]
2. By contrast, T=v_7/c measures the raw representative before gauge-fixing. Since
   \[
   (u_7^{new},v_7^{new})=(u_7,v_7)+c_2(u_5,v_5),
   \]
   T is gauge-dependent, while \(\lambda/c\) is the canonical section value after fixing v_7^{new}=0.
3. The paper explicitly says the quotient-septic class is canonically closed but still locally free; after lower data are fixed, the remaining order-7 theorem is provenance-sensitive, not raw representative equality.

Conclusion:

- T=v_7/c is useful as a local raw hidden parameter on a chosen representative, but it is not the best invariant candidate for the extra state.
- The better patchwise theorem target for C is the canonical lift coordinate
  \[
  \frac{\lambda}{c}=\frac{\Delta_7}{c v_5},
  \]
  or equivalently the affine-line section selected by the local gauge v_7^{new}=0.
- Thus the extra one-dimensional state for C should be phrased as an affine lift coordinate with canonical local section value \(\lambda/c\), not as the raw coordinate T itself.

Strongest objection: this still lives on the good patch v_5\neq0 and in one-pair/septic geometry. It does not yet construct the corresponding corrected two-atom exceptional-divisor state on the full collision chart.