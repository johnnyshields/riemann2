Third follow-up on C2a(rel): search for the smallest named relational/provenance-sensitive state beyond quotient-visible descent.

Best candidate extracted from current materials:

1. On the good patch \(v_5\neq 0\), equal reduced-package fibers leave exactly one uncontrolled \(\Phi_K\)-visible scalar,
   \[
   T:=\frac{v_7}{c}.
   \]
   This is already recorded in current-cycle collation.
2. Explorer-hidden-state geometry identifies the hidden visible state more invariantly as the affine lift-coordinate along the septic gauge line
   \[
   A_7^{\mathfrak f}+\mathbf C A_5^{\mathfrak f},
   \]
   equivalently the choice of lift of the quotient-septic class inside \(\mathfrak f=\operatorname{span}\{I,S\}\).
3. Prior strategist notes sharpen the two-point obstruction further: the missing datum is not another scalar built from one-point invariants, but the first relational/provenance-sensitive two-atom datum beyond the descended secant-shadow / quotient-transport state.

Synthesis:

- Local patch expression of the candidate extra state: \(T=v_7/c\) on \(v_5\neq 0\).
- Invariant interpretation: affine lift-coordinate of the corrected septic class over the reduced package base.
- Global theorem shape for C: the exceptional-divisor trace must factor through
  \[
  (\text{descended quotient-visible collision state})\ +\ (\text{affine lift-coordinate }T)
  \]
  and diagonal merger must force that affine coordinate to be canonical/trivial at coincidence.

So the smallest currently named candidate for C2a(rel) is one-dimensional: the affine septic lift coordinate, patchwise represented by \(T=v_7/c\). This is smaller and sharper than an unspecified “provenance-sensitive package state,” but still not proved to be the whole surviving state on the full collision chart.

Strong objection: this candidate is best-supported on the good patch \(v_5\neq 0\) and from one-pair/septic-lift geometry; it is not yet a proved full-chart two-atom state for the actual corrected package.