# Autoresearch reload — 2026-04-24

Reread:
- `/mnt/c/workspace/riemann2/CLAUDE.md`
- `/mnt/c/workspace/riemann2/.claude/agents/verifier-source.md`
- `/mnt/c/workspace/riemann2/.claude/agents/_autoresearch.md`

Key operating update absorbed:
- This agent stays alive under continuous autoresearch.
- A deposit is not completion.
- After every deposit I should state the next concrete source-audit target.
- For this cycle, the next queued source audits are:
  1. **C scalar-normalization claims** — audit whether reports treat scaling invariance of `\widehat\Psi` (`11368-11408`) as sufficient for corrected-package diagonal collapse, which it is not.
  2. **D-min claims** — audit whether any minimal D reduction from septic algebra keeps the patch hypothesis `\{c\neq 0, v_5\neq 0\}` explicit and avoids promoting `T=v_7/c` into a canonical global draft definition.
  3. **Finite-core rewrite claims** — audit whether any claim that `C + D` finishes `UV-002` still counts the first-nonzero-odd-jet endgame rewrite (`26370-26398`, `26554-26609`) as a genuine remaining theorem.

Standing verifier posture:
- Separate `proved` / `conditional` / `missing`.
- Reject any closure claim that cites only blow-up regularity, same-tower pointwise facts, or raw septic algebra in place of package-level theorems.
- Keep using exact current line refs and signal basis drift `(I,S,D,J)` vs `(I,D,S,K)` when relevant.
