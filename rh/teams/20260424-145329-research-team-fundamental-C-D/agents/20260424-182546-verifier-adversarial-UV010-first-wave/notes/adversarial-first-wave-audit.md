# Adversarial First-Wave Audit Notes

Pinned checks from the brief:

1. The theorem boundary is safe only if UV-010 states a definition-level edge
   package: construct the fixed-midpoint quotient edge law and leave descent,
   \(\kappa\)-independence, determinant constancy, and merger normalization for
   later.
2. The determinant pairing gives a canonical scalar chart only on
   \(A_5^{\mathfrak f}(m)\neq0\). It is coordinate infrastructure, not
   descent.
3. The \(A_5^{\mathfrak f}=0\) obstruction is a rank jump. The smallest
   convention is either to exclude the locus or choose a projectivized/prepared
   limiting line \(\ell\in\mathbf P(\mathfrak f)\) before forming a quotient.
4. The centered \(D_2=2\kappa(AV_1-BU_1)\) calculation is a comparison target.
   The source does not identify it with the actual two-atom quotient Hessian
   \(\mathfrak O_7\).
5. Scalar-only formulations fail the formal \(a_1a_2\delta^2P(m,\kappa)\)
   test: determinant scalarization preserves the free septic coordinate unless
   the actual package computes or forbids it.

Script:
`scripts/uv010_first_wave_adversarial_check.js`

Script hash:
`cb36def17e0eb5b64bfa91c65536c58d5c1f4bb6`
