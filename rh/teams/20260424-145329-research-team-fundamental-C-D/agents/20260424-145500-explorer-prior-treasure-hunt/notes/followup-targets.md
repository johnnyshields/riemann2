# Follow-up targets after autoresearch reload

## Immediate next treasure-hunt targets

1. **C scalar normalization / exceptional-divisor convention**
   Mine prior-cycle reports for the exact statement that must be frozen before C is theorem-ready:
   `\widetilde\Psi^{\corr}_{\mathrm{red}}(m,\kappa,0)` means the analytically continued blow-up value on the exceptional divisor after `2\omega=\kappa\delta`, not a raw diagonal restriction.

2. **D-min vs canonical D**
   Separate the abstract hidden-state / package-to-transform theorem from the local proxy `\widetilde\Psi_{\min}=(x,Y,S,T)`. The key negative to preserve is that `\widetilde\Psi_{\min}` is exactly normalized septic data, so by itself it still needs finite determination or a uniform bound `N\le 4`.

3. **Finite-core endgame rewrite**
   Mine reports for the cleanest post-C rewrite of the theorem queue: what becomes formal after C, what remains as D, and which local support fronts stay support-only rather than queue heads.

## Candidate capture misses to test

- Whether current collation captures the exceptional-divisor convention sharply enough.
- Whether it overstates `C \Rightarrow B` without explicitly noting the required scalar normalization.
- Whether it captures the exact obstruction that D-min is only order-7 data and therefore not yet the canonical theorem object.
