# RH Proof Agent 3

- Source: https://chatgpt.com/c/69eccade-5f74-83e8-82c1-0fa28dcab954
- Created: 2026-04-25 14:08:53 UTC
- Default model: gpt-5-5-thinking

---

## User (2026-04-25 14:08:52)

We are trying to prove the RH. Here's the paper to base it on. Do the Agent 3 instruction.

## Assistant (2026-04-25 14:08:53)

I’ll treat this as the Agent 3 task: audit the \(T^\Theta\) package-to-transform bridge, not re-litigate the old local defect. I’ll pull the relevant definitions from the draft, then give a theorem-level verdict with exact failure/survival conditions.

The paper already contains the old augmented bridge: quotient data alone does not determine \(\Phi_K(A_7^{\mathfrak f})\), so the added coordinate \(T=v_7/c\) is necessary. The new question is narrower: whether replacing the old package by the \(\Theta_\pm\)-normal package gives a legitimate \(T^\Theta=v_7^\Theta/c\) readout, or merely hides the same physical coefficient in the coordinate map.

_Thought for 1m 19s_

## Agent 3 verdict

\[
\boxed{
\text{The }T^\Theta\text{ readout is algebraically coherent, but only as a corrected-package readout.}
}
\]

It is **not** justified if \(\Theta_\pm\) is treated as a mere coordinate relabeling of the same physical finite-\(s\) block. In that case the old \(\eta\eta\) curvature reappears through the source-to-\(\Theta\) chain rule. The repair works only if the \(\Theta_\pm\)-normal form is made part of the **definition of the corrected raw package before forming** \(C_{\mathcal W}^{\rm raw,\Theta}\), exactly as the handoff warns. 

---

# 1. Define the corrected septic fixed-sector coefficient

Let the \(\Theta\)-normal corrected two-point germ be

\[
\widehat\Omega^{\corr,\Theta}(z;m)
=
A_3^\Theta z^3+A_5^\Theta z^5+A_7^\Theta z^7+O(z^9).
\]

Project to the fixed sector

\[
\mathfrak f_m=\mathbf C I\oplus \mathbf C S.
\]

Define

\[
A_7^{\mathfrak f,\Theta}
:=
\pi_{\mathfrak f} A_7^\Theta.
\]

Then write

\[
\boxed{
A_7^{\mathfrak f,\Theta}
=
u_7^\Theta I+v_7^\Theta S.
}
\]

The \(\Theta\)-augmented hidden coordinate is therefore

\[
\boxed{
T^\Theta:=\frac{v_7^\Theta}{c}.
}
\]

Here \(c\) is the same cubic normalization parameter, assuming the \(\Theta_\pm\) repair starts at the promoted nonlinear \(\eta\eta\) level and does not alter the cubic normalizing coefficient. If a later audit finds that the repair modifies \(c\), the safe version is

\[
T^\Theta:=\frac{v_7^\Theta}{c^\Theta}.
\]

But under the current intended repair, \(c^\Theta=c\).

---

# 2. The \(\Phi_K\)-readout survives

Since

\[
\Phi_K(X)=x_{12}+x_{21},
\]

and

\[
I=
\begin{pmatrix}
1&0\\
0&1
\end{pmatrix},
\qquad
S=
\begin{pmatrix}
0&1\\
1&0
\end{pmatrix},
\]

we have

\[
\Phi_K(I)=0,
\qquad
\Phi_K(S)=2.
\]

Therefore

\[
\Phi_K(A_7^{\mathfrak f,\Theta})
=
\Phi_K(u_7^\Theta I+v_7^\Theta S)
=
2v_7^\Theta.
\]

Thus

\[
\boxed{
\Phi_K(A_7^{\mathfrak f,\Theta})
=
2v_7^\Theta
=
2cT^\Theta.
}
\]

So the formal readout theorem is valid:

\[
\boxed{
\frac1c [z^7]\Phi_K(\widehat\Omega^{\corr,\Theta}(z;m))
=
2T^\Theta.
}
\]

This is the exact \(\Theta\)-analogue of the old augmented readout theorem.

---

# 3. The important distinction: coordinate change vs package change

This is the decisive point.

The off-diagonal normal variables are

\[
\Theta_+=\sin\Delta+q_+s\cos\Delta,
\qquad
\Theta_-=\sin\Delta+q_-s\cos\Delta.
\]

Then

\[
N_{12}^{\Theta}=\frac{\Theta_+}{s^2},
\qquad
N_{21}^{\Theta}=-\frac{\Theta_-}{s^2}.
\]

As functions of the variables \(\Theta_\pm\), these are affine, so

\[
D^2_{\Theta_+}N_{12}^{\Theta}=0,
\qquad
D^2_{\Theta_-}N_{21}^{\Theta}=0.
\]

But along an actual source deformation \(\lambda\mapsto(q_\pm(\lambda),\Delta(\lambda))\),

\[
\frac{d^2}{d\lambda_i d\lambda_j}N_{21}^{\Theta}
=
-\frac1{s^2}
\frac{d^2}{d\lambda_i d\lambda_j}\Theta_-(\lambda).
\]

So if \(\Theta_\pm\) is merely a coordinate relabeling of the same physical block, the quadratic source curvature has not disappeared. It has moved from the Hessian of \(N_{21}\) into the Hessian of the map

\[
(q_-,q_+,\Delta)\mapsto \Theta_-.
\]

That is exactly why the handoff says \(\Theta_\pm\) must be treated as a **real package redesign**, not a harmless relabeling, and why \(T^\Theta\) must be computed after the normal-form correction. 

Therefore:

\[
\boxed{
\text{Pure coordinate-change interpretation: no repair.}
}
\]

\[
\boxed{
\text{Corrected-package interpretation: possible repair, conditional on compatibility.}
}
\]

---

# 4. Corrected Bottleneck C condition

The old transform-safe Bottleneck C condition was not just quotient-horizontal merger. It also required hidden-horizontal merger.

For the \(\Theta\)-normal package, the correct condition is:

\[
\boxed{
\partial_\kappa Q_{7,m}^{q,\Theta}(m,\kappa,0)=0
}
\]

and

\[
\boxed{
\partial_\kappa T^\Theta(m,\kappa,0)=0.
}
\]

Equivalently, if

\[
P_7^\Theta(a_1,a_2)
=
[z^7][\delta^1]\tau_{h\to m}\pi_{\mathfrak f_h}
(1-\Pi_{1,1})_{\rm mix}
C_{\mathcal W}^{\rm raw,\Theta},
\]

then hidden-horizontal merger is

\[
\boxed{
\frac1c
\Phi_K\!\left(
\partial_{a_2}P_7^\Theta(a_1,-a_1)
\right)
=
0.
}
\]

Writing

\[
\partial_{a_2}P_7^\Theta(a_1,-a_1)
=
e_{A,Y}^\Theta I+f_{A,Y}^\Theta S,
\]

this is simply

\[
\boxed{
f_{A,Y}^\Theta=0.
}
\]

So the \(\Theta\)-normal package closes the hidden Bottleneck C row exactly when the repaired package kills the \(S\)-component of the promoted carrier.

The old obstruction had

\[
\partial_\kappa T
=
-a_1^2\frac{f_{A,Y}}{c}.
\]

The \(\Theta\)-repair must replace this by

\[
\partial_\kappa T^\Theta
=
-a_1^2\frac{f_{A,Y}^\Theta}{c}.
\]

Thus the target is

\[
\boxed{
f_{A,Y}^\Theta=0,
}
\]

not merely

\[
e_{A,Y}^\Theta v_5-u_5f_{A,Y}^\Theta=0.
\]

The quotient condition can still hold while \(T^\Theta\) leaks, especially on \(v_5\neq0\).

---

# 5. Mismatch test against the physical finite-\(s\) transform

The required compatibility theorem is:

\[
\boxed{
[z^7]\Phi_K
\left(
\widehat\Omega_\zeta^{\corr}(z;m)
-
\widehat\Omega^{\corr,\Theta}(z;m)
\right)
=
0
}
\]

or, more generally, that the difference is a known correction already included in \(T^\Theta\).

Without this theorem, \(T^\Theta\) is only the readout of the redesigned package, not necessarily the readout of the physical zeta-side finite-\(s\) transform.

So the valid package-to-transform bridge should be stated as:

\[
\boxed{
\text{If }\widehat\Omega^{\corr,\Theta}\text{ is the corrected finite-}s
\text{ transform representative, then }
\Phi_K(A_7^{\mathfrak f,\Theta})=2cT^\Theta.
}
\]

But the stronger statement

\[
\boxed{
\text{The physical zeta transform automatically has septic state }T^\Theta
}
\]

is **not yet proved**.

---

# 6. Theorem-ready formulation

```latex
\begin{definition}[\(\Theta\)-normal transform-safe augmented package]
Let
\[
\widehat\Omega^{\corr,\Theta}(z;m)
=
A_3^\Theta z^3+A_5^\Theta z^5+A_7^\Theta z^7+O(z^9)
\]
be the corrected finite-\(s\) germ after applying the
\(\Theta_\pm\)-normal off-diagonal package correction.  On the fixed sector
\[
\mathfrak f_m=\mathbf C I\oplus \mathbf C S
\]
write
\[
A_7^{\mathfrak f,\Theta}
:=
\pi_{\mathfrak f}A_7^\Theta
=
u_7^\Theta I+v_7^\Theta S.
\]
On a good patch with \(c\ne0\), define
\[
T^\Theta:=\frac{v_7^\Theta}{c}.
\]
The \(\Theta\)-normal transform-safe augmented package is
\[
\widehat\Psi_{\rm red}^{+,\Theta}
:=
(\widehat\Psi_{\rm red}^{\Theta},T^\Theta).
\]
\end{definition}

\begin{theorem}[\(\Theta\)-normal augmented package-to-transform readout]
Assume that \(\widehat\Omega^{\corr,\Theta}\) is the corrected finite-\(s\)
transform representative after the \(\Theta_\pm\)-normal package correction.
Then
\[
\Phi_K(A_7^{\mathfrak f,\Theta})
=
2v_7^\Theta
=
2cT^\Theta.
\]
Equivalently,
\[
\frac1c[z^7]\Phi_K(\widehat\Omega^{\corr,\Theta}(z;m))
=
2T^\Theta.
\]
Thus the first \(\Phi_K\)-visible septic state of the
\(\Theta\)-normal corrected package is determined by
\(\widehat\Psi_{\rm red}^{+,\Theta}\), not by the quotient package alone.
\end{theorem}

\begin{theorem}[\(\Theta\)-normal hidden Bottleneck C condition]
Let
\[
P_7^\Theta(a_1,a_2)
=
[z^7][\delta^1]\tau_{h\to m}\pi_{\mathfrak f_h}
(1-\Pi_{1,1})_{\rm mix}C_{\mathcal W}^{\rm raw,\Theta}.
\]
Then the \(\Theta\)-normal transform-safe Bottleneck C condition is
\[
\partial_\kappa Q_{7,m}^{q,\Theta}(m,\kappa,0)=0
\]
and
\[
\partial_\kappa T^\Theta(m,\kappa,0)=0.
\]
Equivalently,
\[
\frac1c\Phi_K\!\left(
\partial_{a_2}P_7^\Theta(a_1,-a_1)
\right)=0.
\]
If
\[
\partial_{a_2}P_7^\Theta(a_1,-a_1)
=
e_{A,Y}^\Theta I+f_{A,Y}^\Theta S,
\]
then the hidden condition is precisely
\[
f_{A,Y}^\Theta=0.
\]
\end{theorem}

\begin{remark}[Coordinate relabeling is insufficient]
The identities
\[
N_{12}^{\Theta}=\Theta_+/s^2,\qquad
N_{21}^{\Theta}=-\Theta_-/s^2
\]
make the off-diagonal entries affine as functions of \(\Theta_\pm\).
However, if \(\Theta_\pm\) is only a coordinate relabeling induced by
\[
(q_-,q_+,\Delta)\mapsto(q_-,q_+,\Theta_-,\Theta_+),
\]
then the second source derivative of \(N_{21}^{\Theta}\) contains
\[
-\frac1{s^2}D^2_{\source}\Theta_-,
\]
and the old \(\eta\eta\) curvature is not removed.  Hence the
\(\Theta_\pm\)-normal construction must be part of the corrected package
before forming \(C_{\mathcal W}^{\rm raw,\Theta}\).  Otherwise
\(T^\Theta\) is merely the old \(T\) in disguised coordinates.
\end{remark}

\begin{theorem}[Required physical compatibility condition]
The \(\Theta\)-normal package-to-transform bridge applies to the physical
zeta-side transform only if
\[
[z^7]\Phi_K
\left(
\widehat\Omega_\zeta^{\corr}(z;m)
-
\widehat\Omega^{\corr,\Theta}(z;m)
\right)
=
0,
\]
or if the displayed difference is a known correction already included in
\(T^\Theta\).  Without this compatibility theorem, the
\(\Theta\)-normal package determines only the transformed state of the
redesigned package, not the physical finite-\(s\) zeta transform.
\end{theorem}
```

## Bottom line

\[
\boxed{
T^\Theta=\frac{v_7^\Theta}{c}
\text{ is the right hidden coordinate for the repaired package.}
}
\]

\[
\boxed{
\Phi_K(A_7^{\mathfrak f,\Theta})=2cT^\Theta
\text{ holds tautologically once }A_7^{\mathfrak f,\Theta}
\text{ is defined.}
}
\]

\[
\boxed{
\text{The missing theorem is not the readout algebra; it is physical compatibility.}
}
\]

So Agent 3’s answer is: **conditional bridge yes; coordinate-only repair no.**
## User (2026-04-25 14:17:30)

You are Agent C, do your instruction in this message.

## Assistant (2026-04-25 14:17:43)

I found the specific Agent C target in the attached instruction. The key test is not whether \(T^\Theta\) is algebraically definable; it is whether the curvature removed by the \(\Theta_\pm\)-package is \(\Phi_K\)-invisible in the physical finite-\(s\) transform.

_Thought for 40s_

## Agent C — physical compatibility of \(T^\Theta\)

### Verdict

\[
\boxed{
[z^7]\Phi_K(\widehat\Omega_\zeta^{\corr}-\widehat\Omega^{\corr,\Theta})=0
\text{ is not currently provable and is generically false under the known top-row model.}
}
\]

The \(\Theta_\pm\)-normal form is **not a pure gauge correction** at the level needed for \(T^\Theta\). It changes the septic \(\Phi_K\)-visible transform representative unless a separate aggregate cancellation theorem proves that the removed source-curvature package is \(\Phi_K\)-invisible.

So the correct output of Agent C is:

\[
\boxed{
\text{Do not use }T^\Theta=v_7^\Theta/c\text{ alone as the physical hidden coordinate.}
}
\]

Use instead a corrected physical coordinate

\[
\boxed{
T^{\Theta,\phys}
:=
T^\Theta+\frac{1}{2c}\Delta_7^\Theta,
}
\]

where

\[
\boxed{
\Delta_7^\Theta
:=
[z^7]\Phi_K
\left(
\widehat\Omega_\zeta^{\corr}
-
\widehat\Omega^{\corr,\Theta}
\right).
}
\]

The handoff already identifies exactly this fork: either prove the difference is zero, or explicitly include it in \(T^\Theta\). 

---

# 1. The formal readout is fine but not physical

For the redesigned package,

\[
A_7^{\mathfrak f,\Theta}
=
u_7^\Theta I+v_7^\Theta S,
\qquad
T^\Theta=\frac{v_7^\Theta}{c}.
\]

Then

\[
\Phi_K(A_7^{\mathfrak f,\Theta})
=
2v_7^\Theta
=
2cT^\Theta.
\]

This is purely algebraic. It says the redesigned package has a valid \(T^\Theta\)-readout. It does **not** say that \(T^\Theta\) is the septic hidden coordinate of the physical zeta transform.

The physical statement would require

\[
[z^7]\Phi_K
\left(
\widehat\Omega_\zeta^{\corr}
-
\widehat\Omega^{\corr,\Theta}
\right)
=0.
\]

That is the compatibility theorem Agent C was asked to attack.

---

# 2. Why the compatibility theorem fails under current hypotheses

The \(\Theta_\pm\)-normal package removes the off-diagonal \(\eta\eta\) Hessian by declaring

\[
N_{12}^{\Theta}=\frac{\Theta_+}{s^2},
\qquad
N_{21}^{\Theta}=-\frac{\Theta_-}{s^2}
\]

to be affine in \(\Theta_\pm\).

But the actual source does not give free affine \(\Theta_\pm\). It gives constrained variables

\[
\Theta_\pm=\sin\Delta+q_\pm s\cos\Delta.
\]

Thus the actual second source derivative contains

\[
D_{\source}^2\Theta_\sigma
=
A_\sigma\eta_{ij}
+
sC\,\chi_{\sigma,ij}
-
\Theta_{\sigma,0}\eta_i\eta_j
-
sS(\chi_{\sigma,i}\eta_j+\chi_{\sigma,j}\eta_i).
\]

In the pure \(\eta\eta\) channel,

\[
D_{\source}^2\Theta_\sigma
=
-\Theta_{\sigma,0}\eta_i\eta_j.
\]

Therefore

\[
D_{\source}^2N_{21}^{\Theta,\actual}
=
-\frac{1}{s^2}D_{\source}^2\Theta_-
=
\left(
\frac{\sin\Delta_0}{s^2}
+
\frac{q_-\cos\Delta_0}{s}
\right)\eta_i\eta_j.
\]

That is exactly the old \(\nu_{21,7}\) residual channel. The attached audit states this directly: if \(\Theta_\pm\) is only a coordinate change of the actual source functor, the old curvature returns by the chain rule. 

So the difference

\[
\widehat\Omega_\zeta^{\corr}
-
\widehat\Omega^{\corr,\Theta}
\]

contains precisely the source-curvature term removed by the formal \(\Theta_\pm\)-normal package.

---

# 3. It is \(\Phi_K\)-visible, not gauge-invisible

The old top-row contribution was

\[
f_{\eta\eta}^{\rm top}
=
-\frac{u}{24\pi q^3}\nu_{21,7}.
\]

Under the favorable integral model,

\[
\nu_{21,7}
=
\frac{1}{Q^{14}}
\left[
\frac{q_0}{480}
(\rho_{i,2}\rho_{j,4}+\rho_{i,4}\rho_{j,2})
+
\frac{1}{144}
\left(
\frac{q_0''}{6}-\frac{2q_0^3}{3}
\right)
\rho_{i,2}\rho_{j,2}
\right].
\]

For the homogeneous source

\[
r_i=r_j=u^2,
\qquad
q_0''=0,
\]

this gives

\[
\nu_{21,7}
=
-\frac{q_0^3}{216Q^{14}},
\]

which is nonzero on a good patch. The uploaded handoff also records that this residual survives affine removal, endpoint symmetry, source-grade parity, and the existing Bottleneck-C bookkeeping. 

Since this row contributes an \(S\)-component,

\[
A_{\eta\eta,7}^{\top}
=
f_{\eta\eta}^{\rm top}S,
\]

we get

\[
\Phi_K(A_{\eta\eta,7}^{\top})
=
2f_{\eta\eta}^{\rm top}
=
-\frac{u}{12\pi q^3}\nu_{21,7}.
\]

For the homogeneous source,

\[
\Phi_K(A_{\eta\eta,7}^{\top})
=
\frac{u q_0^3}{2592\pi q^3Q^{14}},
\]

which is nonzero on a good patch.

Therefore:

\[
\boxed{
\text{The removed curvature is not }\Phi_K\text{-invisible.}
}
\]

It is not a gauge term unless a new aggregate theorem cancels it against the other curvature channels.

---

# 4. Exact correction term to include

Let

\[
\mathcal Y_{ij}^{\Theta,\curv}
:=
\begin{pmatrix}
0 & D_{\source}^2\Theta_+/s^2\\[2mm]
-D_{\source}^2\Theta_-/s^2 & 0
\end{pmatrix}.
\]

Let \(\mathcal P_7\) denote the full septic raw Gate-B projection operator, including same-point whitening, right whitening, midpoint transport, fixed-sector projection, \((1-\Pi_{1,1})_{\rm mix}\), and symmetrization:

\[
\mathcal P_7(Y)
:=
[z^7][\delta^1]\,
\tau_{h\to m}\pi_{\mathfrak f_h}
(1-\Pi_{1,1})_{\rm mix}
\operatorname{Sym}
\bigl(
\text{all raw whitening insertions with }Y
\bigr).
\]

Then the physical-package mismatch is

\[
\boxed{
D_7^\Theta
:=
\mathcal P_7(\mathcal Y_{ij}^{\Theta,\curv}).
}
\]

The scalar \(\Phi_K\)-visible mismatch is

\[
\boxed{
\Delta_7^\Theta
:=
\Phi_K(D_7^\Theta).
}
\]

Therefore the physical hidden coordinate must be

\[
\boxed{
T^{\Theta,\phys}
=
T^\Theta+\frac{1}{2c}\Delta_7^\Theta.
}
\]

Equivalently, if

\[
D_7^\Theta
=
\alpha_7^\Theta I+\beta_7^\Theta S,
\]

then

\[
\Delta_7^\Theta=2\beta_7^\Theta,
\]

and

\[
\boxed{
T^{\Theta,\phys}
=
T^\Theta+\frac{\beta_7^\Theta}{c}.
}
\]

In the pure top-row \(\eta\eta\) channel,

\[
\beta_{7,\eta\eta}^{\Theta,\top}
=
f_{\eta\eta}^{\rm top}
=
-\frac{u}{24\pi q^3}\nu_{21,7}.
\]

So this channel alone contributes

\[
\boxed{
\Delta T_{\eta\eta}^{\Theta,\top}
=
-\frac{u}{24\pi q^3c}\nu_{21,7}.
}
\]

For the homogeneous model,

\[
\boxed{
\Delta T_{\eta\eta}^{\Theta,\top}
=
\frac{u q_0^3}{5184\pi q^3cQ^{14}}
\ne 0.
}
\]

---

# 5. Correct physical Bottleneck C condition

The formal \(\Theta\)-package condition is

\[
\partial_\kappa T^\Theta=0.
\]

But the physical condition must be

\[
\boxed{
\partial_\kappa T^{\Theta,\phys}=0.
}
\]

That is,

\[
\boxed{
\partial_\kappa
\left(
T^\Theta+\frac{1}{2c}\Delta_7^\Theta
\right)
=0.
}
\]

Equivalently,

\[
\boxed{
\partial_\kappa T^\Theta
=
-\partial_\kappa\left(\frac{1}{2c}\Delta_7^\Theta\right).
}
\]

So even if the redesigned package makes

\[
\partial_\kappa T^\Theta=0,
\]

the physical transform is still not horizontal unless

\[
\partial_\kappa\Delta_7^\Theta=0.
\]

This is the real compatibility condition.

---

# 6. Theorem-ready insert

```latex
\begin{definition}[\(\Theta\)-physical septic correction]
Let \(\widehat\Omega^{\corr,\Theta}\) denote the formally corrected
\(\Theta_\pm\)-normal package, and let
\(\widehat\Omega_\zeta^{\corr}\) denote the corresponding physical finite-\(s\)
zeta-side corrected germ.  Define the septic physical mismatch by
\[
D_7^\Theta
:=
[z^7]\pi_{\mathfrak f}
\left(
\widehat\Omega_\zeta^{\corr}
-
\widehat\Omega^{\corr,\Theta}
\right).
\]
Write
\[
D_7^\Theta=\alpha_7^\Theta I+\beta_7^\Theta S.
\]
Equivalently,
\[
\Delta_7^\Theta
:=
[z^7]\Phi_K
\left(
\widehat\Omega_\zeta^{\corr}
-
\widehat\Omega^{\corr,\Theta}
\right)
=
2\beta_7^\Theta.
\]
The physical \(\Theta\)-hidden coordinate is
\[
T^{\Theta,\phys}
:=
T^\Theta+\frac{\beta_7^\Theta}{c}
=
T^\Theta+\frac{1}{2c}\Delta_7^\Theta.
\]
\end{definition}

\begin{proposition}[The \(\Theta_\pm\)-normal correction is not automatically gauge]
Assume the actual source variables satisfy
\[
\Theta_\sigma=\sin\Delta+q_\sigma s\cos\Delta.
\]
Then the source second derivative contains
\[
D_{\source}^2\Theta_\sigma
=
A_\sigma\eta_{ij}
+sC\,\chi_{\sigma,ij}
-\Theta_{\sigma,0}\eta_i\eta_j
-sS(\chi_{\sigma,i}\eta_j+\chi_{\sigma,j}\eta_i).
\]
In particular, in the pure \(\eta\eta\) channel,
\[
D_{\source}^2N_{21}^{\Theta,\actual}
=
\left(
\frac{\sin\Delta_0}{s^2}
+
\frac{q_-\cos\Delta_0}{s}
\right)
\eta_i\eta_j.
\]
Thus the physical finite-\(s\) transform differs from the formal affine
\(\Theta_\pm\)-normal package by the raw projection of this source-curvature
block.  This difference is \(\Phi_K\)-visible whenever its fixed-sector
\(S\)-component is nonzero.
\end{proposition}

\begin{corollary}[Physical Bottleneck C condition]
The transform-safe Bottleneck C condition for the \(\Theta_\pm\)-normal
package is not merely
\[
\partial_\kappa T^\Theta=0.
\]
It is
\[
\partial_\kappa T^{\Theta,\phys}=0,
\]
equivalently
\[
\partial_\kappa
\left(
T^\Theta+\frac{1}{2c}
[z^7]\Phi_K
\left(
\widehat\Omega_\zeta^{\corr}
-
\widehat\Omega^{\corr,\Theta}
\right)
\right)
=0.
\]
Only if
\[
[z^7]\Phi_K
\left(
\widehat\Omega_\zeta^{\corr}
-
\widehat\Omega^{\corr,\Theta}
\right)=0
\]
may one replace this physical condition by
\[
\partial_\kappa T^\Theta=0.
\]
\end{corollary}
```

---

# Final Agent C conclusion

\[
\boxed{
\Theta_\pm\text{-normalization is not a legitimate gauge correction under the current proof.}
}
\]

It removes a curvature term that is already known to be \(S\)-visible and hence \(\Phi_K\)-visible. Therefore it changes the physical septic transform unless a new aggregate cancellation theorem proves

\[
\Delta_7^\Theta=0.
\]

Until that theorem exists, the only safe hidden coordinate is

\[
\boxed{
T^{\Theta,\phys}
=
T^\Theta+\frac{1}{2c}
[z^7]\Phi_K
\left(
\widehat\Omega_\zeta^{\corr}
-
\widehat\Omega^{\corr,\Theta}
\right).
}
\]

So the \(\Theta_\pm\) repair does **not** currently close the local package. It either needs a proof of \(\Delta_7^\Theta=0\), or it must carry \(\Delta_7^\Theta\) explicitly as a new septic correction term.
## User (2026-04-25 14:22:52)

Do the package redesign agent prompt

## Assistant (2026-04-25 14:22:58)

I’m going to distinguish two designs: a formal package that can be made closed by explicit counterterms, and a transform-faithful package. The first is possible; the second requires accepting the counterterm as part of the physical hidden coordinate, not erasing it.

_Thought for 59s_

# Package redesign agent result

## Verdict

\[
\boxed{
\text{A formal counterterm package can be defined, but it does not close the physical local obstruction.}
}
\]

The best possible redesign is a **septic residual-normal package** that subtracts the two known bad rows:

1. the off-diagonal source-curvature residual coming from \(D_{\rm source}^2\Theta_\pm\);
2. the shifted diagonal \(\eta\eta\) septic residual.

This package preserves Gate A, source-linear Gate B, and \(M_A,M_C\) connection closure because the correction is quadratic-source and order-septic. But it does **not** preserve the physical \(T\)-readout unless the subtracted residual is added back into the physical hidden coordinate.

So this is not a real proof closure. It is a clean isolation of the obstruction.

The attached prompt already records the two nonzero defects and the physical \(T\)-mismatch: \(\Theta_\pm\) reintroduces the old off-diagonal curvature under actual constrained variables, the diagonal shifted row is \(S\)-visible, and \(T^\Theta\) alone is not physical. 

---

# 1. Define the total local residual

Let

\[
\mathcal P_7
\]

denote the full septic fixed-sector raw projection:

\[
\mathcal P_7(Y)
:=
[z^7][\delta^1]\,
\tau_{h\to m}\pi_{\mathfrak f_h}
(1-\Pi_{1,1})_{\rm mix}
\operatorname{Sym}
\bigl(
\text{all allowed same-point/right-whitening insertions with }Y
\bigr).
\]

## 1.1 Off-diagonal source-curvature residual

The constrained \(\Theta_\pm\) curvature block is

\[
\mathcal Y_{\off}^{\Theta,\curv}
=
\begin{pmatrix}
0 & D_{\rm source}^2\Theta_+/s^2\\[2mm]
-D_{\rm source}^2\Theta_-/s^2 & 0
\end{pmatrix}.
\]

Define

\[
R_{\off,7}^{\Theta}
:=
\mathcal P_7(\mathcal Y_{\off}^{\Theta,\curv}).
\]

Write its fixed-sector form as

\[
R_{\off,7}^{\Theta}
=
\alpha_{\off}^{\Theta} I+\beta_{\off}^{\Theta}S.
\]

The homogeneous test already shows

\[
\beta_{\off}^{\Theta}\neq 0
\]

on a good patch.

## 1.2 Diagonal shifted residual

Define

\[
R_{\diag,7}^{\Theta}
:=
\pi_{\mathfrak f}
\operatorname{Sym}
\left(
\Lambda^-_{\bullet,1}
Y^{\eta\eta,\diag}_6
W_{+,0}
+
\Lambda^-_{\bullet,0}
Y^{\eta\eta,\diag}_6
W_{+,1}
\right).
\]

Write

\[
R_{\diag,7}^{\Theta}
=
\alpha_{\diag}^{\Theta}I+\beta_{\diag}^{\Theta}S.
\]

The homogeneous test gives a nonzero \(S\)-coefficient

\[
\beta_{\diag}^{\Theta}
=
-\frac{\lambda}{2\pi Q^{12}}
\left(
\frac{q_0\alpha}{72}
+
\frac{q_0^3\beta}{864}
\right)
\neq0.
\]

## 1.3 Total local residual

Set

\[
\boxed{
R_{\loc,7}^{\Theta}
:=
R_{\off,7}^{\Theta}
+
R_{\diag,7}^{\Theta}.
}
\]

Write

\[
R_{\loc,7}^{\Theta}
=
\alpha_{\loc}^{\Theta}I+\beta_{\loc}^{\Theta}S,
\]

where

\[
\alpha_{\loc}^{\Theta}
=
\alpha_{\off}^{\Theta}+\alpha_{\diag}^{\Theta},
\qquad
\beta_{\loc}^{\Theta}
=
\beta_{\off}^{\Theta}+\beta_{\diag}^{\Theta}.
\]

The known tests imply

\[
\boxed{
\beta_{\loc}^{\Theta}\not\equiv0.
}
\]

---

# 2. Minimal formal corrected package

Define the formally corrected septic-normal package by subtracting the total residual:

\[
\boxed{
C_{\mathcal W}^{\raw,\star}
:=
C_{\mathcal W}^{\raw,\Theta}
-
z^7\,\tau_{m\to h}^{-1}
R_{\loc,7}^{\Theta}.
}
\]

Equivalently, after projection,

\[
\boxed{
\mathcal P_7(C_{\mathcal W}^{\raw,\star})
=
\mathcal P_7(C_{\mathcal W}^{\raw,\Theta})
-
R_{\loc,7}^{\Theta}.
}
\]

This is the canonical minimal counterterm: it subtracts exactly the fixed-sector septic residual produced by the off-diagonal source-curvature channel and the diagonal shifted channel.

Then

\[
A_7^{\mathfrak f,\star}
=
A_7^{\mathfrak f,\Theta}
-
R_{\loc,7}^{\Theta}.
\]

If

\[
A_7^{\mathfrak f,\Theta}
=
u_7^\Theta I+v_7^\Theta S,
\]

then

\[
\boxed{
A_7^{\mathfrak f,\star}
=
(u_7^\Theta-\alpha_{\loc}^{\Theta})I
+
(v_7^\Theta-\beta_{\loc}^{\Theta})S.
}
\]

Define

\[
u_7^\star:=u_7^\Theta-\alpha_{\loc}^{\Theta},
\qquad
v_7^\star:=v_7^\Theta-\beta_{\loc}^{\Theta}.
\]

The package hidden coordinate is

\[
\boxed{
T^\star:=\frac{v_7^\star}{c}
=
T^\Theta-\frac{\beta_{\loc}^{\Theta}}{c}.
}
\]

The formal readout remains valid:

\[
\Phi_K(A_7^{\mathfrak f,\star})
=
2v_7^\star
=
2cT^\star.
\]

---

# 3. Collateral terms through order \(7\)

This counterterm has tightly controlled collateral effects.

## 3.1 Orders \(<7\)

None.

\[
[z^n]C_{\mathcal W}^{\raw,\star}
=
[z^n]C_{\mathcal W}^{\raw,\Theta},
\qquad n<7.
\]

So the cubic and quintic package structure is unchanged.

## 3.2 Source-linear terms

None.

The correction is built from second-source curvature and diagonal \(\eta\eta\) data. Therefore its source-linear variation vanishes:

\[
D_{\rm source} C_{\mathcal W}^{\raw,\star}
=
D_{\rm source} C_{\mathcal W}^{\raw,\Theta}
\]

at the source-linear level.

Consequences:

\[
\boxed{
\text{Gate A is unchanged.}
}
\]

\[
\boxed{
\text{Source-linear Gate B tables are unchanged.}
}
\]

\[
\boxed{
M_A,M_C\text{ connection closure is unchanged.}
}
\]

## 3.3 Septic fixed sector

Only the septic fixed-sector coefficient changes:

\[
u_7^\Theta\mapsto u_7^\Theta-\alpha_{\loc}^{\Theta},
\]

\[
v_7^\Theta\mapsto v_7^\Theta-\beta_{\loc}^{\Theta}.
\]

Thus

\[
T^\Theta\mapsto T^\star
=
T^\Theta-\frac{\beta_{\loc}^{\Theta}}{c}.
\]

## 3.4 Quotient line

If the old quotient test used

\[
Q_{7,m}^{q,\Theta}
\]

from the class of \(A_7^{\mathfrak f,\Theta}\) modulo the fixed \(A_5^{\mathfrak f}\)-line, then the corrected quotient is

\[
\boxed{
Q_{7,m}^{q,\star}
=
Q_{7,m}^{q,\Theta}
-
\left[
R_{\loc,7}^{\Theta}
\right]_{/A_5^{\mathfrak f}}.
}
\]

So the quotient coordinate also shifts unless

\[
R_{\loc,7}^{\Theta}
\in \mathbf C A_5^{\mathfrak f}.
\]

There is no evidence this containment holds generically.

---

# 4. Formal closure theorem

The formal package satisfies the intended local closure by construction:

\[
\mathcal P_7(C_{\mathcal W}^{\raw,\star})
=
\mathcal P_7(C_{\mathcal W}^{\raw,\Theta})
-
R_{\loc,7}^{\Theta}.
\]

If

\[
R_{\loc,7}^{\Theta}
\]

is exactly the total septic \(S\)-visible local residual, then

\[
\boxed{
\pi_S \mathcal P_7(C_{\mathcal W}^{\raw,\star})=0
}
\]

for the two known bad channels.

So as a **formal package redesign**, this works.

But that is not enough.

---

# 5. Physical compatibility problem

The physical zeta-side transform did not lose the residual. The counterterm only subtracts it from the formal package.

Thus

\[
A_7^{\mathfrak f,\phys}
=
A_7^{\mathfrak f,\star}
+
R_{\loc,7}^{\Theta}
+
R_{\rem,7}^{\Theta},
\]

where \(R_{\rem,7}^{\Theta}\) denotes any additional untracked mismatch.

Ignoring \(R_{\rem,7}^{\Theta}\) for the moment, we get

\[
v_7^{\phys}
=
v_7^\star+\beta_{\loc}^{\Theta}.
\]

Therefore the physical hidden coordinate is

\[
\boxed{
T^{\phys}
=
T^\star+\frac{\beta_{\loc}^{\Theta}}{c}.
}
\]

Equivalently,

\[
T^{\phys}
=
T^\Theta.
\]

So the counterterm has not removed the physical obstruction. It has only moved it out of the formal package and into the package-to-transform correction.

The physical Bottleneck C condition is therefore

\[
\boxed{
\partial_\kappa T^{\phys}=0,
}
\]

i.e.

\[
\boxed{
\partial_\kappa
\left(
T^\star+\frac{\beta_{\loc}^{\Theta}}{c}
\right)=0.
}
\]

The formal condition

\[
\partial_\kappa T^\star=0
\]

is insufficient unless

\[
\boxed{
\partial_\kappa\left(\frac{\beta_{\loc}^{\Theta}}{c}\right)=0.
}
\]

But the known homogeneous tests show

\[
\beta_{\loc}^{\Theta}\not\equiv0.
\]

So there is no current reason to expect this.

---

# 6. No-go statement

The following no-go is the real output.

```latex
\begin{proposition}[No transform-faithful septic normal-form repair]
Assume a local normal-form correction \(K_7\) satisfies all of the following:

\begin{enumerate}
\item \(K_7\) is invisible below order \(7\);
\item \(K_7\) is source-quadratic, hence does not alter source-linear Gate A,
Gate B, or the \(M_A,M_C\) connection tables;
\item \(K_7\) cancels the known local septic residual
\[
R_{\loc,7}^{\Theta}
=
\alpha_{\loc}^{\Theta}I+\beta_{\loc}^{\Theta}S;
\]
\item the physical \(\Phi_K\)-readout is unchanged, so no correction term is
added back to the physical hidden coordinate.
\end{enumerate}

Then necessarily
\[
\beta_{\loc}^{\Theta}=0.
\]

In the homogeneous scalar tests recorded for the off-diagonal curvature row and
the diagonal shifted row, one has
\[
\beta_{\loc}^{\Theta}\not\equiv0.
\]
Therefore no such transform-faithful local normal-form correction exists on a
good patch.
\end{proposition}
```

Proof sketch:

To cancel the residual, the correction must have

\[
\pi_S K_7=-\beta_{\loc}^{\Theta}S.
\]

But preserving the physical \(\Phi_K\)-readout without adding a correction requires

\[
\Phi_K(K_7)=0.
\]

Since

\[
\Phi_K(\beta S)=2\beta,
\]

this forces

\[
\beta_{\loc}^{\Theta}=0.
\]

The homogeneous tests give \(\beta_{\loc}^{\Theta}\neq0\). Contradiction.

---

# 7. The only coherent redesigned package

The only coherent redesign is therefore not a closure theorem but an obstruction-isolating package:

\[
\boxed{
\widehat\Psi_{\rm red}^{\star,+}
:=
\left(
\widehat\Psi_{\rm red}^{\star},
T^\star,
\beta_{\loc}^{\Theta}
\right).
}
\]

The physical readout is

\[
\boxed{
T^{\phys}
=
T^\star+\frac{\beta_{\loc}^{\Theta}}{c}.
}
\]

This package is honest: it preserves the formal gates and records the physical mismatch as an explicit state variable.

But it does not prove Bottleneck C. It reduces Bottleneck C to the new condition

\[
\boxed{
\partial_\kappa
\left(
T^\star+\frac{\beta_{\loc}^{\Theta}}{c}
\right)=0.
}
\]

If the corrected formal package gives

\[
\partial_\kappa T^\star=0,
\]

then the remaining necessary condition is

\[
\boxed{
\partial_\kappa\beta_{\loc}^{\Theta}=0
}
\]

up to the derivative of \(c\).

There is no evidence for this. The known tests point the other way.

---

# 8. Final answer

\[
\boxed{
\text{A formal corrected package exists: subtract }R_{\loc,7}^{\Theta}.
}
\]

But:

\[
\boxed{
\text{It does not close the physical local obstruction.}
}
\]

The obstruction is \(\Phi_K\)-visible. Any correction that cancels it in the package must either:

1. change the physical transform, breaking the \(T\)-readout; or
2. add the same residual back into \(T^{\phys}\), preserving the readout but restoring the obstruction.

Therefore:

\[
\boxed{
\text{Local Bottleneck C cannot be closed by normal-form package redesign alone.}
}
\]

The next mathematically honest move is to carry

\[
\beta_{\loc}^{\Theta}
\]

as an explicit physical septic obstruction variable and seek a genuine identity proving

\[
\partial_\kappa\left(\frac{\beta_{\loc}^{\Theta}}{c}\right)=0,
\]

or abandon this local-normal-form route.
## User (2026-04-25 14:26:41)

Do this: Attack the physical edge obstruction
\[
\beta_{\loc}^\Theta
=
\beta_{\off}^\Theta+\beta_{\diag}^\Theta.
\]

Known:
- The off-diagonal constrained-source curvature gives a nonzero homogeneous test coefficient.
- The diagonal shifted septic row gives a nonzero homogeneous test coefficient.
- Normal-form counterterms merely move these into the physical hidden coordinate.

Target:
Determine whether the full physical edge term satisfies
\[
\partial_\kappa(\beta_{\loc}^\Theta/c)=0
\]
after summing all labelled sources, transpose companions, left/right insertions, and amplitude factors.

Do not propose new package coordinates. Do not subtract the term. Work with the physical edge term.

Deliverable:
Either a genuine aggregate cancellation theorem for \(\beta_{\loc}^\Theta\), or a counterexample/model showing \(\partial_\kappa(\beta_{\loc}^\Theta/c)\neq0\).

## Assistant (2026-04-25 14:26:55)

I’ll attack this as a falsification problem: if an aggregate cancellation theorem were true, it must hold in the homogeneous good-patch scalar model. I’ll use that model to test the full edge coefficient rather than introduce any new package machinery.

_Thought for 1m 5s_

# Physical edge obstruction attack

## Verdict

\[
\boxed{
\partial_\kappa\!\left(\frac{\beta_{\loc}^\Theta}{c}\right)=0
\text{ fails in a legitimate local coefficient model.}
}
\]

I do **not** see a viable aggregate cancellation theorem for

\[
\beta_{\loc}^\Theta
=
\beta_{\off}^\Theta+\beta_{\diag}^\Theta.
\]

The obstruction is already visible in the homogeneous good-patch model after the edge amplitude factor is applied. The decisive point is that the physical edge carrier has the usual cubic amplitude factor

\[
a_1a_2(a_1+a_2),
\]

so along the merger edge

\[
a_2=-a_1+\kappa
\]

one gets

\[
a_1a_2(a_1+a_2)
=
a_1(-a_1+\kappa)\kappa
=
-a_1^2\kappa+a_1\kappa^2.
\]

Therefore

\[
\left.\partial_\kappa
\bigl(a_1a_2(a_1+a_2)\bigr)
\right|_{\kappa=0}
=
-a_1^2.
\]

Thus any nonzero physical edge coefficient \(B_{\loc}\) gives

\[
\left.
\partial_\kappa
\left(
\frac{\beta_{\loc}^\Theta}{c}
\right)
\right|_{\kappa=0}
=
-\frac{a_1^2}{c_0}B_{\loc}.
\]

Since \(c_0\neq0\) on a good patch, the only way the derivative vanishes is if

\[
B_{\loc}=0.
\]

The homogeneous good-patch model gives \(B_{\loc}\neq0\).

---

# 1. What must cancel

Write the physical edge term as

\[
\beta_{\loc}^\Theta(\kappa)
=
a_1a_2(a_1+a_2)\,
B_{\loc}^\Theta
+
O(\kappa^2\text{ from higher carriers}),
\]

with

\[
B_{\loc}^\Theta
=
B_{\off}^\Theta+B_{\diag}^\Theta
+
B_{\companions}^\Theta.
\]

Here \(B_{\companions}^\Theta\) denotes transpose companions, right-side companions, and any other labelled source insertions.

The edge condition is

\[
\left.
\partial_\kappa
\left(
\frac{\beta_{\loc}^\Theta}{c}
\right)
\right|_{\kappa=0}
=
0.
\]

Because

\[
\beta_{\loc}^\Theta(0)=0,
\]

the derivative of \(1/c\) does **not** contribute at \(\kappa=0\). Hence

\[
\left.
\partial_\kappa
\left(
\frac{\beta_{\loc}^\Theta}{c}
\right)
\right|_{\kappa=0}
=
\frac{1}{c_0}
\left.
\partial_\kappa
\beta_{\loc}^\Theta
\right|_{\kappa=0}
=
-\frac{a_1^2}{c_0}
B_{\loc}^\Theta.
\]

So the target identity is equivalent to the aggregate identity

\[
\boxed{
B_{\loc}^\Theta=0.
}
\]

That is a coefficient identity, not a coordinate issue.

---

# 2. Off-diagonal contribution is already nonzero

The known constrained-source \(\Theta_\pm\) second variation contains the lower-left term

\[
\frac{\Theta_{-,0}}{s^2}\eta_i\eta_j
+
\frac{S_0}{s}
(\chi_{-,i}\eta_j+\chi_{-,j}\eta_i).
\]

The first term is the old obstruction integrand

\[
\frac{\Theta_{-,0}}{s^2}\eta_i\eta_j
=
\left(
\frac{\sin\Delta_0}{s^2}
+
\frac{q_-\cos\Delta_0}{s}
\right)\eta_i\eta_j.
\]

In the homogeneous scalar model

\[
r_i=r_j=u^2,
\qquad
q_0''=0,
\]

the \(\chi\eta\)-channel does not cancel it, and the septic coefficient is

\[
[z^7]F_{21}(z/Q^2)
=
\frac{q_0^3}{216Q^{14}}
\neq0.
\]

This is already after the relevant constrained-source check; the attached audit records that \(\mathcal R_7^\Theta\neq0\) under actual constrained variables. 

Using the earlier top-row \(S\)-coefficient normalization,

\[
f_{\eta\eta}^{\top}
=
-\frac{u}{24\pi q^3}\nu_{21,7},
\]

and

\[
\nu_{21,7}
=
-\frac{q_0^3}{216Q^{14}},
\]

we get the homogeneous off-diagonal \(S\)-coefficient

\[
\boxed{
B_{\off}^{\Theta,\hom}
=
\frac{u q_0^3}
{5184\pi q^3Q^{14}}.
}
\]

On a good patch,

\[
u\neq0,\qquad q\neq0,\qquad q_0\neq0,\qquad Q\neq0,
\]

so

\[
\boxed{
B_{\off}^{\Theta,\hom}\neq0.
}
\]

The prior handoff also records that this top-row coefficient survives endpoint symmetry, source-grade parity, affine removal, and frozen Bottleneck-C bookkeeping, so it cannot be dismissed as a raw unsymmetrized lower-left artifact. 

---

# 3. The diagonal term is not a forced cancellation partner

The diagonal shifted row contributes

\[
B_{\diag}^{\Theta,\hom}
=
-\frac{\lambda}{2\pi Q^{12}}
\left(
\frac{q_0\alpha}{72}
+
\frac{q_0^3\beta}{864}
\right),
\]

with

\[
\lambda=
\frac{\alpha\beta}{4\pi Q(p_1+p_2)}.
\]

This is also \(S\)-visible and nonzero in the active constant-baseline model. 

But it cannot supply a structural cancellation theorem for the off-diagonal term, for three reasons.

First, it has a different algebraic dependence. The off-diagonal coefficient is proportional to

\[
\frac{u q_0^3}{q^3Q^{14}},
\]

whereas the diagonal shifted coefficient is proportional to

\[
\frac{\alpha\beta}{Q^{13}(p_1+p_2)}
\left(
q_0\alpha+q_0^3\beta
\right).
\]

These are independent coefficient directions in the local model.

Second, the diagonal coefficient can be made arbitrarily small while keeping the off-diagonal coefficient nonzero. For example, take

\[
\alpha=\varepsilon\alpha_0,
\qquad
\beta=\varepsilon\beta_0,
\qquad
p_1+p_2\neq0,
\]

with \(\varepsilon\neq0\) but arbitrarily small. Then

\[
\lambda=O(\varepsilon^2),
\]

and

\[
B_{\diag}^{\Theta,\hom}
=
O(\varepsilon^3)+O(\varepsilon^3 q_0^3),
\]

while

\[
B_{\off}^{\Theta,\hom}
=
\frac{u q_0^3}{5184\pi q^3Q^{14}}
\]

is \(O(1)\). Hence

\[
B_{\loc}^{\Theta,\hom}
=
B_{\off}^{\Theta,\hom}
+
O(\varepsilon^3)
\neq0
\]

for all sufficiently small nonzero \(\varepsilon\).

Third, if a claimed aggregate cancellation theorem were true, it would have to hold as an identity in these local coefficient variables. The small-\(\varepsilon\) family disproves that identity.

---

# 4. Transpose and left/right companions do not rescue the identity

A possible escape would be:

\[
B_{\companions}^{\Theta}
=
-B_{\off}^{\Theta}-B_{\diag}^{\Theta}.
\]

But this cannot be a structural identity under the current hypotheses.

The off-diagonal \(\eta\eta\) top-row coefficient has already been reported as surviving endpoint symmetry and fixed-sector bookkeeping. That means the transpose companion does not cancel it in the \(S\)-projection; it either reinforces it or lands in the same \(S\)-visible channel. 

Right-side and odd-whitening companions are coefficient-dependent. They can be suppressed or made arbitrarily small in the same local model while keeping the left off-diagonal top-row coefficient nonzero. A universal cancellation cannot depend on a coefficient that can be independently varied away.

Thus the full aggregate coefficient has the form

\[
B_{\loc}^{\Theta,\hom}
=
\frac{u q_0^3}{5184\pi q^3Q^{14}}
+
O(\varepsilon)
\]

or, in the active diagonal scaling above,

\[
B_{\loc}^{\Theta,\hom}
=
\frac{u q_0^3}{5184\pi q^3Q^{14}}
+
O(\varepsilon^3).
\]

For sufficiently small nonzero \(\varepsilon\),

\[
\boxed{
B_{\loc}^{\Theta,\hom}\neq0.
}
\]

---

# 5. Counterexample computation

Take a good-patch local coefficient model with

\[
q_0\neq0,\quad q\neq0,\quad Q\neq0,\quad u\neq0,\quad c_0\neq0,
\]

and homogeneous source

\[
r_i=r_j=u^2,
\qquad
q_0''=0.
\]

Choose non-diagonal whitening coefficients

\[
\alpha=\varepsilon\alpha_0,
\qquad
\beta=\varepsilon\beta_0,
\]

with

\[
\varepsilon\neq0
\]

small, and keep \(p_1+p_2\neq0\). Then the diagonal shifted term is present but arbitrarily small:

\[
B_{\diag}^{\Theta,\hom}=O(\varepsilon^3).
\]

The off-diagonal term remains

\[
B_{\off}^{\Theta,\hom}
=
\frac{u q_0^3}{5184\pi q^3Q^{14}}
\neq0.
\]

Therefore

\[
B_{\loc}^{\Theta,\hom}
=
\frac{u q_0^3}{5184\pi q^3Q^{14}}
+
O(\varepsilon^3)
\neq0.
\]

Along the edge

\[
a_2=-a_1+\kappa,
\]

we get

\[
\beta_{\loc}^\Theta(\kappa)
=
a_1(-a_1+\kappa)\kappa
B_{\loc}^{\Theta,\hom}
+
O(\kappa^2\text{ from higher edge carriers}).
\]

Thus

\[
\left.
\partial_\kappa
\left(
\frac{\beta_{\loc}^\Theta}{c}
\right)
\right|_{\kappa=0}
=
-\frac{a_1^2}{c_0}
B_{\loc}^{\Theta,\hom}.
\]

Substituting the leading coefficient gives

\[
\boxed{
\left.
\partial_\kappa
\left(
\frac{\beta_{\loc}^\Theta}{c}
\right)
\right|_{\kappa=0}
=
-\frac{a_1^2u q_0^3}
{5184\pi c_0 q^3Q^{14}}
+
O(\varepsilon^3).
}
\]

For sufficiently small nonzero \(\varepsilon\),

\[
\boxed{
\left.
\partial_\kappa
\left(
\frac{\beta_{\loc}^\Theta}{c}
\right)
\right|_{\kappa=0}
\neq0.
}
\]

This is the requested counterexample/model.

---

# 6. Theorem-ready no-go

```latex
\begin{proposition}[Physical edge obstruction is not aggregate-horizontal]
There exists a good-patch homogeneous local coefficient model for which
\[
\left.
\partial_\kappa
\left(
\frac{\beta_{\loc}^\Theta}{c}
\right)
\right|_{\kappa=0}
\neq0.
\]
Consequently the identity
\[
\partial_\kappa(\beta_{\loc}^\Theta/c)=0
\]
does not follow from summing labelled sources, transpose companions,
left/right insertions, and the standard edge amplitude factor.
\end{proposition}

\begin{proof}
Use the homogeneous scalar source
\[
r_i=r_j=u^2,\qquad q_0''=0,
\]
on a good patch with
\[
u q_0 q Q c_0\neq0.
\]
The constrained-source off-diagonal curvature contributes the fixed-sector
\(S\)-coefficient
\[
B_{\off}^{\Theta,\hom}
=
\frac{u q_0^3}{5184\pi q^3Q^{14}},
\]
which is nonzero.  The diagonal shifted contribution has the form
\[
B_{\diag}^{\Theta,\hom}
=
-\frac{\lambda}{2\pi Q^{12}}
\left(
\frac{q_0\alpha}{72}
+
\frac{q_0^3\beta}{864}
\right),
\qquad
\lambda=
\frac{\alpha\beta}{4\pi Q(p_1+p_2)}.
\]
Set
\[
\alpha=\varepsilon\alpha_0,\qquad
\beta=\varepsilon\beta_0,
\]
with \(\varepsilon\neq0\) sufficiently small and \(p_1+p_2\neq0\).
Then
\[
B_{\diag}^{\Theta,\hom}=O(\varepsilon^3),
\]
while \(B_{\off}^{\Theta,\hom}\) remains nonzero. Hence
\[
B_{\loc}^{\Theta,\hom}
=
B_{\off}^{\Theta,\hom}+B_{\diag}^{\Theta,\hom}
+
B_{\companions}^{\Theta,\hom}
\neq0
\]
for sufficiently small \(\varepsilon\), after including all companions whose
coefficients are either already included in the fixed-sector symmetrized
off-diagonal coefficient or are \(O(\varepsilon)\)-controlled in this local
model.

The edge amplitude carrier is
\[
a_1a_2(a_1+a_2).
\]
Along
\[
a_2=-a_1+\kappa
\]
this equals
\[
-a_1^2\kappa+a_1\kappa^2,
\]
so
\[
\left.
\partial_\kappa
\bigl(a_1a_2(a_1+a_2)\bigr)
\right|_{\kappa=0}
=
-a_1^2.
\]
Since \(\beta_{\loc}^\Theta(0)=0\), differentiating \(1/c\) contributes
nothing at \(\kappa=0\). Therefore
\[
\left.
\partial_\kappa
\left(
\frac{\beta_{\loc}^\Theta}{c}
\right)
\right|_{\kappa=0}
=
-\frac{a_1^2}{c_0}
B_{\loc}^{\Theta,\hom}
\neq0.
\]
\end{proof}
```

---

# Final conclusion

\[
\boxed{
\text{No aggregate cancellation theorem for }\beta_{\loc}^\Theta
\text{ is supported by the current coefficient calculus.}
}
\]

The physical edge obstruction survives. In the homogeneous good-patch model,

\[
\boxed{
\left.
\partial_\kappa
\left(
\frac{\beta_{\loc}^\Theta}{c}
\right)
\right|_{\kappa=0}
=
-\frac{a_1^2u q_0^3}
{5184\pi c_0 q^3Q^{14}}
+
O(\varepsilon^3)
\neq0.
}
\]

So the local Bottleneck C branch fails unless a **new zeta-specific identity** is added that is not present in the current package, symmetry, amplitude, transpose, or whitening calculus.
