## Claim

The cleanest compact-interval summation/regularization theorem needed inside the paired source package is the following theorem-facing source identity, and it should be stated only at blueprint scope.

**Candidate theorem unit (paired compact-interval source identity).** Let `chi` be a primitive Dirichlet character, set
`Xi_chi(s)=Lambda(s,chi)Lambda(s,bar chi)`, and define the paired strip-edge quotient
`Phi_chi^pair(s)=Xi_chi(2s-1)/Xi_chi(2s)`.
Fix a compact interval `I` on which the boundary-phase normalization attached to
`Phi_chi^pair(1/2+it)` is regular. Then the needed theorem unit is a localized identity on `I`
which writes the normalized paired phase derivative in the form

`q_chi^pair(t)=B_chi^pair(t)+S_chi^pair(t),  t in I,`

where `S_chi^pair(t)` is the compact-interval regularized sum of the paired one-zero strip-edge kernel contributions, and `B_chi^pair(t)` is one unified background term absorbing the conductor/scaling, gamma-derivative, and trivial-zero or pole bookkeeping. Repeated zeros are counted by an explicit multiplicity convention inside that regularized compact-interval identity.

The theorem should assert only this localized summation/regularization identity and its bookkeeping shell. It must stop before any claim that `S_chi^pair(m)` is already the manuscript's exact local `S(m)` slot, and before any local whitening-interface theorem.

**Proved.** From the zeta blueprint, the safe formal content is the package shape: quotient/phase normalization first, then one bundled compact-interval localized source identity; inside that localized theorem, the one-zero strip-edge kernel shape and the idea of a compact-interval regularized sum are the parts that transfer formally upstairs.

**Conditional on a family-specific paired source theorem.** For the Dirichlet paired family one may aim for the exact identity above with the specific paired quotient `Phi_chi^pair`, one unified paired background `B_chi^pair`, and a paired multiplicity convention. This is the right theorem target, but only as a family-specific closure of the zeta template.

**Missing.** What is not yet formal from the notes is the actual proof of paired compact-interval convergence/regularization, theorem-ready identification of the unified paired background term, theorem-facing multiplicity closure, exact paired `S(m)`-slot realization, and any transport to a local whitening/positivity theorem.

Honest verdict: the needed theorem is a localized compact-interval source identity for the paired quotient, but at present this is only the clean theorem target, not a proved transfer from zeta.

## Status

proved

## Evidence

The paired source-package note states that the safe first theorem must be only a compact-interval source-package blueprint and must stop before exact slot realization and local whitening. The paired compact-theorem note sharpens that to a localized blueprint mirroring the zeta source patch while keeping unified background, convergence, and multiplicity as explicit missing burdens. The source-package transfer note is the decisive transfer statement: from zeta one gets only the quotient shell, the upstairs one-zero kernel shape, and the idea of a localized compact-interval source theorem; one does not get paired compact-interval convergence/regularization, unified paired background identification, or theorem-facing multiplicity bookkeeping for free. The universal-source-kernel note matches this split by calling the one-zero strip-edge kernel universal only in the scoped upstairs sense, while leaving background and convergence family-specific. The zeta proof-plan and zeta source-package notes fix the blueprint shape itself: after normalization, the theorem endpoint is one normalized compact-interval source identity, not a broader global declaration. Combining these notes, the clean paired summation/regularization theorem is exactly a localized identity `q_chi^pair=B_chi^pair+S_chi^pair` on a compact interval, with all later slot and whitening claims kept outside the theorem.

## Exact refs

- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_source_package.md:7-20`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/source_package_transfer.md:17-38`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/universal_source_kernel.md:8-35`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_compact_theorem.md:8-27`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/zeta_source_proof_plan.md:16-45`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/zeta_source_package.md:13-28`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/dirichlet_paired_source.md:29-55`

## Dependencies

This statement depends on fixing the paired quotient `Phi_chi^pair`, the associated boundary-phase normalization on the compact interval, the paired one-zero strip-edge kernel, and a family-specific proof that the localized sum can be regularized into one exact paired source scalar `S_chi^pair` with one unified background term `B_chi^pair`. Any use beyond the source package depends separately on exact source-to-slot identification and the later local analytic package.

## Strongest objection

Even this candidate may still read too strongly if taken as already justified mathematics rather than as the clean theorem target. The notes repeatedly say that paired convergence/regularization, unified background identification, and multiplicity closure are themselves family-specific open burdens, so writing `q_chi^pair=B_chi^pair+S_chi^pair` without a scope reminder can falsely suggest that the hard paired summation theorem has already been transferred from zeta.

## Needed for promotion

1. A family-specific proof that the paired compact-interval one-zero contributions admit an exact regularized sum on `I`.
2. A theorem-ready identification of one unified paired background term `B_chi^pair` covering conductor/scaling, gamma-derivative, and trivial-zero or pole terms.
3. An explicit multiplicity convention that is stable inside the compact-interval regularized identity.
4. A separate proof that the resulting scalar `S_chi^pair(m)` is exactly the manuscript's local `S(m)` coefficient, not just an upstairs source scalar.
5. If needed downstream, a separate local whitening-interface theorem or an explicit conditional hypothesis block.
