## Claim

The cleanest theorem-proof plan for the paired compact-interval convergence/regularization subproblem is to target only a compact-interval source-package theorem schema, not a closed paired source theorem and not the exact manuscript `S(m)` slot.

Safe theorem endpoint:

**Theorem target (safe endpoint).** Fix a primitive Dirichlet character `chi`, the paired quotient `Phi_chi^pair(s)=Xi_chi(2s-1)/Xi_chi(2s)`, and a compact interval `I` on which the boundary-phase normalization is regular. The safe endpoint is only a theorem blueprint asserting that one seeks a localized decomposition on `I`

`q_chi^pair(t)=B_chi^pair(t)+sum_I^reg K_{rho,chi}^pair(t),  t in I,`

where `sum_I^reg` is a compact-interval regularized zero contribution, `B_chi^pair` is one unified paired background term, and multiplicities are handled by explicit bookkeeping stated adjacent to the theorem. This endpoint must be presented as the first theorem-facing package to prove, not as already closed mathematics.

Ordered proof plan:

1. Fix the paired quotient/phase normalization and compact interval `I`, using only the formally transferred upstairs shell from the zeta blueprint.
2. Isolate the family-specific paired one-zero contribution on `I` in the universal strip-edge kernel form; this is only the local kernel bookkeeping layer, not yet the compact summed theorem.
3. Formulate the precise regularization object `sum_I^reg K_{rho,chi}^pair(t)` and state the convergence target on compact subintervals. This is the first genuinely paired theorem burden.
4. Prove that the paired zero contributions admit a compact-interval convergence/regularization scheme on `I` strong enough to define the localized source identity.
5. In the same normalization, identify the unified background term `B_chi^pair`, keeping conductor/scaling, gamma-derivative, and trivial-zero or pole terms in one theorem-facing package.
6. Attach explicit multiplicity bookkeeping immediately after the theorem statement or in its bookkeeping corollary, but keep it out of the theorem body if it is not yet fully closed.
7. Stop the theorem here. Do not claim exact realization in the manuscript's paired `S(m)` slot, and do not claim any local whitening or positivity interface beyond the upstairs one-zero kernel positivity already scoped in the notes.
8. Treat exact `S(m)`-slot realization as a later second-step theorem, with any local positivity/whitening hypotheses stated there rather than imported backward into the compact-interval package.

Proved / conditional / missing:

- **Proved:** the safe endpoint is only a compact-interval source-package blueprint; the zeta case transfers only the quotient shell, upstairs one-zero kernel shape, and the idea of a localized compact-interval theorem; the paired theorem must stop before exact `S(m)`-slot realization and before any whitening-interface claim.
- **Conditional on a family-specific paired quotient/source theorem:** the paired proof may be organized around the ordered plan above, with a compact-interval regularized source identity as the first theorem-facing milestone.
- **Missing:** the actual compact-interval convergence/regularization proof; theorem-ready unified `B_chi^pair` identification; theorem-facing multiplicity closure; exact paired `S(m)`-slot realization; and any downstream local whitening or remainder-dominance theorem.

Honest verdict: the right next theorem is not a closed paired source theorem but a narrowly scoped compact-interval regularization package. Even that package is still open; the present result is only the clean proof order and the safe theorem endpoint.

## Status

open

## Evidence

`paired_proof_plan.md` fixes the global order: step 1 is only a compact-interval paired source-package blueprint/schema, while exact paired local `S(m)`-slot matching is step 2 and must carry its own local positivity/whitening interface hypotheses. The same note explicitly leaves compact-source closure, unified background bookkeeping, convergence/regularization, and multiplicity treatment marked missing. `paired_source_package.md` sharpens step 1 internally: the first genuinely new paired burden is exactly compact-interval convergence/regularization, with unified `B_chi^pair` and multiplicity coupled to the same package, and with both background and multiplicity placed immediately after the theorem as explicit bookkeeping rather than hidden conventions. `source_package_transfer.md` shows why this ordering is forced: only the quotient shell, upstairs one-zero kernel shape, and the idea of a localized compact-interval theorem transfer formally from zeta, while exact paired `S(m)`-slot realization, unified paired background, convergence/regularization, and multiplicity do not. `universal_source_kernel.md` gives the scope boundary for the kernel input: conditional on a family-specific quotient/source theorem, the one-zero strip-edge kernel and its positivity are universal only upstairs, while convergence/regularization, background identification, and local slot realization remain family-specific.

Together these notes support one safe proof plan. First fix the formal shell and local kernel bookkeeping. Next close compact-interval regularization strongly enough to define a localized paired source identity. Then package the unified background and multiplicity conventions adjacent to that theorem. Only afterward move to exact `S(m)`-slot realization. Any wording stronger than this would collapse the proved/conditional/missing separation the notes insist on.

## Exact refs

- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_proof_plan.md:8-18`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_proof_plan.md:19-39`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_source_package.md:7-28`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/source_package_transfer.md:13-38`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/universal_source_kernel.md:8-15`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/universal_source_kernel.md:22-35`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/agents/20260424-095958-paired-regularization-routeA/report.md:3-36`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/agents/20260424-095958-paired-regularization-routeB/report.md:3-15`

## Dependencies

The plan depends on preserving the two-step theorem architecture already fixed in the note package: compact-interval source-package first, exact local `S(m)` slot second. It also depends on a family-specific paired quotient/source theorem strong enough to license the upstairs one-zero kernel bookkeeping, on a compact-interval regularization argument that defines the paired zero sum on `I`, and on a normalization in which the unified background and multiplicity bookkeeping can be stated without smuggling in the later local slot theorem.

## Strongest objection

Step 5 and step 6 may still look too integrated with the regularization theorem: the notes repeatedly say unified background and multiplicity remain open, and in the safest wording they may belong only as explicit bookkeeping adjacent to the theorem rather than inside its body. So even this plan can overstate closure if read as claiming that regularization alone will automatically determine `B_chi^pair` and multiplicity. The safe reading is narrower: regularization is the first irreducible burden, and background/multiplicity must be carried with it as attached bookkeeping, but all three remain open.

## Needed for promotion

1. A family-specific proof that the paired zero contributions admit a well-defined compact-interval regularization on `I` with the strength needed for an exact localized identity.
2. A theorem-ready formula or characterization for one unified `B_chi^pair` in the same normalization.
3. An explicit multiplicity convention compatible with the compact-interval regularized identity.
4. A clean theorem statement separating theorem body from adjacent bookkeeping without ambiguity about what is proved.
5. A later independent theorem proving exact realization of the manuscript's paired `S(m)` slot, with any whitening/positivity hypotheses stated there.
