**Claim**

The strongest safe split, from current scope alone, is:

- source theorem: a compact-interval paired source-package blueprint only;
- immediate post-theorem bookkeeping: unified paired background `B_chi^pair` and multiplicity bookkeeping, stated explicitly right after the theorem but not inside the theorem body;
- later slot theorem: exact paired `S(m)`-slot realization and any local whitening-interface claim.

Separated by proof state:

- proved: the compact-interval localized blueprint is the right first theorem-facing shape;
- conditional: whether the same package can already absorb unified compact-interval convergence/regularization strongly enough to be theorem-body material;
- missing: exact paired `S(m)`-slot realization, local whitening, and full closure of the convergence/background/multiplicity package as theorem-level statements.

**Status**

heuristic

**Evidence**

The three supplied notes point to the same safe boundary. `paired_compact_theorem.md` identifies the correct first shape as a compact-interval paired source-package blueprint, but explicitly says this remains only a localized blueprint and does not buy exact paired `S(m)` realization or closure of the background/convergence/multiplicity package. `paired_source_package.md` sharpens this further: the theorem body should stop before exact slot realization and before any whitening interface, while both unified `B_chi^pair` and multiplicity should be kept out of the theorem body and placed immediately after it as explicit bookkeeping tied to the same compact summation/regularization package. `priority.md` reinforces that the program-level target is still a primitive Dirichlet paired-scalar exact-source theorem, so overstating the current source theorem would blur blueprint progress with exact realization.

My pressure-test verdict is therefore conservative. The theorem itself can safely claim only the localized scaffold: a compact-interval paired source-package blueprint mirroring the zeta patch shape. The post-theorem text can safely announce what bookkeeping will need to accompany that scaffold, but should present unified background and multiplicity as attached burdens, not as already closed theorem hypotheses or conclusions. The exact `S(m)` slot theorem must remain a later step, because the supplied notes repeatedly treat slot realization as the first place where the paired family-specific burden becomes genuinely exact rather than blueprint-level.

**Exact refs**

- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_compact_theorem.md:8-27`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_source_package.md:7-49`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/priority.md:10-18`

**Dependencies**

- the compact-interval paired source-package blueprint from `paired_compact_theorem.md`;
- explicit separation between theorem body and attached bookkeeping from `paired_source_package.md`;
- the program-level priority distinction in `priority.md`, especially the gap between first non-zeta source realization and the still-unrealized exact theorem target.

**Strongest objection**

The weakest part of this split is the status of compact-interval convergence/regularization. `paired_source_package.md` says the first genuinely new paired burden is exactly compact-interval convergence/regularization, with unified background and multiplicity coupled to it. That creates a live ambiguity: if convergence/regularization is essential even for the localized source-package statement, then calling the source theorem itself a theorem may still be too strong unless its body is written so weakly that it is only a blueprint schema. In other words, the line between `theorem` and `theorem-facing blueprint` is not fully closed by the supplied notes.

**Needed for promotion**

- pin down whether compact-interval convergence/regularization is part of the theorem body, a named assumption, or deferred post-theorem bookkeeping;
- give a theorem statement whose hypotheses and conclusion avoid any implicit exact paired `S(m)` realization;
- state the post-theorem bookkeeping block explicitly, with `B_chi^pair` and multiplicity attached there rather than hidden as global convention;
- reserve a later slot theorem for exact paired `S(m)` realization and any whitening/local interface statement.
