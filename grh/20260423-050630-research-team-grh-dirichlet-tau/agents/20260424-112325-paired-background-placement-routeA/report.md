Claim

Unified paired background identification should not sit inside the paired theorem body at the present proof state. The strongest safe recommendation is to place it immediately after the theorem as explicit bookkeeping attached to the same compact summation/regularization package, while the theorem body itself stays at the compact-interval source-package blueprint level.

Status

proved

Evidence

The three supplied notes point in the same direction.

`source_package_transfer.md` identifies unified paired background identification as one of the items that does not transfer formally from the zeta blueprint, alongside paired convergence/regularization and multiplicity bookkeeping. That rules out treating background unification as already theorem-body-ready in the transferred paired statement.

`background_unification.md` says the safest fix is one canonical background symbol in the new source subsection with only scoped parenthetical aliases, and explicitly says it is not safe to claim the current draft already proves exact identity of the existing background notations. That supports bookkeeping-level placement rather than theorem-body assertion.

`paired_source_package.md` gives the placement decision most directly: multiplicity should be kept out of the paired theorem body but also not hidden as loose convention; the safest placement is immediately after the theorem as explicit bookkeeping tied to the same compact summation/regularization package. The same note also states that unified background and multiplicity are coupled to the first genuinely new paired burden, namely compact-interval convergence/regularization. Since unified background is coupled to that later burden, the safest theorem-facing order is: theorem body first, then immediate post-theorem bookkeeping for unified background identification and related conventions.

Separated by proof state:

Proved: the supplied notes support rejecting theorem-body placement and support immediate post-theorem bookkeeping placement.

Conditional on later work: a stronger theorem-body formulation could become safe once the paired source theorem is actually normalized and the background aliases are upgraded from scoped correspondence to exact identity, and once the compact-interval convergence/regularization burden is discharged.

Missing: a proof that the paired background terms are exactly unified in theorem-strength form, rather than merely organized under one canonical symbol with scoped aliases.

Exact refs

`/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/source_package_transfer.md:11-15,17-25,33-38`

`/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/background_unification.md:8-16,24-30`

`/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_source_package.md:16-28,35-42`

Dependencies

This recommendation depends on the accuracy of the three supplied synthesis notes and, substantively, on the current unresolved paired burdens they record: exact paired `S(m)`-slot realization, compact-interval convergence/regularization, and theorem-facing normalization of the background notation.

Strongest objection

The objection is that background identification is one of the five zeta blueprint pieces, so mirroring the zeta package order could suggest putting it into the theorem body for structural symmetry. But the supplied notes cut against that symmetry argument: for the paired setting, background identification is still only safe as scoped bookkeeping and is explicitly coupled to unresolved convergence/regularization work, so importing zeta-style placement would overstate what is currently proved.

Needed for promotion

To promote unified paired background identification into the theorem body, one would need: exact normalization of the paired source theorem's background notation; a proof-level identification rather than role-level aliasing of the active background terms; and closure of the coupled compact-interval convergence/regularization burden showing that the background statement is theorem-strength rather than explanatory bookkeeping.
