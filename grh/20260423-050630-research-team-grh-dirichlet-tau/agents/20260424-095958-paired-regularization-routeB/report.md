## Claim

The smallest genuinely family-specific burden inside the compact-interval source package is the paired compact-interval convergence / regularization step itself: the theorem-sized argument that upgrades the formally transferred upstairs one-zero kernel bookkeeping into a convergent paired source identity on a compact interval. This is earlier, and narrower, than exact paired `S(m)`-slot realization. Unified paired background identification and multiplicity bookkeeping remain part of the same compact-package closure, but the first irreducible family-specific pressure point is the regularization needed to justify the interval summation at all.

The strongest safe roadmap sentence is:

`For the paired family, the first genuinely new theorem burden inside the compact source package is a compact-interval convergence and regularization theorem that turns the formally universal upstairs one-zero kernel into a valid paired source identity; unified background and multiplicity still have to be closed there, while exact S(m)-slot realization remains a later separate step.`

Proved / conditional / missing:

- Proved: current notes consistently treat the zeta source package as only a blueprint for the paired family; the quotient shell and upstairs one-zero kernel shape are the parts that transfer most formally; the paired theorem must stop before exact slot realization and whitening claims.
- Conditional: if a family-specific paired quotient/source theorem exists, then the upstairs one-zero kernel bookkeeping and the idea of a localized compact-interval source theorem survive in the paired setting.
- Missing: an actual paired compact-interval convergence / regularization theorem, together with theorem-facing unified paired background identification and multiplicity bookkeeping.

Honest verdict: the paired regularization theorem is the first genuinely family-specific bottleneck inside the compact package, but it is not closed by the present source package. The safe claim is only that this is the right next theorem burden.

## Status

open

## Evidence

`source_package_transfer.md` gives the cleanest transfer boundary. It says the transfer is `formal only upstairs`, with the open burden still including paired `background/convergence/multiplicity work`. It also explicitly separates what transfers formally from what does not: the quotient shell, upstairs one-zero kernel shape, and the idea of a localized compact-interval theorem transfer formally, while paired compact-interval convergence / regularization, unified paired background identification, and paired multiplicity bookkeeping do not. That makes the regularization layer the first family-specific burden inside the compact package rather than any issue with the universal upstairs kernel itself.

`paired_source_package.md` sharpens the same point one step further. It says the compact paired source theorem is safe only as a `compact-interval source-package blueprint`, and that the first real theorem burden inside that blueprint is `paired compact-interval convergence / regularization`, together with unified `B_chi^pair` and multiplicity bookkeeping. It also says the theorem must stop before exact paired `S(m)`-slot realization and any local whitening-interface claim. So the safe pressure-test conclusion is that regularization is the earliest genuinely family-specific closure problem, while slot realization is later.

`paired_proof_plan.md` confirms the order. It says the first real bottlenecks inside step 1 are `compact-interval summation/regularization and unified paired background identification`, while exact slot match belongs to step 2 and remains separate. It also keeps `convergence / regularization`, `unified background bookkeeping`, and `multiplicity treatment` explicitly marked missing. This shows that the family-specific regularization theorem is not just one missing lemma among many; it is the first point where the paired argument stops being a formal transfer and starts needing new theorem-level closure.

`universal_source_kernel.md` supplies the scoped contrast: conditional on a family-specific quotient/source theorem, the single-zero strip-edge kernel and its positivity are universal upstairs, but `background identification`, `convergence / regularization`, and the local realization into the exact `S(m)` slot remain family-specific. That rules out the stronger but unsafe sentence that the paired theorem burden is already reduced to local slot realization alone.

## Exact refs

- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_source_package.md:7-24`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_source_package.md:26-28`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/source_package_transfer.md:11-15`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/source_package_transfer.md:19-38`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/priority.md:12-29`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_proof_plan.md:11-32`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/universal_source_kernel.md:8-15`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/universal_source_kernel.md:22-35`

## Dependencies

- The current note consensus that `new for the paired family` should mean `first theorem component no longer inherited formally from the zeta blueprint`, not `last downstream obstacle in the whole program`.
- A family-specific paired quotient/source theorem strong enough to license the upstairs one-zero kernel bookkeeping.
- A compact-interval summation framework that justifies paired convergence / regularization before any exact local slot claim is attempted.
- Unified paired background identification and multiplicity conventions compatible with later source extraction.

## Strongest objection

One could argue that unified paired background identification, not regularization, is the true first family-specific burden, because the notes often pair those two items and the final source identity is not theorem-ready without the background term fixed in one convention. That objection is real, but it does not displace the present claim: without convergence / regularization there is not yet a justified compact-interval paired identity to which any unified background formula can even be attached. So the safest minimal burden is the regularization step, with background and multiplicity explicitly still open in the same package.

## Needed for promotion

1. A theorem proving paired compact-interval convergence / regularization, not just blueprint language.
2. A theorem-facing unified `B_chi^pair` identification in the same normalization as the compact source identity.
3. A multiplicity statement compatible with the compact summation theorem and later extraction of the exact paired scalar.
4. Only after those are closed, a separate exact paired `S(m)`-slot realization theorem with any local positivity/whitening hypotheses stated explicitly.
