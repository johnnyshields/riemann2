# Claim

The strongest safe ordering sentence is:

`after the compact-interval paired source package is fixed, the next paired theorem burden should prove together exact local S_chi^pair(m)-slot identification and the source-normalized scalar match, namely that the same paired source scalar from the compact package is exactly the coefficient of the paired local value-channel derivative; only after that is it safe to treat the paired value channel as a one-scalar whitening perturbation.`

This is the safest internal priority choice from the current notes alone.

# Status

proved

# Evidence

Proved:

- The paired source-package note explicitly stops the first theorem at a `compact-interval source-package blueprint` and says `exact slot realization and local whitening belong later`, so the first bridge is the compact paired source package and not the slot theorem or whitening theorem.
- The paired slot-realization note makes the theorem-facing patch order explicit: `(1)` compact-interval paired source-package theorem, `(2)` exact paired local `S(m)`-slot identification. It also states the clean target as proving that `S_chi^pair(m)=q_chi^pair(m)-B_chi^pair(m)` is exactly the coefficient of the paired local value-channel derivative in `Delta_chi^pair = S_chi^pair A_val,chi^pair + R_chi^pair`.
- The paired normalization-compatibility note sharpens what must be bundled with that second step: the coefficient in `Delta_chi^pair = S_chi^pair A_{val,chi}^pair + R_chi^pair` should be exactly the same `S_chi^pair(m)` coming from the compact paired source package, `not a renormalized surrogate or an upstairs-only scalar`. So exact slot identification and source-normalized scalar compatibility are not safely separable.
- The paired value-channel note says the one-scalar perturbation picture is safe `only after the paired source slot and local whitening interface are fixed`, and the strongest safe phrasing there is still `conditional, source-normalized interface`. So whitening is downstream of the second-step source-to-slot theorem burden, not part of the first bridge.

Conditional on [local paired interface package]:

- Once the compact paired source package is proved, one can formulate the second step as a combined source-to-slot theorem provided the local freeze rules, paired corrected same-point and mixed blocks, microscopic holomorphy, and same-point positivity/nondegeneracy are fixed well enough for `A_{val,chi}^{pair}` to be meaningful.
- Under those local interface assumptions, the safest packaging is a single theorem burden that simultaneously identifies the exact local slot and rules out any extra scalar renormalization between the compact source scalar and the local coefficient.

Missing:

- A proved paired theorem establishing the compact paired source package itself with full bookkeeping.
- A proved paired theorem that the same source scalar is exactly the coefficient of the paired local value-channel derivative with no extra normalization factor.
- A proved local whitening-interface theorem making `A_{val,chi}^{pair}` canonical rather than merely `conditional, source-normalized`.
- Any downstream closure for remainder dominance, denominator/whitening control, odd/transverse realization, boundary estimates, or calibration uniformity.

# Exact refs

- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_source_package.md:7-20`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_slot_realization.md:8-22`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_slot_realization.md:26-46`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_normalization_compatibility.md:7-27`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_value_channel.md:7-19`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_value_channel.md:25-37`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/dirichlet_paired_source.md:22-27`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/dirichlet_paired_source.md:44-55`

# Dependencies

- The compact-interval paired source package with unified `B_chi^pair` and exact source-side `S_chi^pair`.
- A local paired interface package fixing the same-point and mixed blocks, freeze rules, microscopic holomorphy, and same-point positivity/nondegeneracy.
- A theorem identifying `S_chi^pair(m)` as the exact coefficient in `Delta_chi^pair = S_chi^pair A_{val,chi}^{pair} + R_chi^pair`.
- Only after those are fixed, the paired value-channel whitening theorem.

# Strongest objection

The notes still do not prove the first bridge itself, and they do not yet provide a canonical local whitening interface. So the proposed priority is a safe ordering judgment, not a closure result: it says the next things that should move together are exact slot identification and source-normalized scalar compatibility, but it does not prove that the local package needed to make that theorem precise is already in hand.

# Needed for promotion

- Prove the compact paired source package with its unified `B_chi^pair` bookkeeping.
- State and prove the second-step theorem so that exact local slot identification and unit-coefficient source-normalized compatibility are established in the same theorem burden.
- Fix the paired local freeze rules and admissibility hypotheses tightly enough that `A_{val,chi}^{pair}` is theorem-ready.
- Only then promote the one-scalar paired value-channel whitening formulation as more than a conditional interface.
