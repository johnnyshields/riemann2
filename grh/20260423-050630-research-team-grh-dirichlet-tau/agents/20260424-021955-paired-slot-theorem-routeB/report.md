# Claim

The cleanest exact paired local-slot theorem target is narrow and should be stated as a source-plus-slot theorem, not as a portability theorem.

Safe target:

1. for the paired quotient `\Phi_\chi^{\mathrm{pair}}(s)=\Xi_\chi(2s-1)/\Xi_\chi(2s)` with `\Xi_\chi(s)=\Lambda(s,\chi)\Lambda(s,\bar\chi)`, prove a compact-interval paired source package with a unified background term `B_\chi^{\mathrm{pair}}`;
2. define `S_\chi^{\mathrm{pair}}(m)=q_\chi^{\mathrm{pair}}(m)-B_\chi^{\mathrm{pair}}(m)`;
3. prove exact local-slot identification: this `S_\chi^{\mathrm{pair}}(m)` is exactly the coefficient of the paired local value-channel derivative in a decomposition of the manuscript form
   `\Delta_\chi^{\mathrm{pair}}(m,\sigma)=S_\chi^{\mathrm{pair}}(m)A_{\mathrm{val},\chi}^{\mathrm{pair}}(m,\sigma)+R_\chi^{\mathrm{pair}}(m,\sigma)`.

This is where the exact paired slot theorem should stop. The downstream remainder package begins only when one asks for a calibrated estimate of the form `\Psi(R_\chi^{\mathrm{pair}})=o((x/B)S_\chi^{\mathrm{pair}})`. The odd/transverse package begins only when one applies the transverse functional to kill the value channel and then needs the realized odd scalar, denominator/whitening control, and boundary bounds.

Clean roadmap sentence:

`The paired local-slot theorem should prove exact identification of the paired background-subtracted scalar as the coefficient of the paired value-channel derivative, and should stop before any smallness claim on the remainder or any corrected odd/transverse realization claim.`

# Status

open

# Evidence

Proved from the supplied notes and the manuscript architecture:

- The notes already isolate the paired theorem burden as two linked parts: upstairs paired source realization and downstairs exact local-slot identification, with the theorem-facing order sharpened further to `compact-interval paired source-package theorem` followed by `exact paired local S(m)-slot identification`.
- The notes also explicitly say what remains after source-plus-slot closure: remainder dominance, corrected odd/transverse realization, denominator/whitening control, boundary estimates, and calibration uniformity issues. So those are outside the exact paired slot theorem.
- In the manuscript, the exact slot is not a vague positive scalar; it is the coefficient of the value-channel derivative in the decomposition `\Delta_\zeta=S(m)A_{\val}+R_\zeta`. This is the exact interface any paired theorem has to match.
- In the manuscript, remainder dominance is downstream of that split. The calibrated conclusion only starts once one already has `\Delta=S A_{\val}+R` and then asks for `\Psi(R)=o((x/B)S)`.
- In the manuscript, the odd/transverse stage is also downstream of the split: `\Phi_K(A_{\val})=0` annihilates the leading value channel, and then the odd scalar is built from what remains. That makes the odd package a separate target, not part of exact slot identification.

Conditional on a paired family analogue of the manuscript's local value-channel algebra:

- The smallest honest paired theorem is not yet a theorem about domination or contradiction. It is only a theorem that the paired background-subtracted scalar is the exact coefficient of the paired local value-channel derivative.
- If that exact coefficient theorem is closed, then the next bottleneck for the calibrated value-channel subchain is remainder dominance. But this is only a statement about that subchain, not about the full paired program.
- If one additionally has a realized odd transverse scalar with boundary control, then the universal odd-germ/projector algebra can start. That is a separate family theorem after, not inside, source-plus-slot realization.

Missing:

- An exact compact-interval paired source theorem with unified paired background bookkeeping `B_\chi^{\mathrm{pair}}`, including conductor/scaling, gamma-derivative, and any trivial-zero or pole corrections.
- A proof that the resulting paired background-subtracted scalar is exactly the coefficient occupying the manuscript's local value-channel slot, rather than merely a nearby positive strip-edge kernel.
- A precise paired definition of the local deformation `\Delta_\chi^{\mathrm{pair}}` and of `A_{\mathrm{val},\chi}^{\mathrm{pair}}` that makes the coefficient identity exact.
- Any calibrated remainder estimate of the form `\Psi(R_\chi^{\mathrm{pair}})=o((x/B)S_\chi^{\mathrm{pair}})`.
- Any corrected odd/transverse realization theorem, including denominator comparability, corrected whitening, and microscopic boundary control.

Honest verdict: the cleanest target is an exact paired source-plus-slot theorem whose endpoint is the identity `\Delta_\chi^{\mathrm{pair}}=S_\chi^{\mathrm{pair}}A_{\mathrm{val},\chi}^{\mathrm{pair}}+R_\chi^{\mathrm{pair}}`. Exact slot identification stops there. The moment one asks that `R_\chi^{\mathrm{pair}}` be small in calibration or that `\Phi_K(R_\chi^{\mathrm{pair}})` satisfy odd holomorphic boundary control, one has already moved into downstream remainder or odd-package theorems.

# Exact refs

- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/birds-eye-view-for-rh-agent.md:102-108`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/birds-eye-view-for-rh-agent.md:164-176`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/dirichlet_paired_source.md:20-27`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/dirichlet_paired_source.md:40-56`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/dirichlet_paired_source.md:66-87`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_slot_realization.md:11-20`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/paired_slot_realization.md:24-46`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/remainder_dominance.md:8-18`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/remainder_dominance.md:20-38`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/odd_package_transport.md:8-26`
- `/mnt/c/workspace/riemann2/grh/20260423-050630-research-team-grh-dirichlet-tau/notes/odd_package_transport.md:28-49`
- `/mnt/c/workspace/riemann2/paper/proof_of_rh.tex:739-779`
- `/mnt/c/workspace/riemann2/paper/proof_of_rh.tex:1540-1565`
- `/mnt/c/workspace/riemann2/paper/proof_of_rh.tex:1590-1620`
- `/mnt/c/workspace/riemann2/paper/proof_of_rh.tex:2060-2086`
- `/mnt/c/workspace/riemann2/paper/proof_of_rh.tex:2214-2231`
- `/mnt/c/workspace/riemann2/paper/proof_of_rh.tex:2236-2290`

# Dependencies

- The supplied notes correctly identify the paired theorem burden as `source realization + exact slot identification`, not merely `paired positivity`.
- A paired-family analogue of the manuscript's local value-channel interface, including a well-defined paired local deformation `\Delta_\chi^{\mathrm{pair}}` and derivative `A_{\mathrm{val},\chi}^{\mathrm{pair}}`.
- A unified paired background term `B_\chi^{\mathrm{pair}}` on compact intervals with explicit bookkeeping for conductor, gamma, and trivial-zero/pole terms.
- The manuscript reading that remainder dominance and the odd/transverse package use the `\Delta=S A_{\val}+R` split but are not part of proving that split.

# Strongest objection

The sharpest risk is that the boundary between `exact slot identification` and `downstream local analysis` may be less clean in the paired family than in the zeta manuscript. In the manuscript the coefficient identity lives inside a corrected whitened local object, so a paired analogue may still need some nontrivial local setup before `\Delta_\chi^{\mathrm{pair}}=S_\chi^{\mathrm{pair}}A_{\mathrm{val},\chi}^{\mathrm{pair}}+R_\chi^{\mathrm{pair}}` is even a well-posed exact statement. Current notes support keeping denominator/whitening and odd-boundary estimates outside the paired slot theorem, but they do not yet prove that no fragment of that analytic setup must be imported into the theorem hypotheses.

# Needed for promotion

- State the paired theorem explicitly in manuscript-interface form: define `\Phi_\chi^{\mathrm{pair}}`, `B_\chi^{\mathrm{pair}}`, `S_\chi^{\mathrm{pair}}`, `\Delta_\chi^{\mathrm{pair}}`, and `A_{\mathrm{val},\chi}^{\mathrm{pair}}`, then assert the exact coefficient identity.
- Make the stopping point explicit in the theorem statement or surrounding prose: no smallness claim on `R_\chi^{\mathrm{pair}}`, no remainder dominance, no odd/transverse realization, no boundary estimate.
- Verify adversarially that the paired background bookkeeping absorbs every conductor, gamma, multiplicity, trivial-zero, and normalization term, so the coefficient is genuinely the manuscript slot and not a renormalized surrogate.
- Separate the next two theorems after promotion: `(i)` calibrated remainder dominance for the value-channel subchain, `(ii)` corrected odd/transverse realization-and-boundary control.
