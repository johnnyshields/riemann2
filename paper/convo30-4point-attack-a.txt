Pasted markdown(60).md
File
convo19-nonmixed-4point-deriv-geo3-positive-kernel.txt
Document
proof_of_rh(306).tex
File
We are trying to prove the RH

I’m reading the draft and the companion notes to reconstruct where the proof actually stands and what the live bottlenecks are, rather than guessing from the chat history alone. Then I’ll tell you plainly what looks closed, what does not, and what the best next attack is.

We are trying to prove the RH, a chat has made claim of a closure and I'd like to check it very carefully before adding it to the paper draft

I’m isolating the alleged closure from the notes and cross-checking it against the actual draft hypotheses, so I can tell you whether it is really proved, only conditionally proved, or still missing a bridge.

please see attached paper, chat transcript, and the specific findings


specifically verify if we can make this revision: Sure! Here’s a full restatement of the **mathematical picture** we've developed so far, summarizing everything that isn't yet in the draft, along with key structural findings, positive results, and negative findings.

---

## **Full Mathematical Picture**

### 1. **Quotient-Cusp Lane Structure**

* The **quotient-cusp residual lane** has been reduced to a scalar function problem for ( M_1(u) ), which is defined over the quotient branch. The goal was to understand whether there are any **bad pairs** (i.e., max/min inversions), and we succeeded in proving that these inversions cannot exist due to the **monotonicity** of the gap function ( B(u) = U(u) - L(u) ).

* The gap function ( B(u) ) measures the difference between the **upper envelope** ( U(u) ) (running maximum of maxima) and the **lower envelope** ( L(u) ) (running minimum of minima). By proving that ( B(u) ) is **monotonic and non-negative**, we established that **no bad pairs** (max/min inversions) can exist along the quotient-cusp branch.

### 2. **Monotonicity and Non-Negativity of the Gap Function**

* The **gap function ( B(u) = U(u) - L(u) )** is **monotone non-decreasing**, meaning that once the gap reaches a non-negative value, it cannot decrease. This proves that the envelopes ( U(u) ) and ( L(u) ) cannot overlap at any point along the branch.

* **Zero-crossings of ( B(u) )** are impossible. If ( B(u) = 0 ) at any point, it is not possible for ( B(u) ) to cross zero again, because the envelopes would need to remain exactly equal over an interval, which is impossible unless the function ( M_1(u) ) is constant — but we know ( M_1(u) ) has local extrema, making this scenario impossible.

### 3. **Non-Existence of Bad Pairs**

* Since ( B(u) ) is non-negative and monotonic, we concluded that the **quotient-cusp residual lane is closed**. There are no bad pairs (max/min inversions), and the quotient-cusp lane is **free from overlapping intervals**.

* The final conclusion is that the residual quotient-cusp problem has no nontrivial solutions, as the gap function ( B(u) ) effectively closes the lane by preventing any max/min inversions.

### 4. **Global No-Reentry Condition**

* The gap function ( B(u) ) is closely tied to the **global no-reentry condition**. If the gap function remains non-negative for all ( u ), this ensures that there are no **overlapping intervals** where max/min inversions could occur. We formally proved this with the following theorem:

  [
  \boxed{
  \text{The quotient-cusp residual lane is closed if and only if } B(u) \geq 0 \text{ for all } u.
  }
  ]

  This result directly implies that the quotient-cusp lane cannot contain any bad pairs, and thus it is closed.

### 5. **Key Structural Findings**

* **Gap-Function Monotonicity**: The gap function ( B(u) = U(u) - L(u) ) is monotone non-decreasing, which guarantees that the residual quotient-cusp lane cannot contain bad pairs, as it effectively prevents any overlap between the max and min envelopes.

* **Critical-Value Ordering**: The critical values of ( M_1(u) ), which correspond to the maxima and minima along the quotient-cusp branch, must be strictly ordered to prevent overlap. We showed that the gap function's monotonicity and non-negativity imply a strict ordering of the critical values.

* **No Overlap Between Max/Min Intervals**: By proving that ( B(u) ) is non-negative, we ensured that there is no overlap between the intervals defined by local maxima and minima. This closes the possibility of non-trivial bad pairs.

* **Scalar Transport Object**: The residual quotient-cusp lane has been reduced to a **transport scalar problem** involving ( \widehat{\mathcal D} ) and ( \widehat Q ), which simplifies the analysis further. The problem is now reduced to ensuring that the critical values of ( \widehat Q ) follow a strict ordering.

### 6. **Negative Findings (What Doesn’t Close the Lane)**

* **Pointwise New Generator Lane**: We initially explored the idea of finding a pointwise new generator for the problem, but this avenue has been ruled out. The pointwise generator does not provide a new solution outside of the **( r=q^{(7)} ) tower**. Thus, we abandoned this approach early.

* **Local Cusp Recurrence**: Although we investigated recurrence mechanisms near cusps, we found that these local recursions are not responsible for new solutions to the quotient-cusp problem. The **finite-order inertness** of these recursions helped narrow down the problem and provided us with cleaner **global structural results**.

* **No Non-Degenerate Critical Points at ( 0 )**: The update also indicates that **degenerate critical points** (where ( M_1(u) = 0 )) cannot provide additional branches beyond what we've already found. Thus, we did not need to focus on non-degenerate critical points or potential secondary solutions at ( M_1(u) = 0 ).

### 7. **Final Attack Directions (Next Steps)**

We have effectively closed the quotient-cusp lane, but the next logical step is to:

1. **Link the gap-function results to the broader framework**: We need to confirm that the **no-reentry condition** applies globally, using the transport scalar ( \widehat{\mathcal D} ) and ( \widehat Q ).

2. **Check for additional residual branches**: While we’ve closed the quotient-cusp lane, there may be other residual lanes to examine in the larger geometric picture.

---

### **Paper Revision Plan:**

* **Introduce the gap function** ( B(u) = U(u) - L(u) ) and formally prove its **monotonicity** and **non-negativity**.
* **Formalize the no-reentry condition**: Prove that the quotient-cusp residual lane is closed if and only if ( B(u) \geq 0 ).
* **Clarify the reduced problem**: Emphasize that the problem now reduces to the **critical-value ordering** for the transport scalar ( \widehat Q ).
* **Summarize structural findings**: Include the fact that the quotient-cusp residual lane is closed, based on the gap-function analysis and the **non-intersecting envelopes**.
* **Highlight the negative findings** and explain how they were resolved (e.g., **pointwise generator**, **local recurrence**).

Here is the next honest push.

I think the best continuation on the strict-excess fixed-shear fork is to refine the sign-theorem target for
H(u):=Q^′′(u)(Q^′(u))2
H(u):=(Q
​′(u))2Q
​′′(u)​

into a strip comparison principle.

The last step gave the exact mean-value identity
Δ′(q)=F(ϕ2(q))−F(ϕ0(q))
Δ′(q)=F(ϕ2​(q))−F(ϕ0​(q))

with
F(u)=1Q^′(u),
F(u)=Q
​′(u)1​,

and therefore
Δ′(q)=− H(ξ(q)) Δ(q)
Δ′(q)=−H(ξ(q))Δ(q)

for some intermediate point
ξ(q)∈(ϕ0(q),ϕ2(q)).
ξ(q)∈(ϕ0​(q),ϕ2​(q)).

Since Δ(q)>0Δ(q)>0 on the common value interval, the sign of Δ′Δ′ is controlled by the sign of HH somewhere in the strip between the outer runs.

So the right next question is no longer “is HH globally signed?” but:
Can H change sign inside the outer-run transport strip in the strict three-run case?
Can H change sign inside the outer-run transport strip in the strict three-run case?​

That is sharper and more plausible to attack.
1. The transport strip

Define the transport strip
S:={ u:ϕ0(q)≤u≤ϕ2(q) for some q∈I },
S:={u:ϕ0​(q)≤u≤ϕ2​(q) for some q∈I},

where I=[q−,q+]I=[q−​,q+​] is the common value interval of the two outer runs.

The strict-excess case requires an interior zero of Δ′Δ′, hence by the mean-value identity it requires that HH either vanishes or changes sign somewhere in SS.

So the exact next theorem target is:
Strict three-run strip-sign theorem. B1>B0+B2⟹H has fixed sign on S.
Strict three-run strip-sign theorem. B1​>B0​+B2​⟹H has fixed sign on S.​

If that theorem lands, strict excess dies immediately, because then Δ′Δ′ keeps one sign on II.
2. Why this is better than a pointwise theorem on ΞΞ

Earlier I attacked via
Ξ(u)=Q^′′(u)(Q^′(u))3.
Ξ(u)=(Q
​′(u))3Q
​′′(u)​.

That is still natural, but HH is actually the more relevant scalar for the one-variable ODE, because
ϕi′(q)=F(ϕi(q)),F′(u)=−H(u).
ϕi′​(q)=F(ϕi​(q)),F′(u)=−H(u).

So HH measures the monotonicity/convexity of the inverse-branch vector field itself, which is exactly what governs whether two solutions ϕ0,ϕ2ϕ0​,ϕ2​ can approach, separate, or develop an interior critical point in their gap.

In other words, the strict-excess fork has now become a comparison theorem for two solutions of the same scalar ODE
ϕ′=F(ϕ),
ϕ′=F(ϕ),

and H=−F′H=−F′ is the correct coefficient.

That is a useful conceptual simplification.
3. A local comparison lemma that should be true

Suppose HH has fixed sign on SS. Then FF is monotone on SS. Since
ϕ0′(q)=F(ϕ0(q)),ϕ2′(q)=F(ϕ2(q)),
ϕ0′​(q)=F(ϕ0​(q)),ϕ2′​(q)=F(ϕ2​(q)),

and ϕ2(q)>ϕ0(q)ϕ2​(q)>ϕ0​(q), monotonicity of FF implies
ϕ2′(q)−ϕ0′(q)
ϕ2′​(q)−ϕ0′​(q)

has fixed sign on II. But that difference is exactly Δ′(q)Δ′(q). So:
H one-sign on S⟹Δ′ one-sign on I.
H one-sign on S⟹Δ′ one-sign on I.​

This is the clean comparison lemma I would now regard as established modulo the sign theorem for HH.

So the whole strict-excess lane has become:
strict excess can survive only if H changes sign in the outer-run strip.
strict excess can survive only if H changes sign in the outer-run strip.​

That is already a strong compression.
4. How the three-lobe defect should enter

The consolidated update says the only genuinely live three-run case is
Λ:=B1−(B0+B2)>0,
Λ:=B1​−(B0​+B2​)>0,

while
Λ=0
Λ=0

is sterile touching. So the natural conjecture is:
Λ>0⟹H keeps a strict sign on S,
Λ>0⟹H keeps a strict sign on S,​

and
Λ=0⟹H may vanish, corresponding only to touching.
Λ=0⟹H may vanish, corresponding only to touching.​

This is the best current way I know to connect the integrated lobe-amplitude model to the local inverse-branch ODE.

What makes this plausible is that ΛΛ is the unique live scalar defect left by the sign-run compression. If there were an interior sign change of HH in the strict case, that would be a second scalar instability in the same quotient-visible model, which would be surprising given how hard the model has already been compressed.

That is not a proof, but it is a good heuristic for why the sign theorem for HH is the right next target.
5. A weaker theorem that would still help a lot

Even if full one-sign control of HH is too strong, a weaker theorem would still materially tighten the fork:
The zero set of H intersects each same-value segment [ϕ0(q),ϕ2(q)] in at most one point.
The zero set of H intersects each same-value segment [ϕ0​(q),ϕ2​(q)] in at most one point.​

Then Δ′Δ′ could change sign at most once. Combined with endpoint sign information from ΛΛ, that might still be enough to exclude an interior zero in the strict case.

So there are really two nested goals now:

First, strongest:
H has fixed sign on S.
H has fixed sign on S.​

Second, fallback:
H has at most one crossing on each outer-run segment.
H has at most one crossing on each outer-run segment.​

Either would be useful.
6. A more geometric restatement

This can also be phrased in geometric language that may be easier to attack.

Since
F(u)=1Q^′(u),
F(u)=Q
​′(u)1​,

the condition that H=−F′H=−F′ has fixed sign means FF is monotone on the strip. So the strict-excess fork can be restated as:
Can the inverse-slope field F(u)=1/Q^′(u) fail monotonicity between the outer runs in the strict case?
Can the inverse-slope field F(u)=1/Q
​′(u) fail monotonicity between the outer runs in the strict case?​

If FF is monotone there, the two outer inverse branches can never develop a critical-point gap. So another clean theorem target is:
Outer-strip inverse-slope monotonicity in the strict three-run case.
Outer-strip inverse-slope monotonicity in the strict three-run case.​

That is exactly equivalent to the sign theorem for HH.
7. The sharpest current theorem target

So the next statement I would now actually try to prove is:
In the strict three-run quotient-visible model, u↦1Q^′(u) is monotone on the transport strip between the two outer runs.
In the strict three-run quotient-visible model, u↦Q
​′(u)1​ is monotone on the transport strip between the two outer runs.​

From that it follows immediately that
Δ′(q)=ϕ2′(q)−ϕ0′(q)
Δ′(q)=ϕ2′​(q)−ϕ0′​(q)

has fixed sign, so ΔΔ has no interior critical point, and strict excess is impossible.

That is, I think, the sharpest scalar attack currently available on the fixed-shear front.

continue your math attack

Here is the next honest push.

I think the cleanest way forward on the strict-excess fixed-shear fork is to stop asking for a global sign theorem on
H(u):=Q^′′(u)(Q^′(u))2
H(u):=(Q
​′(u))2Q
​′′(u)​

all at once, and instead derive a barrier principle from the middle run.

That is the most natural place where the three-run amplitude data should actually enter.

Recall the current one-variable reduction. On the common value interval II of the two outer runs, the inverse branches ϕ0,ϕ2ϕ0​,ϕ2​ satisfy
ϕi′(q)=F(ϕi(q)),F(u):=1Q^′(u),
ϕi′​(q)=F(ϕi​(q)),F(u):=Q
​′(u)1​,

and the gap
Δ(q):=ϕ2(q)−ϕ0(q)
Δ(q):=ϕ2​(q)−ϕ0​(q)

obeys
Δ′(q)=F(ϕ2(q))−F(ϕ0(q)).
Δ′(q)=F(ϕ2​(q))−F(ϕ0​(q)).

So strict excess is exactly the existence of an interior zero of Δ′Δ′, equivalently an interior same-value same-slope coincidence across the outer runs. The current strict-excess fork is therefore:
Can F(ϕ2(q))−F(ϕ0(q)) vanish in the interior?
Can F(ϕ2​(q))−F(ϕ0​(q)) vanish in the interior?​

Now, by the mean-value theorem, for each q∈Iq∈I there is ξ(q)∈(ϕ0(q),ϕ2(q))ξ(q)∈(ϕ0​(q),ϕ2​(q)) such that
Δ′(q)=F′(ξ(q)) Δ(q)=−H(ξ(q)) Δ(q).
Δ′(q)=F′(ξ(q))Δ(q)=−H(ξ(q))Δ(q).

Since Δ(q)>0Δ(q)>0, the sign of Δ′Δ′ is the sign of −H−H somewhere along the same-value segment between the outer runs. So the strict-excess obstruction is really:
Does the same-value segment between the outer runs cross a zero of H for some q∈I?
Does the same-value segment between the outer runs cross a zero of H for some q∈I?​

That is sharper than the earlier “sign theorem on the whole strip.” It says we only need to control HH on the family of same-value chords joining the two outer runs.

This suggests the next theorem target.
Same-value chord barrier theorem.
Same-value chord barrier theorem.​

For every common value q∈Iq∈I, the chord
[ϕ0(q),ϕ2(q)]
[ϕ0​(q),ϕ2​(q)]

meets the zero set of HH either never, or only in the equality-case touching regime B1=B0+B2B1​=B0​+B2​. In particular, in the strict case
B1>B0+B2,
B1​>B0​+B2​,

every same-value chord sees a fixed sign of HH, hence
Δ′(q)≠0
Δ′(q)=0

for all q∈Iq∈I.

If that theorem lands, the strict-excess fork is dead.

Why this looks more attackable than a global strip theorem is that the same-value chords are exactly the geometric objects already singled out by the quotient-visible model. The lobe amplitudes B0,B1,B2B0​,B1​,B2​ are measuring transport across the same-value structure, not arbitrary points in the strip. So asking about zeros of HH on those chords is much more aligned with the current reduction than asking for full monotonicity of FF on the entire strip.

There is also a useful scalar reformulation.

Define the chord-sampled sign function
σ(q):=sgn⁡(H(ξ(q))),
σ(q):=sgn(H(ξ(q))),

where ξ(q)ξ(q) is any mean-value point with
Δ′(q)=−H(ξ(q)) Δ(q).
Δ′(q)=−H(ξ(q))Δ(q).

If HH has constant sign on each same-value chord, then σ(q)σ(q) is well-defined and
sgn⁡Δ′(q)=−σ(q).
sgnΔ′(q)=−σ(q).

So the problem becomes:
Can σ(q) change sign in the strict three-run case?
Can σ(q) change sign in the strict three-run case?​

The natural conjecture is no: in the strict case, σσ should be constant, while in the equality case it may hit zero, corresponding only to sterile touching.

This leads to an even more concrete intermediate target:
Same-value chord sign theorem: B1>B0+B2⟹sgn⁡H is constant on every same-value chord.
Same-value chord sign theorem: B1​>B0​+B2​⟹sgnH is constant on every same-value chord.​

That theorem would be enough.

There is one more angle that may help.

Because the middle run sits between the outer runs, each same-value chord crosses the whole “forbidden” middle lobe. If the lobe-amplitude defect
Λ:=B1−(B0+B2)
Λ:=B1​−(B0​+B2​)

is strictly positive, then the middle run dominates the two outers in the exact way that made three-run overlap live. So it is plausible that Λ>0Λ>0 should act as a barrier preventing the inverse-slope field F=1/Q^′F=1/Q
​′ from having equal values at the two outer endpoints of a same-value chord. In other words, strict excess should not just forbid a zero of Δ′Δ′ by accident; it should forbid equality
F(ϕ0(q))=F(ϕ2(q))
F(ϕ0​(q))=F(ϕ2​(q))

because the middle run forces one outer endpoint of the chord to lie strictly on one side of the inverse-slope profile. That suggests another equivalent target:
Middle-run barrier theorem for F=1/Q^′.
Middle-run barrier theorem for F=1/Q
​′.​

In the strict three-run case, the middle run imposes a strict ordering
F(ϕ0(q))≠F(ϕ2(q))
F(ϕ0​(q))=F(ϕ2​(q))

with fixed sign on the whole common value interval.

That is probably the most conceptual formulation of what we need.

So the attack stack on this front is now:

First:
strict excess ⟺∃q∈I: F(ϕ2(q))=F(ϕ0(q)).
strict excess ⟺∃q∈I: F(ϕ2​(q))=F(ϕ0​(q)).​

Second, try to prove:
B1>B0+B2⟹F(ϕ2(q))−F(ϕ0(q)) keeps a strict sign on I.
B1​>B0​+B2​⟹F(ϕ2​(q))−F(ϕ0​(q)) keeps a strict sign on I.​

Or equivalently:
B1>B0+B2⟹Δ′(q) keeps a strict sign on I.
B1​>B0​+B2​⟹Δ′(q) keeps a strict sign on I.​

That is the sharpest next theorem I see on the fixed-shear front.

If you want the most concrete next sub-attack, it is this: derive the endpoint values of
F(ϕ2(q))−F(ϕ0(q))
F(ϕ2​(q))−F(ϕ0​(q))

at q=q−q=q−​ and q=q+q=q+​ in terms of the lobe amplitudes B0,B1,B2B0​,B1​,B2​, and see whether the strict defect
Λ=B1−(B0+B2)
Λ=B1​−(B0​+B2​)

already forces those endpoint signs to agree. If it does, then any one-crossing or same-value-chord sign theorem finishes the lane.

please give a complete recap of your whole attack along with new findings since most recent paper update. include theorems, no-gos, structural findings, negative findings, goodies, etc.

Here is the full recap of my attack since the most recent paper draft, including what now looks genuinely established, what is only conditional, what has been killed, and what I think the live endgame still is.

At the highest level, the main change is that the problem has become much cleaner. The geometry has been compressed hard enough that the remaining burden is no longer “find one more local sign argument.” The live space now splits into three narrow fronts: a nonmixed/global affine front where the remaining burden is RH-specific exclusion of zeros of monotone remote endpoint-gap functions; a mixed exact fixed-shear front where the remaining burden is a quotient-visible transport problem downstairs in u=t2u=t2; and a package-level / honest order-7 front where the real target is not raw septic equality but a provenance-sensitive statement such as same reduced image germ or collision-functoriality. The generator audit is part of that cleanup: no second explicit pointwise highest-new generator has appeared beyond r=q(7)r=q(7), so if something survives, it should be transport/global/package-level rather than “one more local field.”
1. The exact fixed-shear corner has been compressed much further than before

The most important positive gain is on the residual exact fixed-shear corner {S=0}∩{Kv=0}{S=0}∩{Kv​=0}. The overlap bad set is now sharply split: (Kv=0,S≠0)(Kv​=0,S=0) is already mixed low-order, (S=0,Kv≠0)(S=0,Kv​=0) transfers to nonmixed 4-point geometry, and only (S=0)∩(Kv=0)(S=0)∩(Kv​=0) remains as the genuinely residual derivative-geometric corner. That corner is involutive / quotient-diagonal: repeated pairs are exactly t↔−tt↔−t, so there is no hidden local partner choice. Downstairs, the natural variable is u=t2u=t2, and the regularized transport state is quotient-visible. On the quartic–sextic rung the quotient-visible transport state has local rank one and there is no hidden finite-order reset. This is a real theorem-level compression of the mixed residual corner.

More concretely, on the involutive fixed-shear branch the regularized quotient
Q(t)=Ju5(t)Jλ(t)
Q(t)=Jλ​(t)Ju5​​(t)​

is even, so
Q(t)=Q^(t2).
Q(t)=Q
​(t2).

Every parity-normalized finite transport jet descends to the quotient variable. On the first surviving rung (m,n)=(4,6)(m,n)=(4,6), equivalently (r,e)=(2,0)(r,e)=(2,0), one has Q^′(0)=D^(0)≠0Q
​′(0)=D
(0)=0, so Q^Q
​ is a local analytic coordinate in uu. That implies the whole finite quotient-visible transport state is locally a function of the single scalar QQ, hence has rank one. It also implies there is no third local reset in any finite descended transport state: the only local duplication is the involution t↔−tt↔−t.

This already yields a strong negative result: if the exact fixed-shear corner is still genuinely live, then the obstruction cannot come from a second local transport variable, a hidden finite-order reset, or any two-pair model built only from the separate descended one-atom states with the natural merger law. In other words, the residual corner is no longer blocked by generic finite-state local geometry.
2. The local chart was corrected, and that changed the attack materially

A second major gain was the correction of the local chart near a close bad 2-point core. The right local regime is not naive diagonal merger. It is the collision-cancellation chart
m=h1+h22,δ=h2−h1,λ=−a2a1≈1,
m=2h1​+h2​​,δ=h2​−h1​,λ=−a1​a2​​≈1,

or equivalently the swap-compatible coordinate
ω=λ−1λ+1.
ω=λ+1λ−1​.

In that chart, swap acts linearly by
(m,ω,δ)↦(m,−ω,−δ).
(m,ω,δ)↦(m,−ω,−δ).

This was not cosmetic. It changes the entire local blow-up structure: the relevant limiting regime is cancellation, not ordinary diagonal coincidence.

Once that chart is used, a strong structural normal form appears. Any analytic swap-even local corrected defect E(m,ω,δ)E(m,ω,δ) with collision vanishing has no linear blown-up term. Its Taylor expansion starts at even total degree and in fact factors as
E(m,ω,δ)=δ2 H(m,κ,δ2),κ=2ωδ.
E(m,ω,δ)=δ2H(m,κ,δ2),κ=δ2ω​.

So the whole ordinary analytic swap-invariant local scalar layer is already dead: the first surviving transverse layer is quadratic, projective, and one-parameter. Along the involutive fixed-shear branch even that quadratic layer is quotient-visible, and on the quartic–sextic rung its coefficient is expected to be a function of the single scalar QQ. So the live obstruction, if any, has already been pushed beyond the entire quadratic blow-up layer. This is one of the strongest structural gains from the later round of attack.

This also gave a cleaner conditional closure target. One no longer needs a full package-level state-locality theorem to close the exact fixed-shear corner. It would already be enough to prove that the actual corrected cubic/quintic defects are analytic in the collision chart, swap-even, and vanish on the collision locus. Under those weaker assumptions, the defects are automatically O((h1−h2)2)O((h1​−h2​)2), and the already-established good-patch defect-closeness mechanism excludes the branch.
3. The quotient-visible exact fixed-shear model now has a sharp global reduction

The updated fixed-shear side is no longer just “some quotient transport model.” It now has a specific global compression in terms of sign-runs and lobe amplitudes BjBj​. The live data are the run amplitudes, not the raw zero list. The exact distance-two overlap criterion is the three-lobe inequality
Bj+Bj+2>Bj+1.
Bj​+Bj+2​>Bj+1​.

Two consecutive distance-two separations are impossible. Therefore no-reentry on a quotient-visible branch forces at most three sign-runs, and in the three-run case the only surviving condition is
B1≥B0+B2,
B1​≥B0​+B2​,

with strict inequality giving the genuinely live case and equality giving only sterile touching. The quartic–sextic rung is explicitly inert for this lobe-amplitude obstruction because D^(0)≠0D
(0)=0 there. This is a real global reduction inside the quotient-visible exact fixed-shear model.

My attack then pushed that strict three-run case further. The natural inverse-branch object is
Δ(q)=ϕ2(q)−ϕ0(q),
Δ(q)=ϕ2​(q)−ϕ0​(q),

where ϕ0,ϕ2ϕ0​,ϕ2​ are the outer inverse branches over the common value interval. Strict excess is exactly the existence of an interior critical point of ΔΔ. That is already a major simplification: the live fixed-shear lane is now an exact one-variable inverse-branch-gap problem.

From there I derived a sequence of sharper reformulations.

First, if q∗q∗​ is an interior critical point of ΔΔ, then
ϕ0′(q∗)=ϕ2′(q∗),
ϕ0′​(q∗​)=ϕ2′​(q∗​),

hence the corresponding outer-run points u0,u2u0​,u2​ satisfy
Q^(u0)=Q^(u2),Q^′(u0)=Q^′(u2).
Q
​(u0​)=Q
​(u2​),Q
​′(u0​)=Q
​′(u2​).

So strict excess forces a same-value same-slope coincidence across the separated outer runs. That turns the strict-excess fork into a phase-curve self-intersection problem for
Ψ(u)=(Q^(u),Q^′(u)).
Ψ(u)=(Q
​(u),Q
​′(u)).

This was one of my main new reductions beyond the consolidated update. It is reasoning from the branch-gap formulas, not something already proved in the draft. Its premises come from the quotient-visible inverse-branch model in the summary.

Second, if we define
R(q):=Q^′(ϕ0(q))Q^′(ϕ2(q)),
R(q):=Q
​′(ϕ2​(q))Q
​′(ϕ0​(q))​,

then
Δ′(q)=0  ⟺  R(q)=1.
Δ′(q)=0⟺R(q)=1.

So the strict-excess fork is equivalently the existence of an interior zero of R−1R−1. This gave a cleaner scalar target than the original geometric phrasing. Again, this is my downstream attack from the inverse-branch formulation in the summary.

Third, letting
F(u):=1Q^′(u),
F(u):=Q
​′(u)1​,

the inverse branches satisfy the same scalar ODE
ϕi′(q)=F(ϕi(q)),
ϕi′​(q)=F(ϕi​(q)),

and the gap satisfies
Δ′(q)=F(ϕ2(q))−F(ϕ0(q)).
Δ′(q)=F(ϕ2​(q))−F(ϕ0​(q)).

By the mean-value theorem there exists ξ(q)ξ(q) between the two outer inverse branches with
Δ′(q)=F′(ξ(q)) Δ(q)=− Q^′′(ξ(q))(Q^′(ξ(q)))2 Δ(q).
Δ′(q)=F′(ξ(q))Δ(q)=−(Q
​′(ξ(q)))2Q
​′′(ξ(q))​Δ(q).

So the sign of Δ′Δ′ is controlled by the sign of
H(u):=Q^′′(u)(Q^′(u))2
H(u):=(Q
​′(u))2Q
​′′(u)​

somewhere on the same-value chord joining the two outer runs. This was another new attack reduction: the strict-excess fork becomes a sign/comparison problem for a single scalar field HH on the outer-run transport strip, or more sharply on the family of same-value chords. This is my own reasoning layered on top of the quotient-visible inverse-branch model.

Fourth, I derived the Green-kernel identity
Δ(q)−ℓΔ(q)=∫q−q+G(q,s) Δ′′(s) ds=∫q−q+G(q,s) (Ξ(ϕ0(s))−Ξ(ϕ2(s))) ds,
Δ(q)−ℓΔ​(q)=∫q−​q+​​G(q,s)Δ′′(s)ds=∫q−​q+​​G(q,s)(Ξ(ϕ0​(s))−Ξ(ϕ2​(s)))ds,

with
Ξ(u)=Q^′′(u)(Q^′(u))3.
Ξ(u)=(Q
​′(u))3Q
​′′(u)​.

This shows that the deviation of the outer branch-gap from its endpoint chord is controlled exactly by inverse-curvature mismatch across the outer runs. From there, I proposed the next theorem target: represent the three-lobe defect
Λ:=B1−(B0+B2)
Λ:=B1​−(B0​+B2​)

as a positive weighted functional of Δ−ℓΔΔ−ℓΔ​, equivalently of the inverse-curvature mismatch. If that lands, strict excess becomes a weighted inverse-curvature imbalance problem. This is not in the summaries; it is the main new analytic attack I added on top of them.

So on the exact fixed-shear side, the new progress since the latest paper update is: the live strict-excess case has been reduced further from a generic transport problem to a very specific family of one-variable targets:

    same-value same-slope exclusion on the outer runs,

    or one-crossing / monotonicity of R(q)−1R(q)−1,

    or a sign theorem for H(u)=Q^′′/Q^′2H(u)=Q
    ​′′/Q
    ​′2 on the outer-run transport strip or same-value chords,

    or a weighted-ΛΛ formula for the branch-gap convexity defect.

These are not proved yet, but the attack now has a concrete scalar shape it did not have before.
4. The nonmixed/global affine lane is now reduced but not closed

On the nonmixed/global affine 4-point side, the derivative-geometry work produced both a real theorem-level compression and a hard ceiling.

The compression is: fixed-slope tangency/incidence is now a finite remote endpoint/branch incidence problem. Support curves are strictly concave pseudolines with single-crossing and betweenness; each ordered pair has at most one overlap window; endpoint-gap functions Gξ,JGξ,J​ are strictly monotone, so each endpoint/branch pair contributes at most one remote tangent incidence. Thus the one-pair global burden is no longer “arbitrary recurrence.” It is the existence of zeros of explicit monotone remote endpoint-gap functions. That is a real improvement.

The ceiling is equally important: intrinsic affine/pseudoline geometry is universal for analytic plane curves. So pure planar / pseudoline / interval-graph / local-convexity arguments cannot close this lane by themselves. Any final contradiction there must use RH-specific source structure, not more naked affine geometry. This is a genuine no-go, not just a tactical preference.

My own attack on this side therefore stopped trying to squeeze more pure geometry out of the lane. I now regard the correct target as: identify an RH-specific or source-coupled datum that constrains the zeros of the endpoint-gap functions Gξ,JGξ,J​, and then prove a one-zero impossibility or incompatibility theorem for those endpoint-gap zeros. In other words, derivative geometry has already taken the affine lane to its universal ceiling; what remains there is no longer geometric but source-coupled.
5. The honest order-7 / package lane is now much clearer

The corrected target on the package side is now explicit. The real order-7 datum is the quotient-septic class
[A7f],
[A7f​],

not raw septic equality. Once two corrected germs agree through cubic and quintic order and differ only by odd reparametrization plus already-quotiented lower scalar/value gauge, the remaining order-7 mismatch is automatically a multiple of A5fA5f​, so the quotient-septic defect vanishes. Therefore the honest theorem target is a package-level statement such as same reduced image germ at coincidence or collision-functoriality of the corrected block triple on the collision/cancellation locus. If that holds, the quotient-septic collapse is formal afterward. This is now the clean global/package-level lane.

The generator audit is central here. The consolidated update says no second primitive pointwise highest-new generator has appeared beyond r=q(7)r=q(7), and all the currently explicit pointwise fields remain in the differential algebra generated by the lower field and rr. So if a package-level obstruction survives, it should be transport/global/provenance-sensitive, not another local scalar field.

My attack then tried to connect this package lane to the local good-patch lane.

On a good patch
J⋐{c≠0, M≠0},M:=c(A5f)′−c′A5f,
J⋐{c=0, M=0},M:=c(A5f​)′−c′A5f​,

the one-pair tangent model pair (C,K)(C,K) has a unique cubic-killing section θ∗(m)θ∗​(m), and the quintic value there is
Γ(m)=K(m,θ∗(m))=−M(m)c(m)2+c′(m)2≠0.
Γ(m)=K(m,θ∗​(m))=−c(m)2+c′(m)2
​M(m)​=0.

So any local same-order compensator must change the section-corrected quintic gap after recentering the cubic-zero section. For an infinitesimal deformation pair (U,V)(U,V), that detector is
δΓ(U,V)=V(m,θ∗(m))−∂θK(m,θ∗(m))∂θC(m,θ∗(m)) U(m,θ∗(m)).
δΓ(U,V)​=V(m,θ∗​(m))−∂θ​C(m,θ∗​(m))∂θ​K(m,θ∗​(m))​U(m,θ∗​(m)).

A same-order local compensator is exactly a deformation with
δΓ=−Γ≠0.
δΓ=−Γ=0.

This led to my sharp bridge conjecture:

    odd reparametrization and lower scalar/value gauge should satisfy δΓ=0δΓ=0;

    therefore any deformation with δΓ≠0δΓ=0 is nontrivial in the reduced deformation quotient;

    and the first nontrivial transverse class should already be the quotient-septic class.

Equivalently, a local good-patch compensator should already be quotient-septic-visible. The useful contrapositive is:
[A7f]=0⟹no same-order local compensator on any good patch.
[A7f​]=0⟹no same-order local compensator on any good patch.

That is not yet proved, but it is the cleanest bridge I found between the local good-patch lane and the global package lane.

Then I sharpened this further inside the explicit pointwise package. If the no-second-generator audit really implies that the first explicit pointwise transverse quotient is at most one-dimensional and generated by the rr-direction, then there is a complete dichotomy:

    either the rr-direction is gap-visible, meaning δΓr≠0δΓr​=0, in which case inside the explicit pointwise package
    local good-patch compensator⟺[A7f]≠0;
    local good-patch compensator⟺[A7f​]=0;

    or the rr-direction is gap-invisible, meaning δΓr=0δΓr​=0, in which case the whole explicit pointwise package is invisible to the local good-patch detector and any surviving local obstruction must already lie outside that explicit package, i.e. in nonlocal transport, collision-functoriality failure, or genuinely provenance-sensitive two-atom data.

This is one of the sharpest new endgame forks I arrived at. It is not in the paper update itself; it is my inference from combining the generator audit with the section-corrected gap detector. Its premises come from the generator audit and the good-patch tangent model.
6. Strong no-gos and ceilings that now look permanent

A number of negative findings are now strong enough that I would treat them as permanent ceilings unless new source-coupled structure is injected.

The naive source-summed corrected-block route cannot produce the needed linear merger law.

The exact fixed-shear corner cannot be saved by a second local transport variable, a third finite-order reset, or a two-pair model built only from separate descended one-atom states with natural merger law.

The whole analytic swap-invariant local scalar layer on the cancellation chart is dead. Any such scalar is automatically quadratic-harmless.

The quartic–sextic rung is inert for the explicit lobe-amplitude obstruction.

No second explicit pointwise highest-new generator exists in the current package beyond r=q(7)r=q(7).

The nonmixed/global affine lane has reached the universal ceiling of pure affine/pseudoline geometry. Any further progress there has to be RH-specific or source-coupled.

Those are real ceilings, not just failed attempts.
7. The best current attack map

So where does the attack now stand?

On the 2-point / exact fixed-shear side, the best live attack is the strict-excess fork. The cleanest current theorem targets are:

    same-value same-slope exclusion on separated outer runs;

    one-crossing / monotonicity for the slope ratio R(q)R(q);

    a sign theorem for H(u)=Q^′′(u)/Q^′(u)2H(u)=Q
    ​′′(u)/Q
    ​′(u)2 on the outer-run transport strip or same-value chords;

    or a representation of the three-lobe defect Λ=B1−(B0+B2)Λ=B1​−(B0​+B2​) as a positive weighted functional of Δ−ℓΔΔ−ℓΔ​, reducing strict excess to a weighted inverse-curvature mismatch problem.

These are now genuinely one-variable statements inside the quotient-visible exact fixed-shear model. They are the sharpest new mathematical targets I produced since the most recent paper update. Their premises come from the consolidated fixed-shear reduction.

On the nonmixed/global affine side, the right next attack is no longer more naked affine geometry. It is RH-specific exclusion of zeros of the monotone remote endpoint-gap functions Gξ,JGξ,J​. That is the only live burden left there.

On the package/order-7 side, the right next attack is same reduced image germ or collision-functoriality of the corrected block triple. The most ambitious conjectural bridge is that on a good patch the local good-patch compensator detector δΓδΓ and the quotient-septic detector are actually the same first transverse direction, at least inside the explicit pointwise package.
8. Bottom line

The strongest honest summary is this.

Since the latest paper draft, my attack has not produced a miracle closure. But it has substantially clarified the endgame. The fixed-shear residual corner is now compressed to a quotient-visible transport problem with very little finite local freedom. The local chart is corrected, and the whole analytic swap-invariant local scalar layer is dead there. The strict three-run case is now a one-variable inverse-branch-gap problem, and I pushed that further to phase-curve self-intersections, slope-ratio RR, the inverse-slope field F=1/Q^′F=1/Q
​′, the strip scalar H=Q^′′/Q^′2H=Q
​′′/Q
​′2, and a Green-kernel identity for the branch-gap convexity defect. The nonmixed/global affine side has been reduced to RH-specific remote endpoint-gap incidences. The explicit pointwise highest-new package has been audited and shown not to contain a second pointwise generator, which led me to a sharp explicit-pointwise dichotomy based on whether the unique rr-direction is gap-visible or gap-invisible. And the honest order-7 lane is now clearly about quotient-septic obstruction and package-level collision functoriality, not raw septic equality.

So the current frontier is no longer vague. It is now concentrated in a small set of concrete theorem targets:

    kill the strict-excess fixed-shear fork by a one-variable theorem on ΔΔ, RR, FF, HH, or ΛΛ;

    or prove enough package-level functoriality / same reduced image germ to collapse the quotient-septic obstruction;

    or, failing that, identify the first genuinely nonlocal / provenance-sensitive two-atom datum that survives after all the local universal structure has been squeezed away.


