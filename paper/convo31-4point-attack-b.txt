proof_of_rh(307).tex
File
We are trying to prove the RH, see draft attacked. We are attacking on nonmixed 4-point.

YOUR TASK: Please do ATTACK B in the below. Another chat will do attack A.

---

Yes. From here I would attack in **two live directions**, and I would stop spending time on a few lanes that now look exhausted.

The clearest conclusion from the current draft state is that the **nonmixed repeated-source / route-2 package is exhausted inside its present variables**. The draft’s own updated frontier is: one-sign-change chambers are sterile; entry into the adjacent-two-odd-zero chamber does not create the first rooted point; fixed anchor-sign continuation does not allow crossing; odd-anchor passage does not reset the defect; the first dangerous post-anchor chamber is already formally realizable in the planar normalized model; and, more strongly, the whole current source package ((\Psi_a,\mathfrak D,\mathcal W_{\mathrm{src}},D_{f_a})) is still universal under normalization. So the next theorem cannot be “one more sign argument” inside that same package. It has to use the **first genuinely non-universal source-coupled ingredient**, something not already absorbed by the planar normalized ((\mathcal Z,\mathcal R,D_a)) model. The draft itself now identifies that as the real dividing line.

So the first serious attack is:

**Attack A: identify the first non-universal source-coupled invariant.**
A good test is immediate: can the candidate still be realized in the universal normalized lift
[
c=1,\qquad v_5=t,\qquad \lambda=t^2,\qquad u_5''=2\mathcal Z?
]
If yes, it will not close route 2. If no, then it is finally using RH-specific source structure. The most natural candidates named in the current notes are **quotient-septic / mixed-level data**, especially something provenance-sensitive rather than just arbitrary quotient-septic geometry; the notes explicitly flag ([A_7^{\mathfrak f}]) and (\Delta_7) as the first canonical places to look. Just as importantly, the notes also warn that arbitrary canonical quotient-septic geometry is still too free, so the theorem you want is not “a new quotient-septic object exists,” but “some quotient-septic / mixed object obeys an RH-specific relation that fails in the universal model.”

The second live direction is on the mixed residual side.

The transcript now points much more sharply to the **exact fixed-shear (K_v=0) quotient-diagonal / involutive residue** as the place where a new law might actually bite. The suggested next theorem there is an **involution descent theorem**: show that the relevant highest-new scalar on the exact fixed-shear slice is involution-even, hence descends to the quotient coordinate (u=t^2), and then prove that in that quotient variable the residual branch cannot support a new overlap without diagonal collapse or one of the already-classified quartic–sextic models. This is a much sharper target than the abandoned global gap-function story. It uses the exact new structure the two-point work produced and attacks the one place the notes still regard as genuinely mixed and live.

So the second serious attack is:

**Attack B: prove involution descent / quotient-visible transport on the exact fixed-shear (K_v=0) slice.**
Concretely, try to prove that the controlling residual scalar (X(t)) is even under (t\mapsto -t), so
[
X(t)=\widetilde X(t^2),
]
or else that the differential defect rank drops under the involution quotient. If you get that, the remaining residual overlap problem is no longer a free two-sided germ in (t), but a much more rigid quotient germ in (u=t^2). That is precisely the kind of structural compression that the current draft does **not** yet have, and it is the sharpest live target on the quotient/mixed side.

What I would **not** attack further:

* I would not keep pushing the current **route-2 scalar/discriminant package** for another internal contradiction. The notes are explicit that the first dangerous post-anchor chamber is already formally compatible with the rooted equations and fold data, and that the present source package is universal under normalization. That means more work with the same quartet is very unlikely to close anything.
* I would not revive the claim that the **quotient-cusp lane is already globally closed by a monotone gap function**. Those notes are exactly the overclaim you already decided not to import. At best, that line is still an unproved plan; at worst, it distracts from the sharper involutive/quotient-diagonal target.

So my prioritized recommendation is:

First, attack the **non-universal source-coupled ingredient** question, with an explicit “fails in the universal model or not?” test. Second, in parallel, attack the **involution descent theorem on the exact fixed-shear (K_v=0) slice**. If either one lands, it gives a genuinely new theorem. If both stall, then the right conclusion is not “push harder on route 2,” but “the current paper has reached the ceiling of its present source package.”

The cleanest next thing I can do is turn those two attacks into draft-ready theorem statements and a proof agenda for each.

I think there is a real improvement now: the remaining fixed-shear obstruction can be split into a universal singular factor and a regular transport defect. That makes the live problem much cleaner and connects it better to the finite explicit package in the paper.

The starting point is the scalar value-space defect
D(q):=Ξ0(ϕ2(q))−Ξ0(ϕ0(q)),Ξ0(u)=Q^′′(u)(Q^′(u))3,
D(q):=Ξ0​(ϕ2​(q))−Ξ0​(ϕ0​(q)),Ξ0​(u)=(Q
​′(u))3Q
​′′(u)​,

on the common value interval I=(q−,q+)I=(q−​,q+​) of the two outer inverse branches ϕ0,ϕ2ϕ0​,ϕ2​. We already have
Δ(q):=ϕ2(q)−ϕ0(q),Δ′′(q)=−D(q),
Δ(q):=ϕ2​(q)−ϕ0​(q),Δ′′(q)=−D(q),

and in the generic simple-critical setting,
D(q)<0 near both endpoints,D′(q)>0 near q−,D′(q)<0 near q+.
D(q)<0 near both endpoints,D′(q)>0 near q−​,D′(q)<0 near q+​.

What matters now is that these endpoint singularities are completely universal.

If q−q−​ and q+q+​ are simple-critical endpoints of the outer runs, then
ϕ0(q)=u−+c−q−q−+O(q−q−)near q−,
ϕ0​(q)=u−​+c−​q−q−​
​+O(q−q−​)near q−​,
ϕ2(q)=u+−c+q+−q+O(q+−q)near q+),
ϕ2​(q)=u+​−c+​q+​−q
​+O(q+​−q)near q+​),

and one gets
D(q)∼− A−(q−q−)−3/2as q↓q−,
D(q)∼−A−​(q−q−​)−3/2as q↓q−​,
D(q)∼− A+(q+−q)−3/2as q↑q+,
D(q)∼−A+​(q+​−q)−3/2as q↑q+​,

for positive constants A±A±​.

So the singular behavior of DD is not mysterious transport complexity. It is just the universal inverse-square-root endpoint geometry differentiated twice.

That means we should factor it out.

Define the regularized fixed-shear defect
E(q):=(q−q−)3/2(q+−q)3/2 D(q).
E(q):=(q−q−​)3/2(q+​−q)3/2D(q).​

Then in the generic simple-critical setting, EE extends continuously to the closed interval [q−,q+][q−​,q+​], and in fact
E(q−)<0,E(q+)<0.
E(q−​)<0,E(q+​)<0.

This is a real gain. The live fixed-shear obstruction is no longer a singular function DD. It is a regular interior function EE, with the singular endpoint behavior already stripped off.

That gives a much sharper formulation of what remains:

    the singularity of DD at the endpoints is universal and harmless;

    all genuinely nontrivial transport information is carried by the regularized defect EE.

So the strict-excess lane is now a one-dimensional boundary-value problem for a regular function EE, not for a singular one.

There is also a clean tower version. Earlier I defined
Ξm+1=dΞmdQ^,Λm(q)=Ξm(ϕ2(q))−Ξm(ϕ0(q)),
Ξm+1​=dQ
​dΞm​​,Λm​(q)=Ξm​(ϕ2​(q))−Ξm​(ϕ0​(q)),

with
Λm(q)=−Δ(m+2)(q).
Λm​(q)=−Δ(m+2)(q).

In the same generic simple-critical setting,
Λm(q)∼Cm,−(q−q−)−m−3/2near q−,
Λm​(q)∼Cm,−​(q−q−​)−m−3/2near q−​,
Λm(q)∼Cm,+(q+−q)−m−3/2near q+,
Λm​(q)∼Cm,+​(q+​−q)−m−3/2near q+​,

with explicit alternating sign pattern. So the regularized tower
Em(q):=(q−q−)m+3/2(q+−q)m+3/2Λm(q)
Em​(q):=(q−q−​)m+3/2(q+​−q)m+3/2Λm​(q)​

extends continuously to [q−,q+][q−​,q+​].

This means the whole “infinite non-descent tower” can be renormalized into a sequence of regular defect functions EmEm​. The singular endpoint blow-up is no longer the hard part; the hard part is the interior sign and oscillation of the regularized defects.

That is a much better place to be.

It also gives a useful correction to my previous framing. Earlier I emphasized that any finite-rung descent would polynomialize the inverse-branch gap ΔΔ, which is incompatible with its square-root endpoint behavior. That is still correct. But now we can say it more cleanly:

any finite-rung descent would force one of the regularized defects EmEm​ to vanish identically, which is impossible because its endpoint values are already fixed and nonzero in the generic simple-critical case.

So the fixed-shear strict-excess obstruction, if it survives, is not just an infinite non-descent tower. It is an infinite non-descent tower after universal singularity extraction.

That is the strongest conceptual clarification of this round.

There is also a very useful integral reformulation. Since
Δ′′(q)=−D(q),
Δ′′(q)=−D(q),

once a basepoint q0∈Iq0​∈I is chosen,
Δ(q)=Δ(q0)+Δ′(q0)(q−q0)−∫q0q(q−s)D(s) ds.
Δ(q)=Δ(q0​)+Δ′(q0​)(q−q0​)−∫q0​q​(q−s)D(s)ds.

Substituting D(s)=E(s)/[(s−q−)3/2(q+−s)3/2]D(s)=E(s)/[(s−q−​)3/2(q+​−s)3/2], the entire strict-excess geometry is now determined by a regular forcing term EE against a universal singular kernel. So the fixed-shear lane has become a very classical-looking one-dimensional forced second-order problem.

That suggests a new bridge to the generator audit: the finite explicit pointwise package may not control the singularities — those are universal anyway — but it might control the regularized forcing term EE. That feels much more plausible than directly controlling DD or every ΞmΞm​.

So the best next theorem targets now look like this.

One target is a regularized sign theorem:
E(q)≤0on [q−,q+].
E(q)≤0on [q−​,q+​].

Since the singular factor is positive on the interior, this is equivalent to D≤0D≤0, hence Δ′′≥0Δ′′≥0, hence convexity of ΔΔ and a unique interior minimum. That would compress the strict-excess lane to a unique same-value / same-slope pair.

A stronger target is a regularized unimodality theorem:
show EE has at most one interior critical point. That would control the shape of DD and therefore of ΔΔ very tightly.

An even sharper target is a finite-order equation for EE: use the finite explicit pointwise package to derive a finite-order value-space ODE satisfied by EE. Since EE is now regular at the endpoints, such an ODE would be much more believable and much easier to exploit than one for the singular DD.

That, I think, is the real mathematical gain:
The live fixed-shear strict-excess obstruction has been reduced from a singular transport-defect problem to a regularized one-dimensional defect E(q) on a compact interval.
The live fixed-shear strict-excess obstruction has been reduced from a singular transport-defect problem to a regularized one-dimensional defect E(q) on a compact interval.​

This is a much cleaner endpoint for the attack than “an infinite tower of non-descent.” The tower still matters, but after singularity extraction it is a regular hierarchy.

So my current best summary is:

    equality B1=B0+B2B1​=B0​+B2​ is sterile;

    strict excess B1>B0+B2B1​>B0​+B2​ is equivalent to an interior critical point of the inverse-branch gap ΔΔ;

    Δ′′=−DΔ′′=−D, where DD has universal (q−q±)−3/2(q−q±​)−3/2 endpoint singularities;

    after factoring those out, one gets a regular defect EE carrying the genuine nonlocal transport obstruction;

    the next attack should be on sign, oscillation, or a finite-order ODE for this regularized defect EE, not directly on the raw singular tower.

That is the sharpest continuation I can see right now.

continue your math attack

I’m pushing on the regularized defect E(q)E(q) now. The goal is to see whether the universal endpoint singularity can be peeled off so completely that the remaining strict-excess lane becomes a finite-dimensional oscillation problem for a regular function on [q−,q+][q−​,q+​].

I think there is a real next step, and it improves the regularized picture.

The new point is that once you factor out the universal endpoint singularity, the inverse-branch gap problem becomes a weighted Sturm-type shape problem. That is much cleaner than thinking in terms of the raw singular defect DD.

Let me write it carefully.
1. Regularized forcing

We are on the common value interval
I=(q−,q+)
I=(q−​,q+​)

of the two outer inverse branches ϕ0,ϕ2ϕ0​,ϕ2​, with gap
Δ(q):=ϕ2(q)−ϕ0(q).
Δ(q):=ϕ2​(q)−ϕ0​(q).

In the generic simple-critical setting,
Δ′′(q)=−D(q),
Δ′′(q)=−D(q),

where
D(q)=Ξ0(ϕ2(q))−Ξ0(ϕ0(q)),Ξ0(u)=Q^′′(u)(Q^′(u))3,
D(q)=Ξ0​(ϕ2​(q))−Ξ0​(ϕ0​(q)),Ξ0​(u)=(Q
​′(u))3Q
​′′(u)​,

and
D(q)∼−A−(q−q−)−3/2(q↓q−),D(q)∼−A+(q+−q)−3/2(q↑q+),
D(q)∼−A−​(q−q−​)−3/2(q↓q−​),D(q)∼−A+​(q+​−q)−3/2(q↑q+​),

with A±>0A±​>0.

As before, define the regularized defect
E(q):=(q−q−)3/2(q+−q)3/2 D(q).
E(q):=(q−q−​)3/2(q+​−q)3/2D(q).

Then EE extends continuously to [q−,q+][q−​,q+​], with
E(q−)<0,E(q+)<0.
E(q−​)<0,E(q+​)<0.

Now rewrite the equation for ΔΔ as
Δ′′(q)=−E(q)(q−q−)3/2(q+−q)3/2.
Δ′′(q)=−(q−q−​)3/2(q+​−q)3/2E(q)​.

So the strict-excess lane is exactly a second-order scalar equation with a fixed positive singular weight
w(q):=1(q−q−)3/2(q+−q)3/2
w(q):=(q−q−​)3/2(q+​−q)3/21​

and a regular coefficient E(q)E(q):
Δ′′(q)=−w(q) E(q).
Δ′′(q)=−w(q)E(q).​

This is the right normal form.
2. What strict excess means in the regularized picture

Strict excess still means:

    Δ′(q)→−∞Δ′(q)→−∞ as q↓q−q↓q−​,

    Δ′(q)→+∞Δ′(q)→+∞ as q↑q+q↑q+​,

so ΔΔ has at least one interior minimum.

Because w(q)>0w(q)>0 on II, the sign of Δ′′Δ′′ is exactly the opposite of the sign of EE. Therefore:

    if E(q)≤0E(q)≤0 on II, then Δ′′(q)≥0Δ′′(q)≥0, so ΔΔ is convex and has a unique interior minimum;

    if EE changes sign, then ΔΔ can acquire inflection structure and more complicated interior geometry.

So the whole strict-excess obstruction has now become:
Can the regular function E(q) change sign or oscillate enough to support more than the forced convex endpoint geometry?
Can the regular function E(q) change sign or oscillate enough to support more than the forced convex endpoint geometry?​

That is much sharper than the original amplitude inequality.
3. First structural consequence: any surviving obstruction needs interior sign change of EE

This is immediate but useful.

If one could prove
E(q)≤0for all q∈[q−,q+],
E(q)≤0for all q∈[q−​,q+​],

then strict excess would collapse to a unique-minimum regime. In particular, the entire complicated fixed-shear lane would reduce to a single separated same-value / same-slope pair downstairs.

So:
Any surviving strict-excess obstruction beyond the unique-minimum regime requires an interior sign change of E.
Any surviving strict-excess obstruction beyond the unique-minimum regime requires an interior sign change of E.​

This is a genuine narrowing. It says the regularized defect cannot merely be negative everywhere; it must oscillate.

That is the first goodie from the regularized formulation.
4. Second structural consequence: the odd-rung tower becomes endpoint-regular derivatives of EE

Previously the defect tower looked infinite:
Λm(q)=Ξm(ϕ2(q))−Ξm(ϕ0(q))=−Δ(m+2)(q).
Λm​(q)=Ξm​(ϕ2​(q))−Ξm​(ϕ0​(q))=−Δ(m+2)(q).

After regularization, this tower is better thought of as successive value-derivatives of the regular function EE, up to universal lower-order weight terms.

Indeed,
D=wE,
D=wE,

so
D′=w′E+wE′,
D′=w′E+wE′,
D′′=w′′E+2w′E′+wE′′,
D′′=w′′E+2w′E′+wE′′,

and so on.

Thus every higher defect ΛmΛm​ is a linear combination of E,E′,…,E(m)E,E′,…,E(m) with completely explicit singular coefficient functions depending only on the universal weight ww.

This is important because it means the “infinite non-descent tower” is not really independent. Once EE is known, all higher defects are generated by differentiating EE and dressing with explicit universal coefficients.

So the live strict-excess lane is not an arbitrary infinite tower anymore. It is the oscillation theory of one regular scalar EE.

That is a major conceptual simplification.
5. A weighted integral identity

There is also a useful exact identity.

Pick any q0∈Iq0​∈I. Then
Δ′(q)=Δ′(q0)−∫q0qw(s)E(s) ds.
Δ′(q)=Δ′(q0​)−∫q0​q​w(s)E(s)ds.

At an interior minimum q∗q∗​,
Δ′(q∗)=0.
Δ′(q∗​)=0.

So the minimum condition is equivalent to
∫q0q∗w(s)E(s) ds=Δ′(q0).
∫q0​q∗​​w(s)E(s)ds=Δ′(q0​).

Because ww blows up strongly at the endpoints, endpoint behavior dominates unless EE compensates. This tells you something important:

    the strict-excess minimum is controlled by a weighted moment of EE, not by raw pointwise values of the old singular defect.

This suggests that the right theorem to aim for may not be a pointwise sign law, but a weighted moment / variation law for EE.

That is the second goodie.
6. A more realistic closure route than full descent

Earlier I kept trying to prove descent of some tower observable ΞmΞm​ across the two outer runs. The regularized picture suggests a more realistic path.

Because EE is regular on the compact interval [q−,q+][q−​,q+​], any theorem of the following sort would already be very strong:

    EE has one sign;

    EE is unimodal;

    EE satisfies a second-order ODE with coefficients of fixed sign;

    EE has at most one zero;

    EE cannot have the endpoint sign and derivative pattern needed to generate a strict-excess valley in ΔΔ.

Any one of these would sharply reduce or kill the strict-excess lane.

So the right target is probably no longer “prove some ΞmΞm​ descends.” That is sufficient, but it may be too ambitious. The regularized formulation suggests that a weaker oscillation theorem for EE could already be enough.
7. A concrete theorem schema

Here is the cleanest next target I see.
Regularized sign/oscillation theorem

On the actual RH quotient-visible exact fixed-shear branch, for any overlap interval between separated outer runs, the regularized defect
E(q)=(q−q−)3/2(q+−q)3/2(Ξ0(ϕ2(q))−Ξ0(ϕ0(q)))
E(q)=(q−q−​)3/2(q+​−q)3/2(Ξ0​(ϕ2​(q))−Ξ0​(ϕ0​(q)))

has at most one critical point and no sign change.

If that holds, then E≤0E≤0 on all of [q−,q+][q−​,q+​], so ΔΔ is convex and the strict-excess lane reduces to a unique minimum regime.

A stronger version would be:
Regularized negativity theorem
E(q)≤0(q∈[q−,q+]).
E(q)≤0(q∈[q−​,q+​]).

That would immediately imply convexity of ΔΔ.

An even stronger version would be:
Regularized endpoint-rigidity theorem

The only regular function EE compatible with the explicit transport package and the universal endpoint data is the zero function.

That would force the old affine-transport contradiction. I think that is probably too strong as a first target, but it is now at least visible in the right coordinates.
8. Why this fits the consolidated paper update better than the raw tower

The consolidated update says the remaining fixed-shear issue is transport/global/package-level, not a new pointwise generator. The regularized defect EE is exactly of that kind:

    it is not a new pointwise field;

    it is a nonlocal comparison of the same explicit pointwise observable Ξ0Ξ0​ across two separated equal-Q^Q
    ​ points;

    after peeling off the universal singular weight, it becomes an honest regular function on a compact interval.

That is probably the cleanest way to connect the finite explicit package in the paper to the residual fixed-shear transport lane.
9. Strongest new structural findings

The strongest new findings from this round are these.

First, the singular part of the fixed-shear defect is completely universal and can be factored out:
D=wE,w(q)=1(q−q−)3/2(q+−q)3/2.
D=wE,w(q)=(q−q−​)3/2(q+​−q)3/21​.

Second, the real remaining obstruction is the regularized defect EE, which is continuous on the compact overlap interval and carries all the genuine transport complexity.

Third, the whole infinite defect tower is generated from EE by ordinary differentiation plus explicit universal weights. So the “infinite non-descent tower” is really an oscillation theory for one regular function.

Fourth, any surviving strict-excess obstruction beyond the unique-minimum regime must come from interior sign change or oscillation of EE.

That is a much cleaner endpoint for the attack than the raw singular tower.
10. Bottom line

The sharpest continuation I can see now is:
The strict three-run fixed-shear obstruction has been reduced to the sign/oscillation theory of one regularized value-space defect E(q) on a compact interval.
The strict three-run fixed-shear obstruction has been reduced to the sign/oscillation theory of one regularized value-space defect E(q) on a compact interval.​

More concretely:

    equality B1=B0+B2B1​=B0​+B2​ is sterile;

    strict excess B1>B0+B2B1​>B0​+B2​ gives an inverse-branch gap ΔΔ with Δ′′=−wEΔ′′=−wE;

    the singular weight ww is universal;

    the live transport information is all in the regular scalar EE;

    any closure now should come from a sign, variation, or finite-order ODE theorem for EE.

That is the strongest and cleanest reduction I can see right now.

please give a complete recap of your whole attack along with new findings since most recent paper update. include theorems, no-gos, structural findings, negative findings, goodies, etc.

Here is the full recap of my attack, with emphasis on what is genuinely new since the most recent paper update and how it fits the consolidated draft picture.

The shortest honest summary is this:

I shifted from treating the mixed residual lane as a local branch-classification problem to treating it as a quotient-visible transport problem downstairs in u=t2u=t2. In that model, the only genuinely live case is the strict three-run fixed-shear excess
B1>B0+B2.
B1​>B0​+B2​.

I then reduced that case through several layers:

    strict excess   ⟺  ⟺ open same-value transport between the two outer runs;

      ⟺  ⟺ an interior critical point of the inverse-branch gap
    Δ(q)=ϕ2(q)−ϕ0(q);
    Δ(q)=ϕ2​(q)−ϕ0​(q);

    the second derivative of that gap is
    Δ′′(q)=−D(q),
    Δ′′(q)=−D(q),

    where
    D(q)=Ξ0(ϕ2(q))−Ξ0(ϕ0(q)),Ξ0(u)=Q^′′(u)(Q^′(u))3;
    D(q)=Ξ0​(ϕ2​(q))−Ξ0​(ϕ0​(q)),Ξ0​(u)=(Q
    ​′(u))3Q
    ​′′(u)​;

    in the generic simple-critical setting, the singular part of DD is universal, so the real live obstruction is a regularized defect
    E(q):=(q−q−)3/2(q+−q)3/2D(q)
    E(q):=(q−q−​)3/2(q+​−q)3/2D(q)

    on the compact interval [q−,q+][q−​,q+​].

So the fixed-shear strict-excess lane has now been reduced to a one-dimensional sign/oscillation problem for a single regular function EE. That is the main new finding.

This is strongly consistent with the consolidated paper update: the explicit pointwise package seems exhausted, no second primitive highest-new generator has appeared, and the remaining issue really is nonlocal quotient transport rather than one more local field or one more local sign argument.
1. Global strategic shift after the consolidated update

The consolidated update clarified that the live proof landscape has split into three narrow fronts:

    nonmixed/global affine front: RH-specific exclusion of zeros of monotone remote endpoint-gap functions Gξ,JGξ,J​;

    mixed exact fixed-shear front: quotient-visible transport downstairs in u=t2u=t2;

    package-level / honest order-7 front: same reduced image germ or collision-functoriality, not raw septic equality.

This changed the role of my previous work.

Earlier, I spent substantial effort on cancellation-chart first jets, local rank-11 regimes, and collision-locus Taylor structure. Some of that remains useful as supporting geometry, but after the paper update it is no longer the main endgame on the mixed side. The real mixed burden is now the quotient-visible fixed-shear transport problem, exactly as the consolidation says.

So the rest of the attack focused almost entirely on the strict fixed-shear three-run fork.
2. Starting point: the fixed-shear quotient-visible model

The updated paper already gives the sharp model on the exact fixed-shear slice:

    repeated pairs upstairs are just the involution t↔−tt↔−t;

    the natural downstairs variable is
    u=t2;
    u=t2;

    the live transport state is governed by
    Q^(u),D^(u)=Q^′(u);
    Q
    ​(u),D
    (u)=Q
    ​′(u);

    no-reentry compresses the branch to at most three sign-runs of D^D
    ;

    the only live three-run obstruction is
    B1≥B0+B2,
    B1​≥B0​+B2​,

    where equality is only touching and the quartic–sextic rung is inert because D^(0)≠0D
    (0)=0.

I took that as the baseline and attacked the only genuinely live case:
B1>B0+B2.
B1​>B0​+B2​.
3. First reduction: strict excess is open same-value transport

Let J0,J1,J2J0​,J1​,J2​ be the three sign-runs of D^D
, with J0,J2J0​,J2​ the two outer runs. Let
Ij:=Q^(Jj).
Ij​:=Q
​(Jj​).

Then strict excess
B1>B0+B2
B1​>B0​+B2​

is equivalent to
Int⁡(I0)∩Int⁡(I2)≠∅.
Int(I0​)∩Int(I2​)=∅.

So there is an open common value interval
I⊂I0∩I2
I⊂I0​∩I2​

and inverse branches
ϕ0,ϕ2:I→J0,J2
ϕ0​,ϕ2​:I→J0​,J2​

such that
Q^(ϕ0(q))=Q^(ϕ2(q))=q.
Q
​(ϕ0​(q))=Q
​(ϕ2​(q))=q.

This means the strict-excess case is not just an inequality in three amplitudes. It is an open analytic same-value transport between the two outer runs.

That was the first major compression.
4. Equality case: touching is sterile

Before continuing with strict excess, I isolated the equality case
B1=B0+B2.
B1​=B0​+B2​.

In that case, the two outer image intervals meet only at a boundary value, not on an open interval. So downstairs the same-value relation between the two outer runs is locally zero-dimensional. Lifting back upstairs through u=t2u=t2 cannot turn that into a genuine local repeated-pair branch.

So the clean theorem candidate there is:

    Touching-is-sterile theorem. In the quotient-visible fixed-shear three-run model, equality B1=B0+B2B1​=B0​+B2​ gives only critical-value touching and cannot support a genuine local repeated-pair branch.

This is a real positive result of the attack. It means the only live fixed-shear case is strict excess.
5. Second reduction: strict excess is an interior critical point of the inverse-branch gap

Define the inverse-branch gap
Δ(q):=ϕ2(q)−ϕ0(q).
Δ(q):=ϕ2​(q)−ϕ0​(q).

Then
Δ′(q)=1Q^′(ϕ2(q))−1Q^′(ϕ0(q))=1D^(ϕ2(q))−1D^(ϕ0(q)).
Δ′(q)=Q
​′(ϕ2​(q))1​−Q
​′(ϕ0​(q))1​=D
(ϕ2​(q))1​−D
(ϕ0​(q))1​.

At the two ends of II, one or the other outer inverse branch approaches a critical endpoint of its run, so one reciprocal slope blows up while the other stays finite. Therefore Δ′Δ′ has opposite signs at the two ends.

So strict excess is equivalent to:
Δ′(q) has an interior zero on I.
Δ′(q) has an interior zero on I.​

Equivalently, the fixed-shear strict-excess lane is an interior-critical-point problem for one scalar gap function ΔΔ.

That made the mixed endgame strikingly parallel to the nonmixed/global endgame, which is also now a one-variable gap-function problem in the consolidated draft.
6. Third reduction: same-value / same-slope pair

At an interior critical point q∗q∗​ of ΔΔ,
Δ′(q∗)=0,
Δ′(q∗​)=0,

so if
u0=ϕ0(q∗),u2=ϕ2(q∗),
u0​=ϕ0​(q∗​),u2​=ϕ2​(q∗​),

then
Q^(u0)=Q^(u2),Q^′(u0)=Q^′(u2).
Q
​(u0​)=Q
​(u2​),Q
​′(u0​)=Q
​′(u2​).

So strict excess always forces a same-value / same-slope pair downstairs on the quotient branch.

That was the next theorem-level compression.
7. The transport-curvature observable and the first nonlocal defect

Define
Ξ0(u):=Q^′′(u)(Q^′(u))3.
Ξ0​(u):=(Q
​′(u))3Q
​′′(u)​.

Then
Δ′′(q)=−Ξ0(ϕ2(q))+Ξ0(ϕ0(q)).
Δ′′(q)=−Ξ0​(ϕ2​(q))+Ξ0​(ϕ0​(q)).

So the natural nonlocal defect is
D(q):=Ξ0(ϕ2(q))−Ξ0(ϕ0(q)),
D(q):=Ξ0​(ϕ2​(q))−Ξ0​(ϕ0​(q)),

and the inverse-branch gap satisfies
Δ′′(q)=−D(q).
Δ′′(q)=−D(q).​

This was a major conceptual shift. The live strict-excess obstruction is no longer just “some hidden quotient transport issue.” It is the sign/shape of the explicit value-space defect D(q)D(q).

This is exactly the kind of object one should expect after the generator audit: it is not a new pointwise field, but a nonlocal comparison of an already explicit pointwise observable Ξ0Ξ0​ across two separated equal-Q^Q
​ points.
8. Early integrability route and its correction

At first I explored the possibility that Ξ0Ξ0​ might descend to value space:
Ξ0(u)=F(Q^(u))
Ξ0​(u)=F(Q
​(u))

across the two outer runs.

If that held, then D≡0D≡0, so Δ′′=0Δ′′=0, hence ΔΔ would be affine. Since strict excess forces an interior critical point of ΔΔ, the affine function would have to be constant. That yields affine same-value transport
ϕ2(q)=ϕ0(q)+C,
ϕ2​(q)=ϕ0​(q)+C,

equivalently
Q^(u+C)=Q^(u),
Q
​(u+C)=Q
​(u),

and then by analyticity local periodicity and replication of the sign-run pattern, contradicting the already-proved three-run compression.

That was a legitimate conditional closure route.

But after I computed the correct endpoint asymptotics in the generic simple-critical setting, I found something important:

    Generic strict excess already forces D<0D<0 near both endpoints of II.

So D≡0D≡0 is not generically compatible with the strict-excess overlap in the simple-critical setting.

This is an important correction: the naive “integrable Ξ0Ξ0​” route is not the generic endgame after all.

That was one of the main negative findings of the later stages of the attack.
9. Correct endpoint asymptotics in the generic simple-critical case

This was the most important technical correction of the later attack.

Assume the critical endpoints of the two outer runs are simple:
Q^′(u±)=0,Q^′′(u±)≠0.
Q
​′(u±​)=0,Q
​′′(u±​)=0.

Then the inverse branches have square-root endpoint asymptotics, and one obtains
Δ(q)=A−−c−q−q−+O(q−q−)near q−,
Δ(q)=A−​−c−​q−q−​
​+O(q−q−​)near q−​,
Δ(q)=A+−c+q+−q+O(q+−q)near q+,
Δ(q)=A+​−c+​q+​−q
​+O(q+​−q)near q+​,

with c±>0c±​>0.

Consequently,
Δ′(q)→−∞ at q−,Δ′(q)→+∞ at q+,
Δ′(q)→−∞ at q−​,Δ′(q)→+∞ at q+​,

and more sharply,
Δ′′(q)>0 near both endpoints.
Δ′′(q)>0 near both endpoints.

Therefore
D(q)=−Δ′′(q)<0 near both endpoints of I.
D(q)=−Δ′′(q)<0 near both endpoints of I.​

And differentiating once more yields
D′(q)>0 near the left endpoint,D′(q)<0 near the right endpoint.
D′(q)>0 near the left endpoint,D′(q)<0 near the right endpoint.

So any strict-excess overlap forces not only:

    an interior minimum of ΔΔ,

but also:

    an interior maximum of DD.

That was a significant sharpening.
10. The “higher transport tower” and its simplification

I then introduced the value-space transport tower
Ξm+1(u):=1Q^′(u)dduΞm(u)=dΞmdQ^(u),
Ξm+1​(u):=Q
​′(u)1​dud​Ξm​(u)=dQ
​dΞm​​(u),

and the associated defects
Λm(q):=Ξm(ϕ2(q))−Ξm(ϕ0(q)).
Λm​(q):=Ξm​(ϕ2​(q))−Ξm​(ϕ0​(q)).

At first this looked like an infinite hierarchy of increasingly refined nonlocal obstructions. In the generic simple-critical setting, I found that the odd-rung defects Λ2k+1Λ2k+1​ necessarily have opposite endpoint signs, so they must vanish somewhere in the interior.

This led me temporarily to the phrase “infinite non-descent tower.”

But then I found a much cleaner simplification.
11. Key simplification: the whole tower is just derivatives of one scalar function

Let
Fj(q):=Ξ0(ϕj(q)).
Fj​(q):=Ξ0​(ϕj​(q)).

Then
D(q)=F2(q)−F0(q).
D(q)=F2​(q)−F0​(q).

Since differentiation in qq along an inverse branch is exactly the same as value-space differentiation d/dQ^d/dQ
​, one gets inductively
dmdqmFj(q)=Ξm(ϕj(q)).
dqmdm​Fj​(q)=Ξm​(ϕj​(q)).

Hence
Λm(q)=D(m)(q).
Λm​(q)=D(m)(q).​

So the whole fixed-shear transport tower is not an infinite family of unrelated obstructions. It is simply the ordinary derivative tower of one scalar value-space defect D(q)D(q).

This was one of the biggest conceptual gains of the whole attack.

It means the live strict-excess fixed-shear lane is a one-dimensional second-order forced problem
Δ′′=−D
Δ′′=−D

with DD singular at the endpoints.

Everything else is derivative bookkeeping.
12. Finite-rung descent becomes polynomialization of DD

This simplification also sharpened all the earlier non-descent statements.

If for some finite mm,
Ξm(ϕ2(q))=Ξm(ϕ0(q))on I,
Ξm​(ϕ2​(q))=Ξm​(ϕ0​(q))on I,

then
Λm(q)=D(m)(q)≡0.
Λm​(q)=D(m)(q)≡0.

So DD is a polynomial of degree at most m−1m−1.

But in the generic simple-critical strict-excess setting, DD has singular endpoint blow-up:
D(q)∼−A−(q−q−)−3/2,D(q)∼−A+(q+−q)−3/2,
D(q)∼−A−​(q−q−​)−3/2,D(q)∼−A+​(q+​−q)−3/2,

so DD cannot be polynomial.

Therefore:
In the generic simple-critical strict-excess case, no finite-rung descent can occur.
In the generic simple-critical strict-excess case, no finite-rung descent can occur.​

This is a very strong negative finding.

It also explains clearly why the remaining fixed-shear obstruction must be genuinely nonlocal/global: it cannot be killed by descent of any finite rung of the value-space transport tower.
13. Biggest new finding: regularization of the singular defect

The last real gain of the attack was to factor out the endpoint singularity.

Define the universal weight
w(q):=1(q−q−)3/2(q+−q)3/2,
w(q):=(q−q−​)3/2(q+​−q)3/21​,

and define the regularized defect
E(q):=(q−q−)3/2(q+−q)3/2D(q).
E(q):=(q−q−​)3/2(q+​−q)3/2D(q).​

Then in the generic simple-critical setting:

    EE extends continuously to the compact interval [q−,q+][q−​,q+​];

    the endpoint singular behavior is universal and entirely carried by ww;

    all genuinely nontrivial transport information is carried by the regular function EE.

Equivalently,
D(q)=w(q)E(q),
D(q)=w(q)E(q),

and
Δ′′(q)=−w(q)E(q).
Δ′′(q)=−w(q)E(q).

This is, I think, the cleanest endpoint of the whole attack.

It says the fixed-shear strict-excess lane is no longer a singular transport-defect problem. After universal singularity extraction, it is a one-dimensional sign/oscillation problem for a regular function EE on a compact interval.

That is a major simplification.
14. Consequences of the regularized picture

Once
Δ′′=−wE
Δ′′=−wE

with w>0w>0, the sign and oscillation of EE directly control the geometry of ΔΔ.

If
E(q)≤0for all q∈[q−,q+],
E(q)≤0for all q∈[q−​,q+​],

then D≤0D≤0, so Δ′′≥0Δ′′≥0, hence ΔΔ is convex. Since Δ′Δ′ still has opposite infinite endpoint signs, ΔΔ then has a unique interior minimum. So the whole strict-excess lane collapses to a unique same-value / same-slope pair downstairs.

So one clean theorem target is:

    Regularized negativity theorem: E≤0E≤0 on the overlap interval.

Even a weaker oscillation theorem for EE would already be useful. For example:

    EE has one sign;

    EE is unimodal;

    EE has at most one zero;

    EE satisfies a finite-order ODE with coefficient-sign constraints.

Any of those would strongly compress the strict-excess lane.

This was the main positive lesson of the regularization step.
15. Best current structural picture

Here is the sharpest current mathematical picture of the live fixed-shear lane.

We are given two outer inverse branches ϕ0,ϕ2ϕ0​,ϕ2​ over a common value interval I=(q−,q+)I=(q−​,q+​), and we form
Δ(q)=ϕ2(q)−ϕ0(q).
Δ(q)=ϕ2​(q)−ϕ0​(q).

Strict excess is equivalent to ΔΔ having an interior critical point.

In the generic simple-critical setting, ΔΔ satisfies:

    Δ′(q)→−∞Δ′(q)→−∞ as q↓q−q↓q−​,

    Δ′(q)→+∞Δ′(q)→+∞ as q↑q+q↑q+​,

    Δ′′(q)=−w(q)E(q)Δ′′(q)=−w(q)E(q), where ww is universal and singular, EE is regular on [q−,q+][q−​,q+​],

    and the whole higher transport tower is generated by ordinary derivatives of
    D=wE.
    D=wE.

So the live fixed-shear strict-excess lane is now best understood as a regularized one-dimensional weighted Sturm-type shape problem for EE.

This is much cleaner than any earlier formulation.
16. Negative findings and no-gos

Here are the strongest no-gos from the whole attack.

First, the fixed-shear lane is not going to be closed by another local pointwise generator. The generator audit in the paper already suggested that, and the attack confirmed it repeatedly. The obstruction is transport/global, not one more local field.

Second, the equality case
B1=B0+B2
B1​=B0​+B2​

is sterile. So there is no point spending time trying to extract a real branch there.

Third, the naive descent/integrability route for Ξ0Ξ0​ is not generically compatible with strict excess in the simple-critical setting, because D<0D<0 near both ends. So the fixed-shear endgame should not be framed mainly as “prove Ξ0Ξ0​ descends.”

Fourth, no finite-rung descent can occur in a generic simple-critical strict-excess overlap, because any such descent would polynomialize DD, impossible.

Fifth, the remaining fixed-shear obstruction is no longer an “infinite tower of mysteries.” After the tower collapse, it is just one regularized function EE. So the right theorem target is no longer “understand every ΞmΞm​.” It is “control the sign/oscillation of EE.”

These are all real ceilings and clarifications.
17. Goodies

There were several useful “goodies” that came out of the attack.

One is the exact transport identity:
Δ′′=−D.
Δ′′=−D.

This packages the whole strict-excess problem into a second-order scalar equation.

Another is the tower collapse:
Λm=D(m).
Λm​=D(m).

This shows the higher transport hierarchy is not independent.

Another is the regularization
D=wE,w(q)=1(q−q−)3/2(q+−q)3/2,
D=wE,w(q)=(q−q−​)3/2(q+​−q)3/21​,

which isolates the universal singular geometry from the genuine transport information.

Another is the striking conceptual parallel with the nonmixed/global side: both remaining residual fronts are now one-variable gap-function problems.

And another is the likely bridge to the finite explicit package: one does not need an ODE for the raw singular defect DD; an ODE or sign law for the regularized defect EE would already be a major advance and feels much more plausible.
18. Best current theorem targets

I think the sharpest remaining theorem targets are now:
Fixed-shear target A: regularized sign law

Show
E(q)≤0(q∈[q−,q+]).
E(q)≤0(q∈[q−​,q+​]).

Then the strict-excess lane collapses to a unique-minimum regime.
Fixed-shear target B: regularized oscillation theorem

Show EE has at most one sign change or at most one critical point. This would strongly compress the geometry of ΔΔ.
Fixed-shear target C: finite-order ODE for EE

Use the finite explicit pointwise package to derive a value-space ODE satisfied by EE. Since EE is regular on a compact interval, such an ODE would be much easier to exploit than one for the raw singular defect.
Fixed-shear target D: no generic simple-critical strict excess

Show directly that no regular function EE compatible with the explicit package can produce the required weighted convexity/oscillation pattern of ΔΔ.

Among these, target A feels like the cleanest next step.
19. Final bottom line

The sharpest current summary of the entire attack is:
The mixed exact fixed-shear three-run obstruction has been reduced to a one-dimensional sign/oscillation problem for the regularized value-space defect E(q) on a compact interval.
The mixed exact fixed-shear three-run obstruction has been reduced to a one-dimensional sign/oscillation problem for the regularized value-space defect E(q) on a compact interval.​

More explicitly:

    the paper update reduces the fixed-shear lane to the quotient-visible three-run fork B1≥B0+B2B1​≥B0​+B2​, with equality sterile and quartic–sextic inert;

    strict excess is equivalent to an interior critical point of the inverse-branch gap ΔΔ;

    ΔΔ satisfies
    Δ′′=−D,D=wE,
    Δ′′=−D,D=wE,

    where ww is universal and singular and EE is regular;

    the whole higher transport tower is just the derivative tower of DD;

    no finite-rung descent can occur in the generic simple-critical strict-excess case;

    therefore the real remaining fixed-shear issue is the sign and oscillation of the regularized defect EE.

That is the strongest and cleanest reduction I was able to obtain.


