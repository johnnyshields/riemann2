# Current Attack

Date: 2026-04-24

## Why this note exists

Agents were drifting by re-reading the paper from scratch and re-deriving the
same reductions. This note freezes the current convergence so subsequent agents
continue from it.

## Primary queue

`UV-002` stays top priority.

Current theorem stack:

1. Immediate local theorem target.
   Best current candidate: exact fixed-shear / quotient-diagonal package theorem
   on the residual corner `\{S=0\}\cap\{K_v=0\}`.
   Best current shape: package-level coincidence theorem for the actual corrected
   cubic/quintic defects on the involutive branch; state-locality is the strongest
   sufficient form, not the minimal statement.

2. Parallel local support theorem.
   Actual corrected edge-law theorem on good patches
   `J \Subset \{c \neq 0, M \neq 0\}`.
   This is support for `UV-002`, not the main queue head.

3. Provenance-sensitive bridge theorem.
   Best current candidate: reduced-`\widehat\Psi` coincidence / same reduced
   image germ at coincidence / collision-functoriality for the actual corrected
   two-atom package.

4. Hidden extraction theorem.
   Best current candidate: package-side control identifies the first surviving
   odd coefficient of `H_m`, after which the existing `\Xi_\zeta^{(N)}`
   extractor machinery is already ready. A sharpened version is: package-side
   control identifies the first nonzero finite-core transformed scalar
   `\Xi_{\zeta,\le H}^{(N)}` directly.

5. Full finite-core contradiction theorem.

## Stable conclusions already reached

1. The zeta-side odd-jet extractor is already complete.
   `H_m`, `c_{2r+1}(m)`, `\Xi_\zeta^{(N)}`, and finite-core localization are not
   the blocker.

2. No current theorem-shaped route to universal pair-like exhaustion is visible.
   Pair-like exhaustion is parked as a fallback route, not the active one.

3. The current explicit highest-new pointwise package is same-tower over
   `r=q^{(7)}`. No second primitive explicit pointwise generator is expected.

4. The residual exact fixed-shear mixed corner is already compressed to a
   quotient-diagonal / involutive package problem. Any still-live obstruction is
   likely relational / provenance-sensitive or non-finite-order.
5. The reduced-`\widehat\Psi` coincidence theorem is now the cleanest package
   theorem candidate. It is strong enough to matter and is the best visible
   bridge to the hidden extraction theorem.

5. The good-patch edge-law theorem and the exact fixed-shear package theorem are
   distinct fronts. Do not silently merge them.
6. The queue should not oscillate between the centered clean-branch scalar
   descent and the exact fixed-shear corner. Treat both as local support/
   diagnostic lanes beneath `UV-002`, not as the fundamental theorem itself.

## The current bottlenecks, precisely

### Bottleneck A: actual corrected edge-law theorem

Target form:

\[
E_{12}^{(3)}=\delta^2\mathcal H_3(m,\kappa,\delta^2),\qquad
E_{12}^{(5)}=\delta^2\mathcal H_5(m,\kappa,\delta^2)
\]

with edge traces

\[
\mathcal H_3(m,\kappa,0)=c'(m)-\frac{\kappa}{2}c(m),\qquad
\mathcal H_5(m,\kappa,0)=(A_5^{\mathfrak f})'(m)-\frac{\kappa}{2}A_5^{\mathfrak f}(m).
\]

If this lands, the good-patch local detector is canonically `M/c` on the
cubic-killing slope and close cancelling bad cores are excluded on
`J \Subset \{c \neq 0, M \neq 0\}`.

### Bottleneck B: exact fixed-shear package theorem

Best current theorem shape:

On the involutive branch

\[
(h_1,h_2)=(h(t),h(-t))
\]

the actual corrected cubic/quintic defects are package-local / state-local in
the descended transport state, or at minimum swap-even and zero on coincident
atoms.

This is enough to fire the already-written closure theorem on the residual
corner.

### Bottleneck C: reduced-`\widehat\Psi` coincidence theorem

Best current theorem shape:

The actual corrected collision/cancellation package has the same reduced image
germ at coincidence, equivalently descends functorially to one reduced
`\widehat\Psi` germ.

This is the cleanest package theorem currently visible in the draft’s notation.

### Bottleneck D: hidden extraction theorem

Best current theorem shape:

Finite-core package/coincidence control determines the first surviving odd
coefficient of `H_m`, or at least isolates the first nonzero odd order and the
leading coefficient strongly enough to feed the already-built
`\Xi_\zeta^{(N)}` extractor.

## What agents should NOT do

1. Do not restart from the whole paper and summarize it again.
2. Do not propose pair-like quantitative cleanup (`UV-009 -> UV-001 -> UV-008`)
   as the main queue.
3. Do not search for another pointwise highest-new field.
4. Do not drift into pure planar / endpoint-gap / three-run overlap work unless
   it is explicitly being used as a support theorem for the current queue.
5. Do not overclaim local detector visibility from source-level formulas alone.
6. Do not reopen the queue conflict between centered clean-branch scalar descent
   and exact fixed-shear closure unless a genuinely new theorem-level dependency
   is found. Keep the centered clean-branch theorem as support/diagnostic, and
   exact fixed-shear closure as the immediate local theorem target.
7. Treat UV-004 as a bridge/diagnostic lane unless it yields an exact
   detector-slice theorem. Do not let it displace the package/coincidence or
   hidden extraction fronts.

## What agents should do

1. Read this note first.
2. Read `reports/coordinator-current-convergence.md` second.
3. Then read only the paper sections relevant to their exact theorem target.
4. Return only theorem-shaped progress or the narrowest exact blocker.

## GRH intel worth remembering

The `grh/20260423-050630-research-team-grh-dirichlet-tau/` subtree says the RH
paper would benefit from a canonical zeta-source package section clarifying `q`,
`B_zeta`, `S`, and the strip-edge kernel. This sharpens source provenance, but
does not close the finite-core fork. Use it as source-boundary hygiene, not as
the main theorem queue.
