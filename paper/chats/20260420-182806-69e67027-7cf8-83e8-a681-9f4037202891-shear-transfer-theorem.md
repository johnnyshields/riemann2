# Shear-Transfer Theorem

- Source: https://chatgpt.com/c/69e67027-7cf8-83e8-a681-9f4037202891
- Created: 2026-04-20 18:28:06 UTC
- Default model: gpt-5-4-thinking

---

## User (2026-04-20 18:28:05)

Your task: "shear-transfer theorem from the sheared repeated-node event to original geometry"

We are trying to prove the Riemann Hypothesis. Please see attached paper draft.

Current status, in one paragraph:

On the **nonmixed (4)-point side**, the local derivative-geometry story is essentially finished. Exact nonmixed contacts force zeros of (\mathcal R); one-sign (\mathcal R)-intervals are sterile; recurrent exact contacts force affine-planar collapse; isolated zeros of (\mathcal R) are classified by parity; and the repeated-source / first-loss mechanism is now fully reduced: first loss occurs **iff** one diagonally approaches an odd zero of (\mathcal R). So there is no separate first-loss obstruction left beyond odd-zero birth. Equivalently, on a nonmixed arc the whole burden is to exclude odd zeros of the source-level Wronskian
[
\mathcal W_{\mathrm{src}}=J_\lambda J_{u_5}'-J_{u_5}J_\lambda'.
]
At the same time, two no-go results are now clear: the current (3)-point package for the original planar curve ((x,S)) does **not** by itself contradict the sheared repeated-node tangent-contact event, and the current source-level ratio/Wronskian/(4\times4)-Wronskian package is still formally too free. So the missing ingredient is now a genuine **source-coupling / shear-transfer theorem**, not more local parity analysis. The repeated-source lane is closed cleanly by the theorem that first loss is exactly diagonal odd-zero birth.

Your task: shear-transfer theorem from the sheared repeated-node event to original geometry

Read the latest draft and work only on the following reduced bottleneck.

The current nonmixed analysis rigidifies first loss to a **sheared repeated-node tangent-contact** event for the planar curve
[
(x,\widetilde Y_\gamma),\qquad \widetilde Y_\gamma=Y-\gamma S,
]
at a curvature-zero point. The current (3)-point worker result says the mature (3)-point package for the original planar curve
[
(x,S)
]
does **not** by itself contradict that sheared event. So the missing theorem is a **shear-transfer theorem**.

Your task is to try to prove one of the following:

1. on a genuine opened nonmixed arc, the sheared repeated-node tangent-contact event cannot actually occur;
2. or such a sheared event necessarily transfers to an **actual** repeated-node tangent-contact event for the original (3)-point geometry of ((x,S)), where the global (3)-point package can kill it;
3. or such a sheared event forces (\mathcal R\equiv 0) on an interval, hence affine-planar collapse.

Please assume the no-go result that the existing (3)-point package for ((x,S)) alone is insufficient. So I do **not** want another attempt to contradict the sheared event just from the derivative geometry of (S). I want you to exploit the extra coupling between (Y) and (S) that comes from the genuine RH source structure. The target is a transfer theorem, not a generic planar contradiction.

## Assistant (2026-04-20 18:28:06)

I’m going into the draft to isolate the exact sheared-event statements, the no-go theorem, and the coupling identities between \(Y\), \(S\), \(u_5\), \(v_5\), \(c\), and the source minors. Then I’ll try to turn that into a transfer mechanism rather than another planar contradiction.

I found a sharper angle than a raw transfer-to-\((x,S)\) contradiction. The sheared event is stronger than just \(\Gamma_a'(x)=0\): it imposes two weighted moment identities for \(\mathcal Z-\mathcal Z(a)\). That seems to force a real multi-crossing pattern of the \(\mathcal Z\)-level, which is genuine \(Y\)–\(S\) coupling and not visible from \(S\) alone.

_Thought for 13m 53s_

I think the clean thing you can actually prove here is the collapse route, not transfer to an actual \(H_3\)-event for \((x,S)\).

The key point is that the sheared event is stronger than just
\[
\Gamma_a'(x)=0.
\]
It gives two moment conditions for
\[
\phi(t):=\mathcal Z(t)-\mathcal Z(a),
\]
with respect to the positive measure \(|S''(t)|\,dt\). That forces \(\phi\) to have at least two sign changes on \((a,x)\), hence at least two interior zeros of
\[
\mathcal R=\mathcal Z'.
\]

So the right theorem is:

- a single sheared repeated-node event forces either \(\mathcal R\equiv 0\) on that interval, or at least two interior \(\mathcal R\)-zeros there;
- therefore an isolated odd zero of \(\mathcal R\) is **shear-sterile**;
- therefore any **diagonal** family of sheared repeated-node events forces accumulation of zeros of \(\mathcal R\), hence \(\mathcal R\equiv 0\) locally, i.e. affine-planar collapse.

That is exactly the kind of source-coupling/shear-transfer statement your current bottleneck wants, and it matches the already-proved fact that the only possible first-loss mechanism is diagonal.

Here is the block I would insert, replacing Remark~\ref{rem:needed-shear-transfer-theorem-nonmixed}.

```latex
\begin{proposition}[A sheared repeated-node event forces two interior $\mathcal R$-zeros]
\label{prop:sheared-repeated-node-forces-two-R-zeros}
Let $J$ be a connected opened nonmixed arc, and let
\[
a<x
\qquad\text{lie in }J.
\]
Set
\[
\gamma:=\mathcal Z(a),
\qquad
\widetilde Y_\gamma:=Y-\gamma S,
\]
and let
\[
\widetilde H_3(a,u):=
\widetilde Y_\gamma(u)-\widetilde Y_\gamma(a)-\widetilde Y_\gamma'(a)(u-a).
\]
Assume
\[
\widetilde Y_\gamma''(a)=0,
\qquad
\widetilde H_3(a,x)=0,
\qquad
\partial_x\widetilde H_3(a,x)=0.
\]
Then one of the following holds:
\begin{enumerate}
\item $\mathcal R\equiv 0$ on $[a,x]$;
\item there exist points
\[
a<r_1<r_2<x
\]
such that
\[
\mathcal Z(r_1)=\mathcal Z(r_2)=\mathcal Z(a).
\]
Consequently there exist
\[
\xi_1\in(a,r_1),
\qquad
\xi_2\in(r_1,r_2)
\]
with
\[
\mathcal R(\xi_1)=\mathcal R(\xi_2)=0.
\]
\end{enumerate}
In particular, every nontrivial sheared repeated-node event contains at least two
interior zeros of $\mathcal R$.
\end{proposition}

\begin{proof}
Since
\[
\widetilde Y_\gamma''=Y''-\gamma S''=(\mathcal Z-\gamma)S'',
\]
the choice $\gamma=\mathcal Z(a)$ gives
\[
\widetilde Y_\gamma''=(\mathcal Z-\mathcal Z(a))S''.
\]
Also,
\[
\partial_x\widetilde H_3(a,x)
=
\widetilde Y_\gamma'(x)-\widetilde Y_\gamma'(a)
=
\int_a^x \widetilde Y_\gamma''(t)\,dt,
\]
and
\[
\widetilde H_3(a,x)
=
\int_a^x (x-t)\,\widetilde Y_\gamma''(t)\,dt.
\]
Therefore the hypotheses imply
\[
\int_a^x (\mathcal Z(t)-\mathcal Z(a))\,S''(t)\,dt=0,
\]
and
\[
\int_a^x (x-t)(\mathcal Z(t)-\mathcal Z(a))\,S''(t)\,dt=0.
\]

Choose $\varepsilon\in\{\pm1\}$ so that
\[
\varepsilon S''>0
\qquad\text{on }J,
\]
and define the positive measure
\[
d\mu(t):=\varepsilon S''(t)\,dt,
\qquad
\phi(t):=\mathcal Z(t)-\mathcal Z(a).
\]
Then
\[
\int_a^x \phi\,d\mu=0,
\qquad
\int_a^x (x-t)\phi\,d\mu=0.
\]
Hence also
\[
\int_a^x t\,\phi(t)\,d\mu(t)=0.
\]

If $\phi\equiv 0$ on $[a,x]$, then
\[
\mathcal R=\phi'\equiv 0
\qquad\text{on }[a,x],
\]
which is alternative (1).

Assume now that $\phi\not\equiv 0$. Then $\phi$ cannot have fixed sign on
$[a,x]$, because $\mu$ is positive and
\[
\int_a^x \phi\,d\mu=0.
\]
It also cannot have exactly one sign change. Indeed, if $c\in(a,x)$ were the
unique sign-change point, then
\[
(t-c)\phi(t)\ge 0
\qquad\text{on }[a,x],
\]
with strict inequality on a set of positive $\mu$-measure. Therefore
\[
\int_a^x (t-c)\phi(t)\,d\mu(t)>0.
\]
But
\[
\int_a^x (t-c)\phi(t)\,d\mu(t)
=
\int_a^x t\,\phi(t)\,d\mu(t)
-c\int_a^x \phi(t)\,d\mu(t)
=0,
\]
a contradiction.

So $\phi$ has at least two sign changes on $(a,x)$. Since $\phi(a)=0$ and
$\phi$ is continuous, there exist
\[
a<r_1<r_2<x
\]
such that
\[
\phi(r_1)=\phi(r_2)=0,
\]
i.e.
\[
\mathcal Z(r_1)=\mathcal Z(r_2)=\mathcal Z(a).
\]
Applying Rolle's theorem to $\phi=\mathcal Z-\mathcal Z(a)$ on $[a,r_1]$ and on
$[r_1,r_2]$ gives
\[
\mathcal R(\xi_1)=0
\quad\text{for some }\xi_1\in(a,r_1),
\qquad
\mathcal R(\xi_2)=0
\quad\text{for some }\xi_2\in(r_1,r_2).
\]
This proves (2).
\end{proof}

\begin{corollary}[Isolated $\mathcal R$-zeros are shear-sterile]
\label{cor:isolated-R-zeros-are-shear-sterile}
Let $b\in J$ be an isolated zero of $\mathcal R$. Then there exists a
neighborhood $U\subset J$ of $b$ such that no pair
\[
a<x,
\qquad
a,x\in U,
\]
can satisfy
\[
\gamma=\mathcal Z(a),
\qquad
\widetilde Y_\gamma''(a)=0,
\qquad
\widetilde H_3(a,x)=0,
\qquad
\partial_x\widetilde H_3(a,x)=0.
\]
\end{corollary}

\begin{proof}
Choose $U$ so small that $b$ is the only zero of $\mathcal R$ in $U$. If such a
pair $a<x$ existed in $U$, then
Proposition~\ref{prop:sheared-repeated-node-forces-two-R-zeros} would produce
two interior zeros of $\mathcal R$ in $(a,x)\subset U$, a contradiction.
\end{proof}

\begin{corollary}[Diagonal sheared recurrence forces affine-planar collapse]
\label{cor:diagonal-sheared-recurrence-forces-affine-planar-collapse}
Let $J$ be a connected opened nonmixed arc. Suppose there exists a sequence
\[
a_n<x_n,
\qquad
a_n,x_n\in J,
\qquad
a_n,x_n\to b,
\qquad
x_n-a_n\to 0,
\]
such that, with
\[
\gamma_n:=\mathcal Z(a_n),
\qquad
\widetilde Y_{\gamma_n}:=Y-\gamma_n S,
\]
one has
\[
\widetilde Y_{\gamma_n}''(a_n)=0,
\qquad
\widetilde H_{3,n}(a_n,x_n)=0,
\qquad
\partial_x\widetilde H_{3,n}(a_n,x_n)=0,
\]
where
\[
\widetilde H_{3,n}(a,u):=
\widetilde Y_{\gamma_n}(u)-\widetilde Y_{\gamma_n}(a)-\widetilde Y_{\gamma_n}'(a)(u-a).
\]
Then
\[
\mathcal R\equiv 0
\]
on a neighborhood of $b$. Hence on some nontrivial interval containing $b$ one
has
\[
Y=\gamma S+\alpha x+\beta,
\]
so the lifted curve is locally affine-planar there.
\end{corollary}

\begin{proof}
If for some $n$ the first alternative in
Proposition~\ref{prop:sheared-repeated-node-forces-two-R-zeros} holds, then
$\mathcal R\equiv 0$ on $[a_n,x_n]$ and we are done.

Otherwise, for every $n$ the interval $(a_n,x_n)$ contains at least two zeros of
$\mathcal R$. Since
\[
a_n,x_n\to b,
\qquad
x_n-a_n\to 0,
\]
these zeros accumulate at $b$. Because $\mathcal R$ is analytic, this implies
\[
\mathcal R\equiv 0
\]
on a neighborhood of $b$. Therefore $\mathcal Z$ is locally constant, so
\[
Y''=\gamma S''
\]
there, and integrating twice gives
\[
Y=\gamma S+\alpha x+\beta
\]
on a nontrivial interval around $b$.
\end{proof}

\begin{remark}[Source-level form of the shear transfer]
\label{rem:source-level-form-of-shear-transfer}
For fixed $a\in J$, set
\[
f_a:=u_5-\mathcal Z(a)\lambda.
\]
Then
\[
\widetilde Y_{\mathcal Z(a)}=\frac{f_a}{c},
\qquad
J_{f_a}=J_{u_5}-\mathcal Z(a)J_\lambda
=(\mathcal Z-\mathcal Z(a))J_\lambda,
\]
and
\[
D_{f_a}(a,h)=D_{u_5}(a,h)-\mathcal Z(a)\,D_\lambda(a,h).
\]
Thus the sheared repeated-node conditions are exactly a repeated-node
tangent-contact event for the source triple $(c,v_5,f_a)$:
\[
J_{f_a}(a)=0,
\qquad
D_{f_a}(a,h)=0,
\qquad
\partial_h D_{f_a}(a,h)=0.
\]
Proposition~\ref{prop:sheared-repeated-node-forces-two-R-zeros} shows that such
a source-level event forces either $J_{f_a}\equiv 0$ on the interval or at least
two interior zeros of $J_{f_a}$, equivalently at least two interior zeros of
$\mathcal R$ and hence of $\mathcal W_{\mathrm{src}}$.
\end{remark}

\begin{remark}[Effect on the rigidified first-loss lane]
\label{rem:effect-of-shear-transfer-on-first-loss}
Corollary~\ref{cor:diagonal-sheared-recurrence-forces-affine-planar-collapse}
shows that the rigidified sheared repeated-node event cannot be realized
diagonally near an isolated odd zero of $\mathcal R$. Therefore, in the only
diagonal regime allowed by
Proposition~\ref{prop:first-loss-is-diagonal-odd-zero-birth}, any genuine
diagonal realization of the sheared event already forces the affine-planar
collapse case
\[
\mathcal R\equiv 0.
\]
So the sheared bottleneck closes in the collapse direction, not by a purely
planar contradiction for $(x,S)$.
\end{remark}
```

What I think this buys you:

The right takeaway is not “sheared event transfers to \(H_3\) for \((x,S)\).” I do not see that from the current identities. What this does give is the alternative (3) you asked for, in the form actually aligned with the current first-loss theorem:

- a genuine sheared repeated-node event forces a **multi-crossing** of the \(\mathcal Z\)-level;
- therefore it forces at least two interior zeros of \(\mathcal R\), equivalently of \(\mathcal W_{\mathrm{src}}\);
- hence any **diagonal** realization of the sheared event forces zero accumulation and thus
  \[
  \mathcal R\equiv 0,
  \]
  i.e. affine-planar collapse.

So this is a real source-coupling/shear-transfer theorem. It does not prove option (2), but it does prove the collapse alternative (3) in the exact regime your existing nonmixed first-loss rigidification produces.
## User (2026-04-20 18:45:58)

can we take it further?

## Assistant (2026-04-20 18:46:00)

Yes. The natural next step is to try to strengthen “forces two interior \(\mathcal R\)-zeros” into a real exclusion theorem near an odd zero, or into a sharper source-level statement about the sheared source channel \(f_a=u_5-\mathcal Z(a)\lambda\). I’m checking the exact draft notation so I can push it in the paper’s language rather than inventing a parallel framework.

_Thought for 5m 14s_

Yes.

I think the right next move is not to keep chasing transfer to an actual \(H_3\)-event for \((x,S)\). It is to separate two issues cleanly:

First, your previous shear theorem can be pushed to a **conditional closure statement**.

Second, one can show by an explicit local model that the remaining gap is exactly the passage from a diagonal odd-zero birth of \(\Gamma_a'\) to the stronger curvature-zero sheared event
\[
\Gamma_a(x)=\mathcal Z(a).
\]
That gap is real and nonformal.

The strongest clean statement I think you can now add is this:

```latex
\begin{proposition}[Conditional closure of the nonmixed branch under shear rigidification]
\label{prop:conditional-closure-nonmixed-under-shear-rigidification}
Let $J$ be a connected opened nonmixed arc.

Assume that every diagonal odd-zero birth on $J$ produces a genuine sheared
repeated-node tangent-contact event in the following sense: for every isolated
odd-order zero $b$ of $\mathcal R$, every neighborhood $U\subset J$ of $b$
contains points
\[
a<x,\qquad a,x\in U,
\]
such that, with
\[
\gamma=\mathcal Z(a),\qquad \widetilde Y_\gamma:=Y-\gamma S,
\]
one has
\[
\widetilde Y_\gamma''(a)=0,\qquad
\widetilde H_3(a,x)=0,\qquad
\partial_x\widetilde H_3(a,x)=0.
\]

Then no isolated odd-order zero of $\mathcal R$ can occur on $J$.
Hence either
\[
\mathcal R\equiv 0
\]
on a nontrivial subinterval of $J$, or every zero of $\mathcal R$ on $J$ is
isolated of even order.

In particular, combined with the already-proved even-zero sterility and
one-sign sterility results, the nonmixed $4$-point branch is excluded on $J$
unless affine-planar collapse occurs.
\end{proposition}

\begin{proof}
If $b$ were an isolated odd-order zero of $\mathcal R$ and
$\mathcal R\not\equiv 0$ near $b$, choose a neighborhood $U$ of $b$ containing
no other zero of $\mathcal R$. By the assumed shear rigidification, $U$
contains a sheared repeated-node tangent-contact event. But
Proposition~\ref{prop:sheared-repeated-node-forces-two-R-zeros} implies that
every such event forces either $\mathcal R\equiv 0$ on an interval or at least
two interior zeros of $\mathcal R$ in that neighborhood, contradiction.

Therefore isolated odd-order zeros cannot occur. The rest follows from the
previous parity classification and sterility results.
\end{proof}
```

That is the furthest honest theorem in the current direction.

What this says in plain language is: **if the rigidified first-loss/odd-zero mechanism really does land you on the sheared curvature-zero event, then the nonmixed branch is finished.**

The other thing we can now say, and I think it is genuinely useful, is that the remaining micro-gap is very precise. It is not “some vague transfer issue.” It is exactly the passage
\[
\Gamma_a'(x)=0
\quad\Longrightarrow\quad
\Gamma_a(x)=\mathcal Z(a),
\]
in the diagonal odd-zero regime.

And that implication is not formal. In fact it fails in the universal odd model.

Take
\[
S''\equiv 1,\qquad \mathcal Z(t)=t^{2m+2},
\qquad \mathcal R(t)=(2m+2)t^{2m+1},
\]
so \(0\) is an isolated odd-order zero of \(\mathcal R\). For \(a=-h\), \(x=h\), one has
\[
\Gamma_a(x)
=
\frac{\int_{-h}^{h}(h-t)t^{2m+2}\,dt}{\int_{-h}^{h}(h-t)\,dt}
=
\frac{h^{2m+2}}{2m+3},
\]
while
\[
\mathcal Z(a)=h^{2m+2}.
\]
So
\[
\Gamma_{-h}(h)=\frac{1}{2m+3}\,\mathcal Z(-h)\neq \mathcal Z(-h).
\]

At the same time, by symmetry the derivative-zero condition is exactly what you expect on this odd model:
\[
\Gamma_{-h}'(h)=0.
\]

So the local odd-zero birth mechanism naturally produces a diagonal extremum branch for \(\Gamma_a\), but **not** the sheared curvature-zero condition. The factor \(1/(2m+3)\) is the giveaway.

That means the real remaining theorem is very sharp:

```latex
\begin{remark}[What the final nonmixed bridge really is]
\label{rem:final-nonmixed-bridge-sharp-form}
The remaining gap is not the existence of diagonal extremum points for the
quadratic-envelope scalar $\Gamma_a$: isolated odd-order zeros of $\mathcal R$
already produce those formally.

The genuinely missing input is stronger. One must show that on a genuine opened
nonmixed RH arc, the diagonal extremum branch cannot remain at the generic odd
model level
\[
\Gamma_a'(x)=0,\qquad \Gamma_a(x)\neq \mathcal Z(a),
\]
but is forced by source coupling to hit the codimension-one rooted condition
\[
\Gamma_a(x)=\mathcal Z(a),
\]
equivalently
\[
\widetilde Y_{\mathcal Z(a)}''(a)=0,\qquad
\widetilde H_3(a,x)=0,\qquad
\partial_x\widetilde H_3(a,x)=0.
\]
Once that rooted sheared event is obtained, the previous shear-transfer theorem
forces affine-planar collapse.
\end{remark}
```

I think this is the honest picture now:

The collapse theorem can be pushed to a conditional closure theorem.

The exact missing micro-step is now isolated.

And the monomial model shows why that micro-step is genuinely structural and has to come from RH source coupling, not from generic local parity.

So yes, this goes further — but the “further” is not another planar contradiction. It is: **if you can upgrade odd-zero birth from \(\Gamma_a'=0\) to \(\Gamma_a=\mathcal Z(a)\), you are done.**
## User (2026-04-20 18:52:38)

can you attack it further

## Assistant (2026-04-20 18:52:40)

I’m pushing directly on that last micro-step now: whether the diagonal odd-zero branch can be forced from mere \(\Gamma_a'(x)=0\) to the rooted condition \(\Gamma_a(x)=\mathcal Z(a)\). If that upgrade can be wrung out of the source identities, the shear bottleneck really closes.

_Thought for 8m 43s_

Yes.

I think there is a real strengthening available, and it hits the exact live micro-gap.

The key new statement is this:

> **Near an isolated odd-order zero of \(\mathcal R\), the diagonal critical branch is not rooted.**
> In draft notation, if \(D_a'(x)=0\) is the local diagonal branch born from an odd zero, then
> \[
> D_a(x)-D_a(a)
> \]
> has a fixed nonzero leading term. Since \(D_a(a)=\mathcal Z(a)\), this means
> \[
> \Gamma_a'(x)=0
> \quad\text{does not upgrade to}\quad
> \Gamma_a(x)=\mathcal Z(a)
> \]
> in the odd-zero birth regime.

That is stronger than the monomial toy computation: it is a general local asymptotic theorem.

Here is the clean form.

Assume we are in the normalized nonmixed chart near an isolated odd zero at \(\xi_0=0\), so
\[
\mathcal R(\xi)=\xi^m \rho(\xi),
\qquad m\ \text{odd},\qquad \rho(0)\neq 0,
\]
and
\[
D_a(t)=\widetilde Y[a,a,t],\qquad D_a(a)=\mathcal Z(a).
\]

Take any diagonal critical sequence
\[
a_n<x_n,\qquad a_n,x_n\to 0,\qquad x_n-a_n\to 0,\qquad D_{a_n}'(x_n)=0.
\]
Write
\[
a_n=c_n-h_n,\qquad x_n=c_n+h_n,
\qquad h_n=\frac{x_n-a_n}{2}>0,\qquad c_n=\frac{a_n+x_n}{2}.
\]

Then Lemma \(\ref{lem:Da-derivative-formula}\) gives
\[
0=D_{a_n}'(x_n)
=2\int_0^1 s(1-s)\,\mathcal R(c_n+h_n(2s-1))\,ds
=\frac12\int_{-1}^1 (1-u^2)\,\mathcal R(c_n+h_n u)\,du.
\]
Set
\[
\mu_n:=\frac{c_n}{h_n}.
\]
Since \(\mathcal R(\xi)=\xi^m\rho(\xi)\),
\[
0=h_n^m \cdot \frac12\int_{-1}^1 (1-u^2)(\mu_n+u)^m \rho(h_n(\mu_n+u))\,du.
\]
Passing to a limit along a subsequence gives the model equation
\[
K_m(\mu):=\int_{-1}^1 (1-u^2)(\mu+u)^m\,du=0.
\]
Now
\[
K_m(0)=0
\]
by oddness, and
\[
K_m'(\mu)=m\int_{-1}^1 (1-u^2)(\mu+u)^{m-1}\,du>0
\]
for every real \(\mu\), because \(m-1\) is even. So \(K_m\) has a unique real root, namely
\[
\mu=0.
\]
Hence
\[
\frac{a_n+x_n}{x_n-a_n}=\mu_n\to 0.
\]
So every diagonal critical pair is asymptotically symmetric across the odd zero.

Now compute the rooted defect:
\[
D_{a_n}(x_n)-D_{a_n}(a_n).
\]
Using
\[
D_a(x)=2\int_0^1 (1-s)\,\mathcal Z(a+s(x-a))\,ds
=\frac12\int_{-1}^1 (1-u)\,\mathcal Z(c+hu)\,du,
\]
we get
\[
D_a(x)-D_a(a)
=
\frac12\int_{-1}^1 (1-u)\bigl(\mathcal Z(c+hu)-\mathcal Z(c-h)\bigr)\,du.
\]
Since \(\mathcal Z'=\mathcal R=\xi^m\rho(\xi)\),
\[
\mathcal Z(\xi)=\mathcal Z(0)+\frac{\rho(0)}{m+1}\xi^{m+1}+O(\xi^{m+2}),
\]
with \(m+1\) even. Therefore
\[
D_a(x)-D_a(a)
=
\frac{\rho(0)}{2(m+1)}\,h^{m+1}
\int_{-1}^1 (1-u)\Big((\mu+u)^{m+1}-(\mu-1)^{m+1}\Big)\,du
+O(h^{m+2}).
\]
Since \(\mu\to 0\), the leading coefficient is the value at \(\mu=0\):
\[
\frac{\rho(0)}{2(m+1)}
\int_{-1}^1 (1-u)(u^{m+1}-1)\,du.
\]
That integral is explicit:
\[
\int_{-1}^1 (1-u)(u^{m+1}-1)\,du
=
-\frac{2(m+1)}{m+2}.
\]
So
\[
D_a(x)-D_a(a)
=
-\frac{\rho(0)}{m+2}\,h^{m+1}+o(h^{m+1})
=
-\frac{\rho(0)}{m+2}\left(\frac{x-a}{2}\right)^{m+1}
+o\!\left((x-a)^{m+1}\right).
\]

That is the punchline.

Because \(m+1\) is even, this has fixed nonzero sign for all sufficiently small nontrivial critical pairs. Hence:

\[
D_a'(x)=0
\quad\Longrightarrow\quad
D_a(x)-D_a(a)\neq 0
\]
for every diagonal critical pair near an isolated odd zero.

Translating back to the draft’s sheared notation:

- in the normalized nonmixed chart, \(D_a=\Gamma_a\);
- and \(D_a(a)=\mathcal Z(a)\).

So near an isolated odd zero of \(\mathcal R\),
\[
\Gamma_a'(x)=0
\]
does occur, but
\[
\Gamma_a(x)=\mathcal Z(a)
\]
does not.

Equivalently, the diagonal odd-zero birth branch is **shear-unrooted**.

Why I think this matters:

It does not yet prove a full global source-coupling theorem. But it does show something sharper than before: the only diagonal regime produced by the parity theorem cannot realize the rigidified sheared repeated-node tangent-contact condition. So the local odd-zero birth mechanism and the rooted sheared event are not the same thing.

That gives a much tighter picture:

- odd zeros of \(\mathcal R\) do seed local repeated-node criticality;
- but that criticality misses the rooted value by a universal leading defect
  \[
  -\frac{\rho(0)}{m+2}\left(\frac{x-a}{2}\right)^{m+1};
  \]
- therefore the sheared event is not born from the local odd-zero model.

So the remaining bottleneck becomes very specific: either there is some genuinely nonlocal/source-driven mechanism forcing that defect to vanish, or the sheared-event route is a red herring and should be removed from the nonmixed closure strategy.

I would state it as a proposition right after the odd-zero parity theorem or right before the “needed shear-transfer theorem” remark.
## User (2026-04-20 19:02:37)

can you push further?

## Assistant (2026-04-20 19:02:38)

I’m checking the exact draft formulas for \(D_a\), \(\Gamma_a\), and the sheared-event normal form so I can push the argument in your notation and see whether the “unrooted defect” can be upgraded into an outright incompatibility theorem.

_Thought for 4m 28s_

Yes.

I think there is a stronger local theorem here than the earlier “forces two interior zeros” statement:

> **Near an isolated odd zero of \(\mathcal R\), the diagonal critical branch exists, but it is uniformly unrooted.**
> So the sheared repeated-node tangent-contact event does **not** occur there.

That is a real push. It gives option (1) locally in the only diagonal regime your first-loss theorem produces.

Here is the clean statement in the normalized nonmixed chart.

```latex
\begin{proposition}[Odd-zero critical branch is uniformly unrooted]
\label{prop:odd-zero-critical-branch-uniformly-unrooted}
Let $\xi_0$ be an isolated odd-order zero of
\[
\mathcal R(\xi)=\frac12\,\widetilde Y'''(\xi),
\]
and, after translation, write
\[
\xi_0=0,
\qquad
\mathcal R(\xi)=\xi^m\rho(\xi),
\qquad
m\ge 1 \text{ odd},
\qquad
\rho(0)\neq 0.
\]
Let
\[
D_a(t):=\widetilde Y[a,a,t],
\qquad
D_a(a)=\frac12\,\widetilde Y''(a)=\mathcal Z(a).
\]

Then there exists $\varepsilon>0$ and a unique real-analytic function
\[
\mu:(-\varepsilon,\varepsilon)\to\mathbf R,
\qquad
\mu(0)=0,
\]
such that for every sufficiently small $h>0$ the unique nontrivial critical pair
\[
a(h):=h(\mu(h)-1),
\qquad
x(h):=h(\mu(h)+1)
\]
satisfies
\[
D_{a(h)}'(x(h))=0,
\]
and every critical pair
\[
a<x,\qquad |a|+|x|<\varepsilon,\qquad D_a'(x)=0
\]
is of this form.

Moreover,
\[
D_{a(h)}(x(h))-D_{a(h)}(a(h))
=
-\frac{\rho(0)}{m+2}\,h^{m+1}+O(h^{m+2}).
\]
Equivalently,
\[
D_{a(h)}(x(h))-D_{a(h)}(a(h))
=
-\frac{\rho(0)}{2^{m+1}(m+2)}(x(h)-a(h))^{m+1}
+o\!\left((x(h)-a(h))^{m+1}\right).
\]

In particular, after shrinking $\varepsilon$ if needed, every nontrivial nearby
critical pair satisfies
\[
D_a(x)\neq D_a(a),
\]
indeed
\[
\operatorname{sgn}\bigl(D_a(x)-D_a(a)\bigr)=-\operatorname{sgn}\rho(0).
\]
So no sheared repeated-node tangent-contact event can occur in a sufficiently
small neighborhood of an isolated odd zero of $\mathcal R$.
\end{proposition}

\begin{proof}
Write every nearby pair as
\[
a=c-h,\qquad x=c+h,\qquad h=\frac{x-a}{2}>0,\qquad \mu=\frac{c}{h}.
\]

By Lemma~\ref{lem:Da-derivative-formula},
\[
D_a'(x)
=
2\int_0^1 s(1-s)\,\mathcal R(a+s(x-a))\,ds.
\]
With the change of variables $u=2s-1$, this becomes
\[
D_{c-h}'(c+h)
=
\frac14\int_{-1}^1 (1-u^2)\,\mathcal R(c+hu)\,du
=
h^m F(h,\mu),
\]
where
\[
F(h,\mu)
:=
\frac14\int_{-1}^1 (1-u^2)(\mu+u)^m
\rho\!\bigl(h(\mu+u)\bigr)\,du.
\]
This is real-analytic in $(h,\mu)$ near $(0,0)$. At $h=0$,
\[
F(0,\mu)=\frac{\rho(0)}{4}K_m(\mu),
\qquad
K_m(\mu):=\int_{-1}^1 (1-u^2)(\mu+u)^m\,du.
\]
Since $m$ is odd,
\[
K_m(0)=0,
\]
and
\[
K_m'(\mu)
=
m\int_{-1}^1 (1-u^2)(\mu+u)^{m-1}\,du>0
\]
for all real $\mu$, because $m-1$ is even. Hence $\mu=0$ is the unique real
root of $K_m$, and it is simple. Therefore
\[
\partial_\mu F(0,0)\neq 0,
\]
so the implicit function theorem gives a unique real-analytic branch
\[
\mu=\mu(h),\qquad \mu(0)=0,
\]
parameterizing all nearby critical pairs.

Now write
\[
\mathcal Z(\xi)=\mathcal Z(0)+\xi^{m+1}\eta(\xi),
\qquad
\eta(0)=\frac{\rho(0)}{m+1},
\]
since $\mathcal Z'=\mathcal R=\xi^m\rho(\xi)$.
Using
\[
D_a(x)-D_a(a)
=
\frac12\int_{-1}^1 (1-u)\bigl(\mathcal Z(c+hu)-\mathcal Z(c-h)\bigr)\,du,
\]
we obtain
\[
D_{c-h}(c+h)-D_{c-h}(c-h)=h^{m+1}G(h,\mu),
\]
where
\[
G(h,\mu):=
\frac12\int_{-1}^1 (1-u)\Bigl(
(\mu+u)^{m+1}\eta\!\bigl(h(\mu+u)\bigr)
-(\mu-1)^{m+1}\eta\!\bigl(h(\mu-1)\bigr)
\Bigr)\,du.
\]
Again $G$ is real-analytic. Evaluating at $(0,0)$ gives
\[
G(0,0)
=
\frac{\eta(0)}{2}\int_{-1}^1 (1-u)(u^{m+1}-1)\,du
=
-\frac{\rho(0)}{m+2}\neq 0.
\]
Therefore along the critical branch,
\[
D_{a(h)}(x(h))-D_{a(h)}(a(h))
=
h^{m+1}G(h,\mu(h))
=
-\frac{\rho(0)}{m+2}h^{m+1}+O(h^{m+2}),
\]
which has fixed nonzero sign for $h$ sufficiently small. This proves the claim.
\end{proof}
```

Then add the direct translation back to the sheared event:

```latex
\begin{corollary}[Sheared repeated-node tangent-contact is impossible near an isolated odd $\mathcal R$-zero]
\label{cor:sheared-event-impossible-near-isolated-odd-R-zero}
Let $b$ be an isolated odd-order zero of $\mathcal R$ on a genuine opened
nonmixed arc. Then there exists a neighborhood $U$ of $b$ such that no pair
\[
a<x,\qquad a,x\in U,
\]
can satisfy
\[
\widetilde Y_{\mathcal Z(a)}''(a)=0,
\qquad
\widetilde H_3(a,x)=0,
\qquad
\partial_x\widetilde H_3(a,x)=0.
\]
Equivalently, no rooted sheared repeated-node tangent-contact event occurs in
a sufficiently small neighborhood of an isolated odd zero of $\mathcal R$.
\end{corollary}

\begin{proof}
In the normalized nonmixed chart centered at $b$, the rooted sheared event is
exactly the conjunction
\[
D_a'(x)=0,
\qquad
D_a(x)=D_a(a)=\mathcal Z(a).
\]
Proposition~\ref{prop:odd-zero-critical-branch-uniformly-unrooted} shows that
for every sufficiently small nontrivial critical pair one has
\[
D_a(x)-D_a(a)\neq 0.
\]
So the rooted sheared event is impossible near $b$.
\end{proof}
```

Why I think this is genuinely important:

It means the “shear-transfer theorem” is not just missing. In the only diagonal regime produced by
Proposition~\ref{prop:first-loss-is-diagonal-odd-zero-birth}, the sheared event is actually absent.

So the picture sharpens to:

- isolated odd zeros of \(\mathcal R\) do produce the diagonal critical branch \(D_a'(x)=0\);
- but that branch misses the rooted value \(D_a(x)=D_a(a)\) by a universal nonzero defect;
- therefore the rigidified sheared repeated-node tangent-contact event is **not** the local realization of diagonal odd-zero birth.

That suggests a more honest strategic conclusion:

1. either the sheared-event route should be downgraded, because it does not capture the actual odd-zero branch;
2. or there must be some extra RH source identity forcing cancellation of this explicit defect, which would be a very strong and very specific source-coupling theorem.

I would insert those two blocks right after
Proposition~\ref{prop:parity-classification-isolated-nonmixed-R-zeros},
because they are really a refinement of the odd-order case.
## User (2026-04-20 19:14:59)

anything more we can do on this line of attack?

## Assistant (2026-04-20 19:15:03)

I’m going back into the draft notation to see whether this line can be pushed from “local impossibility near odd zeros” to a stronger structural conclusion about what the sheared route can and cannot possibly prove.

_Thought for 2m 24s_

Yes, but the main thing left on this line is a **negative structural theorem**, not a closure theorem.

The strongest honest push is:

on an isolated odd-zero branch, you can not only show the sheared rooted event fails; you can show it fails with a **universal signed defect**, and you can compute the first asymmetry of the diagonal critical branch explicitly.

That gives two useful conclusions.

First, no theorem of the form

\[
(a,x)\in \Sigma_M \ \text{near an odd zero}
\quad\Longrightarrow\quad
\Gamma_a(x)=\mathcal Z(a)
\]

can be true by local parity/source-free analysis alone.

Second, the only way to rescue the sheared route is via a genuinely new, nonlocal RH identity that cancels a defect whose leading term is already explicit.

Here is the clean sharpening.

```latex
\begin{proposition}[No local shear-rigidification from odd-zero birth]
\label{prop:no-local-shear-rigidification-from-odd-zero-birth}
Let $b$ be an isolated odd-order zero of $\mathcal R$ on a nonmixed arc.
After translating, write
\[
b=0,
\qquad
\mathcal R(\xi)=\xi^m\rho(\xi),
\qquad
m\ge 1 \text{ odd},
\qquad
\rho(0)\neq 0.
\]
Let
\[
\Sigma_M=\{(a,x):a<x,\ \Gamma_a'(x)=0\}.
\]
Then, after shrinking to a sufficiently small neighborhood of $0$, the set
$\Sigma_M$ is the graph of a unique real-analytic function
\[
x=\tau(a),
\qquad
\tau(0)=0,
\qquad
\tau'(0)=-1.
\]
More precisely,
\[
\tau(a)
=
-a-\frac{2\rho'(0)}{(m+4)\rho(0)}\,a^2+O(a^3).
\]

Along this critical branch one has
\[
\Gamma_a(\tau(a))-\mathcal Z(a)
=
-\frac{\rho(0)}{m+2}
\left(\frac{\tau(a)-a}{2}\right)^{m+1}
+O\!\left(|a|^{m+2}\right).
\]
In particular,
\[
\operatorname{sgn}\bigl(\Gamma_a(\tau(a))-\mathcal Z(a)\bigr)
=
-\operatorname{sgn}\rho(0)
\]
for all sufficiently small $a\neq 0$.

Hence near an isolated odd zero of $\mathcal R$, diagonal first loss produces a
unique analytic critical branch, but that branch is uniformly unrooted:
\[
\Gamma_a'(\tau(a))=0
\qquad\text{while}\qquad
\Gamma_a(\tau(a))\neq \mathcal Z(a).
\]
Therefore no purely local theorem can upgrade diagonal odd-zero birth to the
rooted sheared repeated-node tangent-contact condition.
\end{proposition}
```

The proof is just the IFT calculation on the critical equation, pushed one order farther than before.

If you write
\[
a=c-h,\qquad x=c+h,\qquad \mu=\frac{c}{h},
\]
then the critical equation is
\[
F(h,\mu):=\frac14\int_{-1}^1 (1-u^2)(\mu+u)^m \rho(h(\mu+u))\,du=0.
\]
At \((0,0)\),
\[
\partial_\mu F(0,0)\neq 0,
\]
so \(\mu=\mu(h)\) is analytic. The first derivative comes out explicitly as
\[
\mu'(0)
=
-\frac{\rho'(0)}{(m+4)\rho(0)}.
\]
Since
\[
a=h(\mu(h)-1),\qquad x=h(\mu(h)+1),
\]
this gives the expansion for \(\tau(a)\).

The rooted defect calculation then gives
\[
\Gamma_a(\tau(a))-\mathcal Z(a)
=
-\frac{\rho(0)}{m+2}h^{m+1}+O(h^{m+2}),
\]
and because \(m+1\) is even, its sign is fixed.

That is the cleanest further theorem on this line.

What it buys you is important:

The earlier picture said, “maybe odd-zero birth rigidifies to the sheared rooted event.”

This stronger statement says:

- odd-zero birth rigidifies only to the **critical** branch;
- the rooted condition misses by a universal leading defect;
- the first asymmetry of the critical branch depends on \(\rho'/\rho\), but the leading rooted defect does **not**.

That last point matters. It means the failure of rooting is not some fragile symmetry artifact. It is robust.

So I think the honest strategic conclusion is:

this line can still give one more useful theorem, but it is a **barrier theorem**:

```latex
\begin{remark}[Ceiling of the local shear-transfer route]
\label{rem:ceiling-local-shear-transfer-route}
Proposition~\ref{prop:no-local-shear-rigidification-from-odd-zero-birth} shows
that the rooted sheared repeated-node tangent-contact event is not the local
avatar of diagonal odd-zero birth. Near an isolated odd zero of $\mathcal R$,
the critical branch
\[
\Gamma_a'(x)=0
\]
exists and is analytic, but the rooted defect
\[
\Gamma_a(x)-\mathcal Z(a)
\]
has a fixed nonzero leading term.

Accordingly, no argument using only the local parity model of $\mathcal R$ and
the quadratic-envelope scalar $\Gamma_a$ can prove a shear-transfer theorem of
the form
\[
\Gamma_a'(x)=0 \Longrightarrow \Gamma_a(x)=\mathcal Z(a)
\]
near odd zeros.

Any successful nonmixed closure theorem along the sheared route must therefore
use genuinely additional RH source structure, beyond the local odd-zero
critical-branch analysis.
\end{remark}
```

So yes, there is a bit more to do on this line, but the payoff is mostly to show that this line has a real ceiling. After that, I would probably stop trying to make the sheared rooted event be the local realization of odd-zero birth, because the local analysis is now telling you it is not.

The one further computation that might still be worth doing is to rewrite the same defect in the source-determinant language
\[
\Psi_a(h)=\frac{D_{u_5}(a,h)}{D_\lambda(a,h)},
\qquad
f_a=u_5-\mathcal Z(a)\lambda,
\]
so that along the analytic critical branch for \(\Psi_a\), the numerator
\[
D_{f_a}(a,h)
\]
has a fixed signed leading term. That would phrase the same barrier directly at source level, which may fit the current draft even better.
## User (2026-04-20 19:21:01)

lets do rewrite the same defect in the source-determinant language

## Assistant (2026-04-20 19:21:03)

I’m translating the unrooted defect into the paper’s native source language now: \(D_{u_5}\), \(D_\lambda\), \(f_a=u_5-\mathcal Z(a)\lambda\), and the ratio \(\Psi_a(h)\). The goal is to show the same signed leading defect appears directly as a source determinant statement, not just in the sheared planar scalar.

_Thought for 2m 44s_

Yes. The clean source-level rewrite is:

for
\[
f_a:=u_5-\mathcal Z(x(a))\,\lambda,
\]
the rooted defect is exactly the determinant
\[
D_{f_a}(a,h)=D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h).
\]

So along the repeated-source critical branch
\[
\partial_h\Psi_a(h)=0
\qquad\Longleftrightarrow\qquad
\mathfrak D(a,h)=0,
\]
the question “does the sheared event root?” is exactly the question whether
\[
D_{f_a}(a,h)=0.
\]

And the same local calculation shows it does **not** root near an isolated odd zero of \(\mathcal R\): \(D_{f_a}(a,h)\) has a fixed nonzero leading term and fixed sign.

I would insert the following right after Remark~\ref{rem:source-determinant-reformulation-Gammaa} or after the odd-zero classification section if you want it paired with the earlier “uniformly unrooted” proposition.

```latex
\begin{proposition}[Source-determinant form of the odd-zero unrooted defect]
\label{prop:source-determinant-form-odd-zero-unrooted-defect}
Let $J$ be a good nonmixed $v_5$-patch, and let $b\in J$ be an isolated
odd-order zero of
\[
\mathcal R=\mathcal Z'.
\]
Use the local $x$-coordinate centered at $b$, so that
\[
x(b)=0,
\qquad
\mathcal R(x)=x^m\rho(x),
\qquad
m\ge 1\ \text{odd},
\qquad
\rho(0)\neq 0.
\]
For source points $a<h$ near $b$, define
\[
\Psi_a(h):=\frac{D_{u_5}(a,h)}{D_\lambda(a,h)},
\qquad
f_a:=u_5-\mathcal Z(x(a))\,\lambda.
\]
Then:

\begin{enumerate}
\item one has the exact identity
\[
D_{f_a}(a,h)
=
D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h)
=
D_\lambda(a,h)\,\bigl(\Psi_a(h)-\mathcal Z(x(a))\bigr);
\]

\item there exists a neighborhood of $b$ in which the critical locus
\[
\partial_h\Psi_a(h)=0
\qquad\text{equivalently}\qquad
\mathfrak D(a,h)=0
\]
is the graph of a unique real-analytic branch
\[
h=\sigma(a),
\qquad
\sigma(b)=b,
\qquad
\sigma(a)\neq a
\ \text{for }a\neq b;
\]

\item along that branch one has the asymptotic defect formula
\[
\Psi_a(\sigma(a))-\mathcal Z(x(a))
=
-\frac{\rho(0)}{m+2}
\left(\frac{x(\sigma(a))-x(a)}{2}\right)^{m+1}
+O\!\left(|x(a)|^{m+2}\right);
\]

\item equivalently, the rooted source determinant has the asymptotic
\[
D_{f_a}(a,\sigma(a))
=
-\frac{c(b)^4J_\lambda(b)}{2^{m+2}(m+2)M(b)^2}\,\rho(0)\,
\bigl(x(\sigma(a))-x(a)\bigr)^{m+3}
+o\!\left(\bigl|x(\sigma(a))-x(a)\bigr|^{m+3}\right).
\]
\end{enumerate}

In particular, after shrinking the neighborhood if necessary,
\[
D_{f_a}(a,\sigma(a))\neq 0
\qquad(a\neq b),
\]
and indeed has fixed sign
\[
\operatorname{sgn}D_{f_a}(a,\sigma(a))
=
-\operatorname{sgn}\!\bigl(J_\lambda(b)\rho(0)\bigr).
\]
So near an isolated odd zero of $\mathcal R$, the repeated-source critical
branch
\[
\partial_h\Psi_a(h)=0
\]
is uniformly unrooted in source-determinant language:
\[
D_{u_5}(a,\sigma(a))-\mathcal Z(x(a))\,D_\lambda(a,\sigma(a))\neq 0.
\]
\end{proposition}

\begin{proof}
By Remark~\ref{rem:source-determinant-reformulation-Gammaa},
\[
\Psi_a(h)=\Gamma_{x(a)}(x(h)).
\]
The existence and uniqueness of the analytic critical branch
\[
h=\sigma(a)
\]
is therefore just the source-coordinate pullback of the unique analytic branch
for
\[
\partial_x\Gamma_{x(a)}(x)=0
\]
near an isolated odd zero of $\mathcal R$.

For the determinant identity, use linearity of $D_f(a,h)$ in the third column:
\[
D_{f_a}(a,h)
=
D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h).
\]
Dividing by $D_\lambda(a,h)\neq 0$ gives
\[
\frac{D_{f_a}(a,h)}{D_\lambda(a,h)}
=
\Psi_a(h)-\mathcal Z(x(a)).
\]

Next, apply the row-reduction formula from
Remark~\ref{rem:source-determinant-reformulation-Gammaa} with
\[
F=\frac{f_a}{c}=Y-\mathcal Z(x(a))S.
\]
This gives
\[
D_{f_a}(a,h)
=
c(a)^2c(h)x'(a)\,\widetilde A_{x(a)}(x(h)),
\]
where
\[
\widetilde A_{x(a)}(x)
:=
\widetilde Y_{\mathcal Z(x(a))}(x)
-\widetilde Y_{\mathcal Z(x(a))}(x(a))
-\partial_x\widetilde Y_{\mathcal Z(x(a))}(x(a))\,(x-x(a)).
\]
Since
\[
\widetilde A_{x(a)}(x)
=
B_{x(a)}(x)\,\bigl(\Gamma_{x(a)}(x)-\mathcal Z(x(a))\bigr),
\]
we obtain
\[
D_{f_a}(a,h)
=
c(a)^2c(h)x'(a)\,B_{x(a)}(x(h))
\bigl(\Psi_a(h)-\mathcal Z(x(a))\bigr).
\]

Along the critical branch $h=\sigma(a)$, the earlier odd-zero unrooted-defect
formula gives
\[
\Psi_a(\sigma(a))-\mathcal Z(x(a))
=
-\frac{\rho(0)}{m+2}
\left(\frac{x(\sigma(a))-x(a)}{2}\right)^{m+1}
+O\!\left(|x(a)|^{m+2}\right).
\]
Also,
\[
B_{x(a)}(x(\sigma(a)))
=
\frac12\,S''(b)\,\bigl(x(\sigma(a))-x(a)\bigr)^2
+o\!\left(\bigl|x(\sigma(a))-x(a)\bigr|^2\right),
\]
and
\[
S''(b)=\frac{c(b)^3}{M(b)^3}J_\lambda(b),
\qquad
x'(b)=\frac{M(b)}{c(b)^2}.
\]
Substituting these into the previous identity yields
\[
D_{f_a}(a,\sigma(a))
=
-\frac{c(b)^4J_\lambda(b)}{2^{m+2}(m+2)M(b)^2}\,\rho(0)\,
\bigl(x(\sigma(a))-x(a)\bigr)^{m+3}
+o\!\left(\bigl|x(\sigma(a))-x(a)\bigr|^{m+3}\right).
\]
Since $m$ is odd, $m+3$ is even, so this leading term has fixed sign and is
nonzero for every sufficiently small nontrivial critical pair.
\end{proof}
```

Then I would add the short interpretation remark:

```latex
\begin{remark}[Barrier in source-determinant language]
\label{rem:barrier-source-determinant-language}
Proposition~\ref{prop:source-determinant-form-odd-zero-unrooted-defect} is the
source-level version of the odd-zero barrier. Near an isolated odd zero of
$\mathcal R$, the repeated-source critical branch is detected by
\[
\mathfrak D(a,h)=0
\qquad\text{or equivalently}\qquad
\partial_h\Psi_a(h)=0,
\]
but the rooted numerator
\[
D_{f_a}(a,h)
=
D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h)
\]
has a fixed nonzero leading term there.

So the sheared repeated-node tangent-contact condition is not the local source
avatar of diagonal odd-zero birth. Any theorem forcing
\[
\mathfrak D(a,h)=0 \Longrightarrow D_{f_a}(a,h)=0
\]
near odd zeros must use genuinely additional RH source coupling, not merely the
local odd-zero model for $\mathcal R$.
\end{remark}
```

The core identity to keep in mind is the very simple one:
\[
D_{f_a}=D_\lambda(\Psi_a-\mathcal Z(x(a))).
\]
That is the determinant-language version of the rooted defect. The previous “unrooted branch” statement becomes: on the critical branch for \(\Psi_a\), this numerator has a fixed signed leading term of order \(m+3\).

There is one more small upgrade available after this: rewrite the coefficient using \(\mathcal W_{\mathrm{src}}\) instead of \(\rho(0)\), so the leading constant is expressed entirely in source invariants.
## User (2026-04-20 19:26:55)

check the latest draft, and again give me the latest exact latex insertions and where to insert, including "Ceiling of the local shear-transfer route" if we still need that

## Assistant (2026-04-20 19:26:56)

I’m checking the latest TeX to see what is already in, where the odd-zero/shear discussion now lives, and whether the “ceiling” remark still earns its keep or would be redundant.

I found the main state of play in the current draft.

What’s already in: the planar odd-zero unrooted proposition and corollary are there; the source-level “form of the shear transfer” remark is there; the big nonmixed summary remark already says the branch is uniformly unrooted and that the segment must acquire a second zero of \(\mathcal W_{\mathrm{src}}\).

What is still missing if you want this line fully represented in the paper’s native language: the **source-determinant version** of the same unrooted defect, plus a short local-ceiling remark explaining what this does and does not prove.

_Thought for 3m 14s_

I checked the latest draft.

What is already in and does **not** need reinserting:
- `prop:odd-zero-critical-branch-uniformly-unrooted`
- `cor:sheared-event-impossible-near-isolated-odd-R-zero`
- the source-level shear-transfer remark
- the big nonmixed-status remarks

What is still missing if you want this line fully captured in the draft’s native language is the **source-determinant version of the same unrooted defect**. And yes, I would still keep a short **“Ceiling of the local shear-transfer route”** remark, because it is not redundant with `rem:ceiling-of-current-source-level-reduction`: that older remark is about generic Wronskian freedom, while this new one is a sharper local barrier along the actual odd-zero critical branch.

Here are the exact edits.

---

## 1) Insert this immediately **after**
```latex
\begin{remark}[Source-level form of the shear transfer]
\label{rem:source-level-form-of-shear-transfer}
...
\end{remark}
```

and **before**
```latex
\begin{proposition}[Repeated-source first loss is exactly diagonal odd-zero birth]
\label{prop:first-loss-is-diagonal-odd-zero-birth}
```

```latex
\begin{proposition}[Source-determinant form of the odd-zero unrooted defect]
\label{prop:source-determinant-form-odd-zero-unrooted-defect}
Let $J$ be a good nonmixed $v_5$-patch, and let $b\in J$ be an isolated
odd-order zero of $\mathcal R$.  Use the local $x$-coordinate on $J$ centered
at $b$, so that
\[
x(b)=0,
\qquad
\mathcal R(x)=x^m\rho(x),
\qquad
m\ge 1 \text{ odd},
\qquad
\rho(0)\neq 0.
\]
For each source point $a\in J$, set
\[
\Psi_a(h):=\frac{D_{u_5}(a,h)}{D_\lambda(a,h)},
\qquad
f_a:=u_5-\mathcal Z(x(a))\,\lambda.
\]
Then, after shrinking $J$ around $b$ if necessary, the following hold.

\begin{enumerate}
\item The critical locus
\[
\partial_h\Psi_a(h)=0
\qquad\text{equivalently}\qquad
\mathfrak D(a,h)=0
\]
is the graph of a unique real-analytic branch
\[
h=\sigma(a),
\qquad
\sigma(b)=b,
\qquad
\sigma(a)\neq a
\quad (a\neq b).
\]

\item One has the exact identity
\[
D_{f_a}(a,h)
=
D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h)
=
D_\lambda(a,h)\,\bigl(\Psi_a(h)-\mathcal Z(x(a))\bigr).
\]

\item Along the critical branch,
\[
\Psi_a(\sigma(a))-\mathcal Z(x(a))
=
-\frac{\rho(0)}{2^{m+1}(m+2)}
\bigl(x(\sigma(a))-x(a)\bigr)^{m+1}
+o\!\left(\bigl|x(\sigma(a))-x(a)\bigr|^{m+1}\right).
\]

\item Consequently,
\[
D_{f_a}(a,\sigma(a))
=
-\frac{c(b)^4J_\lambda(b)\rho(0)}{2^{m+2}(m+2)M(b)^2}
\bigl(x(\sigma(a))-x(a)\bigr)^{m+3}
+o\!\left(\bigl|x(\sigma(a))-x(a)\bigr|^{m+3}\right).
\]
\end{enumerate}

In particular, for all sufficiently small $a\neq b$ one has
\[
D_{f_a}(a,\sigma(a))\neq 0,
\]
and indeed
\[
\operatorname{sgn}D_{f_a}(a,\sigma(a))
=
-\operatorname{sgn}\!\bigl(J_\lambda(b)\rho(0)\bigr).
\]
So near an isolated odd zero of $\mathcal R$, the born repeated-source
critical branch
\[
\mathfrak D(a,h)=0
\]
is uniformly unrooted in source-determinant language:
\[
D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h)\neq 0.
\]
\end{proposition}

\begin{proof}
By Remark~\ref{rem:source-determinant-reformulation-Gammaa},
\[
\Psi_a(h)=\Gamma_{x(a)}(x(h)).
\]
Therefore the critical locus
\[
\partial_h\Psi_a(h)=0
\]
is exactly the pullback through the source-to-$x$ coordinate of the local
critical branch
\[
\partial_x\Gamma_{x(a)}(x)=0.
\]
So item (1) is the source-coordinate form of
Proposition~\ref{prop:odd-zero-critical-branch-uniformly-unrooted}.

For item (2), linearity of the determinant in the third column gives
\[
D_{f_a}(a,h)
=
D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h).
\]
Dividing by $D_\lambda(a,h)\neq 0$ on the nonmixed patch yields
\[
\frac{D_{f_a}(a,h)}{D_\lambda(a,h)}
=
\Psi_a(h)-\mathcal Z(x(a)).
\]

For item (3), apply
Proposition~\ref{prop:odd-zero-critical-branch-uniformly-unrooted} to the
$x$-coordinate pair
\[
\bigl(x(a),x(\sigma(a))\bigr).
\]
Since
\[
\Psi_a(\sigma(a))=\Gamma_{x(a)}(x(\sigma(a))),
\]
that proposition gives exactly
\[
\Psi_a(\sigma(a))-\mathcal Z(x(a))
=
-\frac{\rho(0)}{2^{m+1}(m+2)}
\bigl(x(\sigma(a))-x(a)\bigr)^{m+1}
+o\!\left(\bigl|x(\sigma(a))-x(a)\bigr|^{m+1}\right).
\]

For item (4), apply the row-reduction identity from
Remark~\ref{rem:source-determinant-reformulation-Gammaa} to
\[
f=f_a,
\qquad
F=\frac{f_a}{c}=Y-\mathcal Z(x(a))S.
\]
This gives
\[
D_{f_a}(a,h)
=
c(a)^2c(h)x'(a)\,
\widetilde H_{3,\mathcal Z(x(a))}\bigl(x(a),x(h)\bigr),
\]
and since
\[
\widetilde H_{3,\mathcal Z(x(a))}(x(a),x(h))
=
B_{x(a)}(x(h))\bigl(\Psi_a(h)-\mathcal Z(x(a))\bigr),
\]
we obtain
\[
D_{f_a}(a,h)
=
c(a)^2c(h)x'(a)\,B_{x(a)}(x(h))
\bigl(\Psi_a(h)-\mathcal Z(x(a))\bigr).
\]

Along the branch $h=\sigma(a)$, write
\[
\delta(a):=x(\sigma(a))-x(a).
\]
Then
\[
B_{x(a)}(x(\sigma(a)))
=
\frac12\,S''(b)\,\delta(a)^2
+
o\!\left(|\delta(a)|^2\right),
\]
while
\[
x'(b)=\frac{M(b)}{c(b)^2},
\qquad
S''(b)=\frac{c(b)^3}{M(b)^3}J_\lambda(b)
\]
by Remark~\ref{rem:below-lifted-wronskian-ratio-reformulation}. Therefore
\[
c(a)^2c(\sigma(a))x'(a)B_{x(a)}(x(\sigma(a)))
=
\frac{c(b)^4J_\lambda(b)}{2M(b)^2}\,\delta(a)^2
+
o\!\left(|\delta(a)|^2\right).
\]
Multiplying by item (3) gives
\[
D_{f_a}(a,\sigma(a))
=
-\frac{c(b)^4J_\lambda(b)\rho(0)}{2^{m+2}(m+2)M(b)^2}\,
\delta(a)^{m+3}
+
o\!\left(|\delta(a)|^{m+3}\right),
\]
which is the claimed asymptotic. Since $m+3$ is even, the leading term has
fixed nonzero sign for all sufficiently small $a\neq b$.
\end{proof}

\begin{remark}[Ceiling of the local shear-transfer route]
\label{rem:ceiling-local-shear-transfer-route}
Proposition~\ref{prop:source-determinant-form-odd-zero-unrooted-defect} shows
that near an isolated odd zero of $\mathcal R$, the genuinely born local
repeated-source branch is the critical branch
\[
\mathfrak D(a,h)=0
\qquad\text{equivalently}\qquad
\partial_h\Psi_a(h)=0,
\]
but its rooted numerator
\[
D_{f_a}(a,h)
=
D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h)
\]
has a fixed nonzero leading term there.

So the rooted sheared repeated-node tangent-contact condition is not the local
source avatar of diagonal odd-zero birth.  In particular, no argument using
only the local odd-zero model and the repeated-source critical-branch
equations can prove an implication of the form
\[
\mathfrak D(a,h)=0 \Longrightarrow D_{f_a}(a,h)=0
\]
near odd zeros.

This is a different obstruction from
Remark~\ref{rem:ceiling-of-current-source-level-reduction}: the latter says the
generic source-level Wronskian/$4\times4$ package is formally too free, while
the present remark says that even along the \emph{actual local odd-zero branch},
the rooted numerator fails to vanish with a universal signed defect.  Any
successful shear-transfer theorem must therefore use genuinely additional RH
source coupling beyond the local odd-zero critical-branch analysis.
\end{remark}
```

---

## 2) Small integration edit in the big nonmixed summary

In
```latex
\begin{remark}[What remains on the nonmixed $4$-point side]
\label{rem:what-remains-nonmixed-after-R-zero-bridge}
```

replace this paragraph:

```latex
Accordingly, the genuinely live nonmixed $4$-point burden is no longer local
birth, nor repeated-source first loss, nor generic determinant sign.  The
positive-kernel route now shows more: on any chamber where the repeated-source
segment sees exactly one sign change of $\mathcal R$, the born critical branch
is simple, has strictly negative slope, and cannot realize the rigidified
first-loss condition
\[
\Gamma_a(x)=\mathcal Z(a).
\]
Equivalently, at source level, a local branch of
\[
\mathfrak D(a,h)=0
\]
cannot become a genuine obstruction until the source segment acquires a second
zero of
\[
\mathcal W_{\mathrm{src}}.
\]
So the sharpened remaining nonmixed burden is a source-coupling / zero-connection
exclusion theorem for odd zeros of
\[
\mathcal W_{\mathrm{src}},
\]
equivalently of $\mathcal R$, on genuine opened nonmixed arcs.
```

with this:

```latex
Accordingly, the genuinely live nonmixed $4$-point burden is no longer local
birth, nor repeated-source first loss, nor generic determinant sign.  The
positive-kernel route now shows more: on any chamber where the repeated-source
segment sees exactly one sign change of $\mathcal R$, the born critical branch
is simple, has strictly negative slope, and cannot realize the rigidified
first-loss condition
\[
\Gamma_a(x)=\mathcal Z(a).
\]
At source level, the same branch is characterized by
\[
\mathfrak D(a,h)=0,
\]
but
Proposition~\ref{prop:source-determinant-form-odd-zero-unrooted-defect}
shows that its rooted numerator
\[
D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h)
\]
has a fixed signed leading term near an isolated odd zero, so the local branch
is uniformly unrooted there.  Thus a local branch of
\[
\mathfrak D(a,h)=0
\]
cannot become a genuine obstruction until the source segment acquires a second
zero of
\[
\mathcal W_{\mathrm{src}},
\]
or exits the local one-sign-change chamber.  So the sharpened remaining
nonmixed burden is a source-coupling / zero-connection exclusion theorem for
odd zeros of
\[
\mathcal W_{\mathrm{src}},
\]
equivalently of $\mathcal R$, on genuine opened nonmixed arcs.
```

---

## 3) Small integration edit in the compact summary

In
```latex
\begin{remark}[Compact status summary for the nonmixed $4$-point branch]
\label{rem:compact-status-summary-nonmixed-four-point}
```

replace the second paragraph:

```latex
On a nonmixed arc, the same structure is encoded by the gauge-invariant
source-level Wronskian
\[
\mathcal W_{\mathrm{src}}=J_\lambda J_{u_5}'-J_{u_5}J_\lambda',
\]
which differs from $\mathcal R$ only by a nonvanishing analytic factor.
Repeated-source first loss is not a separate mechanism: it occurs if and only
if one diagonally approaches an odd-order zero of $\mathcal R$, and the
corresponding rooted sheared repeated-node event is locally absent near an
isolated odd zero of $\mathcal R$. For each fixed shear level $\gamma$, the
associated sheared opposite-side $3$-point branch is globally rigid on its
maximal slope-overlap block. Thus the genuinely remaining nonmixed burden is
the source-coupling problem of excluding odd zeros of
$\mathcal W_{\mathrm{src}}$ on opened nonmixed arcs.
```

with this:

```latex
On a nonmixed arc, the same structure is encoded by the gauge-invariant
source-level Wronskian
\[
\mathcal W_{\mathrm{src}}=J_\lambda J_{u_5}'-J_{u_5}J_\lambda',
\]
which differs from $\mathcal R$ only by a nonvanishing analytic factor.
Repeated-source first loss is not a separate mechanism: it occurs if and only
if one diagonally approaches an odd-order zero of $\mathcal R$.  Near such an
isolated odd zero, the born repeated-source branch
\[
\mathfrak D(a,h)=0
\]
is uniformly unrooted at source level:
\[
D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h)\neq 0
\]
with a fixed signed leading term
(Proposition~\ref{prop:source-determinant-form-odd-zero-unrooted-defect}).
For each fixed shear level $\gamma$, the associated sheared opposite-side
$3$-point branch is globally rigid on its maximal slope-overlap block.
Thus the genuinely remaining nonmixed burden is the source-coupling problem of
excluding odd zeros of $\mathcal W_{\mathrm{src}}$ on opened nonmixed arcs.
```

---

That is the minimal set I would add.

The only thing I would **not** add is a separate new planar “ceiling” remark, because the planar unrooted proposition/corollary are already in. The missing piece is specifically the source-determinant translation plus the local-ceiling remark above.
## User (2026-04-20 19:32:31)

anything more you'd attack on this route?

## Assistant (2026-04-20 19:33:15)

_Thought for 41s_

Yes, but at this point I would attack it as a **global continuation / first-rooting theorem**, not as more local odd-zero analysis.

The local picture now feels basically exhausted:

- odd zeros of \(\mathcal R\) do create the analytic repeated-source critical branch
  \[
  \mathfrak D(a,h)=0;
  \]
- that branch is uniformly unrooted near birth;
- the rooted numerator
  \[
  N(a,h):=D_{u_5}(a,h)-\mathcal Z(x(a))\,D_\lambda(a,h)=D_{f_a}(a,h)
  \]
  has a fixed signed leading term there.

So the real remaining question on this route is:

> can \(N\) ever hit \(0\) later along the critical branch before the source segment picks up extra \(\mathcal W_{\mathrm{src}}\)-zeros?

That is the sharp theorem I would attack next.

A clean target is:

\[
C(a,h):=\mathfrak D(a,h),\qquad N(a,h):=D_{f_a}(a,h).
\]

You already know that near an isolated odd zero \(b\), the critical locus \(C=0\) is an analytic branch
\[
h=\sigma(a),
\]
and along it,
\[
N(a,\sigma(a))
\]
has fixed nonzero sign for \(a\) near \(b\).

So define
\[
n(a):=N(a,\sigma(a)).
\]

The next theorem to prove is essentially:

> On any connected chamber where the source segment \([a,\sigma(a)]\) sees exactly one sign change of \(\mathcal R\) (equivalently exactly one zero of \(\mathcal W_{\mathrm{src}}\)), one has
> \[
> n(a)\neq 0
> \]
> everywhere.

If you get that, then the whole sheared route closes on that chamber. Because a rooted event is exactly \(n(a)=0\).

Why I think this is the right attack:

You already have two ingredients that almost force it.

First, **birth sign**:
\[
n(a)\sim \text{fixed sign}\cdot (x(\sigma(a))-x(a))^{m+3}.
\]

Second, **rooted-event consequence**:
if at some interior point of the chamber you had
\[
n(a_*)=0,
\]
then you would have a genuine sheared repeated-node event, and your earlier shear-transfer proposition gives:

- either \(\mathcal R\equiv 0\) on the segment, or
- the segment contains at least two interior zeros of \(\mathcal R\).

So if the chamber hypothesis really says “exactly one sign change / exactly one zero is seen by the segment,” then first rooting is impossible.

That suggests the next formal result should be something like:

\[
\boxed{
\text{critical-branch first-rooting can occur only after a second zero of }
\mathcal W_{\mathrm{src}}
\text{ enters the segment.}
}
\]

That is stronger and cleaner than the current remarks.

I would attack it in this order.

First, make the chamber statement precise in source language. The right object is not just “small near \(b\),” but the maximal connected branch of \(C=0\) for which the closed source segment between \(a\) and \(\sigma(a)\) contains exactly one zero of \(\mathcal W_{\mathrm{src}}\), namely the born odd one.

Second, prove a first-rooting contradiction. Suppose \(a_*\) is the first parameter along that branch with
\[
n(a_*)=0.
\]
Then \(C(a_*,\sigma(a_*))=0\) and \(N(a_*,\sigma(a_*))=0\), so this is a rooted sheared event. Your transfer theorem then forces either interval collapse or two interior \(\mathcal R\)-zeros, contradicting the one-zero chamber definition.

That would convert what is currently spread across several local propositions and remarks into one exact continuation theorem.

A second worthwhile attack on this same route is to make the defect coefficient fully intrinsic. Right now it is written in terms of
\[
\mathcal R(x)=x^m\rho(x).
\]
But on a nonmixed patch you have
\[
\mathcal W_{\mathrm{src}}
=
J_\lambda^2\,\frac{d\mathcal Z}{d\lambda}
\]
or an equivalent nonvanishing-factor relation already in the draft. So the leading coefficient in
\[
N(a,\sigma(a))
\]
should be rewritable as a nonzero multiple of the first nonvanishing jet of \(\mathcal W_{\mathrm{src}}\) at the odd zero. That is not a new theorem, but it would make the barrier theorem feel fully source-native instead of translated through \(\mathcal R\).

The third possible push is more ambitious and less certain, but potentially powerful: derive a differential equation for \(n(a)\) along the critical branch \(h=\sigma(a)\). Since
\[
C(a,\sigma(a))=0,
\]
differentiating gives
\[
0=C_a+C_h\,\sigma'(a),
\]
and then
\[
n'(a)=N_a+N_h\,\sigma'(a).
\]
If those derivatives can be rewritten as positive-kernel integrals against \(\mathcal R\) or \(\mathcal W_{\mathrm{src}}\), you may get a sign-preservation theorem for \(n\) on a one-zero chamber. That would be a more structural proof than the first-rooting contradiction, though also harder.

So my honest ranking is:

The best next theorem on this route is the first-rooting/second-zero theorem.

The next-best cleanup is rewriting the leading coefficient entirely in \(\mathcal W_{\mathrm{src}}\)-jet language.

The transport-equation route for \(n(a)\) is worth probing, but I would treat it as speculative until the formulas simplify.

The thing I would not keep attacking is transfer to an actual repeated-node tangent-contact event for the original \((x,S)\) geometry. This route now looks too indirect. The source-determinant continuation picture is much sharper.

If you want, I can write the exact proposition/remark blocks for the “first rooting requires a second \(\mathcal W_{\mathrm{src}}\)-zero” theorem in the draft’s notation.
## User (2026-04-20 19:36:07)

yes please give the exact latex insertion to this draft

## Assistant (2026-04-20 19:36:09)

I’m locating the exact section where the odd-zero/source-determinant material now sits, so I can give you insertions that match the current labels and avoid clashing with what is already in.

I found that the draft has actually already moved past the purely local barrier: it now contains a transport-equation proposition and a one-sign-change chamber corollary that are very close to the first-rooting/second-zero theorem. I’m checking whether they fully capture the theorem you asked for, or just need a tighter summary insertion.

_Thought for 3m 51s_

Yes. In this latest draft, the theorem is already essentially proved by the existing chain

\[
\text{single-sign-change obstruction}
\;\to\;
\text{transport / no-return}
\;\to\;
\text{zero-connection reduction}
\;\to\;
\text{at-most-one-odd-zero exclusion}.
\]

So the cleanest edit is not to add a new long section, but to **upgrade the current remark**
```latex
\begin{remark}[Odd-zero connection reduction for the repeated-source branch]
\label{rem:zero-connection-reduction-repeated-source}
...
\end{remark}
```
into a formal proposition.

The existing “Ceiling of the local shear-transfer route” remark should stay; it is still doing useful work and is not replaced by this.

Use this exact replacement.

---

### Replace
```latex
\begin{remark}[Odd-zero connection reduction for the repeated-source branch]
\label{rem:zero-connection-reduction-repeated-source}
Let $b$ be an isolated odd-order zero of $\mathcal R$, and let
\[
t=\tau(a)
\]
denote the local born repeated-source critical branch supplied by
Proposition~\ref{prop:parity-classification-isolated-nonmixed-R-zeros}.
On any maximal continuation interval on which each segment
\[
[a,\tau(a)]
\]
contains exactly one sign change of $\mathcal R$,
Corollary~\ref{cor:one-sign-change-chamber-no-return} shows that the branch
remains simple, satisfies
\[
\tau'(a)<0,
\]
and cannot realize the rigidified first-loss condition
\[
D_a(\tau(a))=\mathcal Z(a).
\]

Thus the only internal way for such a branch to leave the one-sign-change
chamber is for the sign-change count on the segment to change. Since even-order
zeros of $\mathcal R$ do not change sign, this can occur only when an endpoint
of the segment meets another \emph{odd-order zero} of $\mathcal R$
(equivalently, unless the branch exits the local chart or the opened arc).
Via Remark~\ref{rem:source-determinant-reformulation-Gammaa}, the same
statement holds at source level for a local branch of
\[
\mathfrak D(a,h)=0.
\]
Accordingly, any genuine repeated-source obstruction branch must either leave
the nonmixed chart/arc or connect one odd zero of
\[
\mathcal W_{\mathrm{src}}
\]
to a second odd zero of
\[
\mathcal W_{\mathrm{src}},
\]
equivalently one odd zero of $\mathcal R$ to a second odd zero of
$\mathcal R$.
\end{remark}
```

### with
```latex
\begin{proposition}[First rooting requires a second odd zero along the born repeated-source branch]
\label{prop:first-rooting-requires-second-odd-zero-repeated-source}
Let $b$ be an isolated odd-order zero of $\mathcal R$ on an opened nonmixed
arc, and let
\[
t=\tau(a)
\]
denote an analytic continuation of the local born repeated-source critical
branch, so that
\[
\tau(a)>a,
\qquad
D_a'(\tau(a))=0.
\]
Assume that $U$ is a connected continuation interval on which, for every
$a\in U$, the segment
\[
[a,\tau(a)]
\]
contains exactly one sign change of $\mathcal R$.

Then for every $a\in U$ one has
\[
D_a(\tau(a))\neq \mathcal Z(a).
\]
Equivalently, on the corresponding source branch
\[
h=\sigma(a),
\qquad
\mathfrak D(a,\sigma(a))=0,
\]
one has
\[
\Psi_a(\sigma(a))\neq \mathcal Z(x(a)),
\]
or in determinant form,
\[
D_{u_5}(a,\sigma(a))-\mathcal Z(x(a))\,D_\lambda(a,\sigma(a))\neq 0.
\]

Consequently, any first parameter at which the analytic continuation of the
born repeated-source branch realizes a genuine repeated-source obstruction
inside the opened nonmixed arc must occur only after the corresponding segment
ceases to be one-sign-change. Internally, away from affine-planar collapse and
without leaving the arc, this can happen only when the segment acquires a
second odd-order zero of $\mathcal R$, equivalently of
\[
\mathcal W_{\mathrm{src}}.
\]
\end{proposition}

\begin{proof}
The first claim is exactly
Corollary~\ref{cor:one-sign-change-chamber-no-return}(5): on any continuation
interval on which each segment
\[
[a,\tau(a)]
\]
contains exactly one sign change of $\mathcal R$, the born critical branch
cannot realize the rigidified first-loss condition
\[
D_a(\tau(a))=\mathcal Z(a).
\]

Via Remark~\ref{rem:source-determinant-reformulation-Gammaa},
\[
\Psi_a(h)=\Gamma_{x(a)}(x(h)),
\]
so the same statement in source coordinates is
\[
\Psi_a(\sigma(a))\neq \mathcal Z(x(a)).
\]
Using
Proposition~\ref{prop:source-determinant-form-odd-zero-unrooted-defect}(2),
this is equivalent to
\[
D_{u_5}(a,\sigma(a))-\mathcal Z(x(a))\,D_\lambda(a,\sigma(a))\neq 0.
\]

For the final statement, as long as the continued branch remains inside the
opened nonmixed arc and the corresponding segment remains one-sign-change,
the first part shows that rooting cannot occur. Thus any first rooted point
must be preceded by failure of the one-sign-change condition.

Now even-order zeros of $\mathcal R$ do not change sign, so internally the
sign-change count on the segment can change only when an endpoint of the
segment meets another odd-order zero of $\mathcal R$ (unless the branch exits
the arc or enters the affine-planar collapse case). Finally, on a nonmixed arc
the functions $\mathcal R$ and
\[
\mathcal W_{\mathrm{src}}
=
J_\lambda J_{u_5}'-J_{u_5}J_\lambda'
\]
differ only by a nonvanishing analytic factor
(Proposition~\ref{prop:source-level-Wronskian-nonmixed-four-point}), so odd
zeros of the two objects coincide.
\end{proof}
```

---

No other edit is necessary for this theorem. The later remark
```latex
\begin{remark}[Minimal unresolved configuration on this route]
```
already reads correctly after this replacement, and the compact/noncompact status summaries already say the right thing.

The “Ceiling of the local shear-transfer route” remark should remain as-is.
## User (2026-04-20 19:57:10)

here's the latest draft. we're now doing the described attack. You attack the 2. A discriminant / catastrophe analysis in the (a,γ,h) family

## Assistant (2026-04-20 19:57:12)

I’m going straight at the \((a,\gamma,h)\) discriminant picture in the current draft: isolate the exact determinant equations, identify the RH-selected slice \(\gamma=\mathcal Z(a)\), and see whether the sheared-event obstruction can be reframed as an intersection/transversality problem rather than a first-root ODE.

_Thought for 6m 59s_

I attacked the \((a,\gamma,h)\) route, and the main outcome is:

the discriminant picture is **much more rigid than it first looks**. On a nonmixed patch, the sheared discriminant is not a 2D catastrophe surface at all. It collapses to a **1D analytic curve**:
\[
\mathcal D=\{(a,\Psi_a(h),h):\mathfrak D(a,h)=0\}.
\]
So the RH-selected condition
\[
\gamma=\mathcal Z(x(a))
\]
is not “meeting a sheet”; it is meeting a smooth curve. And at a generic rooted point, that meeting is **transverse**, not tangential. So this route does not produce a local fold/cusp contradiction. What it does produce is a cleaner no-go statement: the remaining problem is a **global transverse crossing** problem, not a local catastrophe. That matches the alternative route you sketched in the note. 

I would insert the following **after**
```latex
\begin{remark}[Minimal unresolved configuration on this route]
\label{rem:minimal-unresolved-configuration-repeated-source}
...
\end{remark}
```
and **before**
```latex
\begin{remark}[What remains on the nonmixed $4$-point side]
\label{rem:what-remains-nonmixed-after-R-zero-bridge}
```

```latex
\begin{proposition}[Discriminant reduction for the moving-shear family]
\label{prop:discriminant-reduction-moving-shear}
Let $J$ be a good nonmixed $v_5$-patch. For $\gamma\in\mathbf R$, set
\[
f_\gamma:=u_5-\gamma\lambda.
\]
Define the sheared discriminant set
\[
\mathcal D:=
\left\{
(a,\gamma,h)\in J\times\mathbf R\times J:
a<h,\ 
D_{f_\gamma}(a,h)=0,\ 
\partial_h D_{f_\gamma}(a,h)=0
\right\}.
\]

Then on the region $a<h$ one has the exact equivalence
\[
(a,\gamma,h)\in\mathcal D
\iff
\gamma=\Psi_a(h),
\qquad
\mathfrak D(a,h)=0.
\]
Equivalently,
\[
\mathcal D
=
\left\{
(a,\Psi_a(h),h):\ a<h,\ \mathfrak D(a,h)=0
\right\}.
\]

In particular, on a nonmixed patch the sheared discriminant is not an
independent two-dimensional catastrophe surface in $(a,\gamma,h)$-space:
it is the graph of the repeated-source critical locus
\[
\mathfrak D(a,h)=0
\]
in the extra $\gamma$-direction.
\end{proposition}

\begin{proof}
By Proposition~\ref{prop:source-determinant-form-odd-zero-unrooted-defect}(2),
for every $(a,h)$ with $a<h$ one has
\[
D_{f_\gamma}(a,h)
=
D_\lambda(a,h)\bigl(\Psi_a(h)-\gamma\bigr).
\]
Differentiating in $h$ gives
\[
\partial_h D_{f_\gamma}(a,h)
=
\partial_h D_\lambda(a,h)\bigl(\Psi_a(h)-\gamma\bigr)
+
D_\lambda(a,h)\,\partial_h\Psi_a(h).
\]

On a good nonmixed patch,
\[
D_\lambda(a,h)\neq 0
\qquad (a<h).
\]
Therefore
\[
D_{f_\gamma}(a,h)=0
\iff
\gamma=\Psi_a(h).
\]
At such a point the first term in the formula for $\partial_h D_{f_\gamma}$
vanishes, so
\[
\partial_h D_{f_\gamma}(a,h)=0
\iff
D_\lambda(a,h)\,\partial_h\Psi_a(h)=0
\iff
\partial_h\Psi_a(h)=0.
\]
Finally, by
Remark~\ref{rem:source-determinant-reformulation-Gammaa},
\[
\partial_h\Psi_a(h)
=
\frac{W_h(c,v_5)(a)\,\mathfrak D(a,h)}{D_\lambda(a,h)^2},
\]
and the prefactor is nonvanishing on the nonmixed patch. Hence
\[
\partial_h\Psi_a(h)=0
\iff
\mathfrak D(a,h)=0.
\]
Combining the two equivalences proves the result.
\end{proof}

\begin{proposition}[Generic rooted discriminant point is a transverse crossing]
\label{prop:generic-rooted-discriminant-point-transverse-crossing}
In the setting of
Proposition~\ref{prop:discriminant-reduction-moving-shear},
define the RH-selected slice
\[
\mathcal S_{\mathrm{RH}}
:=
\left\{
(a,\gamma,h)\in J\times\mathbf R\times J:
\gamma=\mathcal Z(x(a))
\right\}.
\]

Let
\[
(a_*,\gamma_*,h_*)\in \mathcal D\cap \mathcal S_{\mathrm{RH}}
\]
and assume
\[
\partial_h^2 D_{f_{\gamma_*}}(a_*,h_*)\neq 0,
\qquad
\mathcal R(x(a_*))\neq 0.
\]
Then there is a neighborhood of $(a_*,\gamma_*,h_*)$ in which
$\mathcal D$ is a unique real-analytic curve
\[
a\longmapsto \bigl(a,\gamma(a),h(a)\bigr),
\]
with
\[
\gamma(a)=\Psi_a(h(a)).
\]

Moreover one has
\[
\gamma'(a_*)=0,
\qquad
h'(a_*)=0,
\]
and
\[
\frac{d}{da}\Bigl(\gamma(a)-\mathcal Z(x(a))\Bigr)\Big|_{a=a_*}
=
-\mathcal R(x(a_*))\,x'(a_*)\neq 0.
\]
Hence the discriminant curve $\mathcal D$ meets the RH-selected slice
$\mathcal S_{\mathrm{RH}}$ transversely at $(a_*,\gamma_*,h_*)$.

In fact the $\gamma$-projection of the discriminant curve has a nondegenerate
local extremum there:
\[
\gamma''(a_*)
=
-\frac{2\,x'(a_*)^2}{x(h_*)-x(a_*)}\,\mathcal R(x(a_*)).
\]
\end{proposition}

\begin{proof}
By Proposition~\ref{prop:discriminant-reduction-moving-shear},
the discriminant is cut out by
\[
\partial_h\Psi_a(h)=0,
\qquad
\gamma=\Psi_a(h).
\]
At the given point,
\[
\partial_h^2 D_{f_{\gamma_*}}(a_*,h_*)\neq 0.
\]
Since
\[
D_{f_\gamma}(a,h)=D_\lambda(a,h)\bigl(\Psi_a(h)-\gamma\bigr),
\]
differentiating twice in $h$ and evaluating at a discriminant point gives
\[
\partial_h^2 D_{f_{\gamma_*}}(a_*,h_*)
=
D_\lambda(a_*,h_*)\,\partial_h^2\Psi_{a_*}(h_*).
\]
Because $D_\lambda(a_*,h_*)\neq 0$, this implies
\[
\partial_h^2\Psi_{a_*}(h_*)\neq 0.
\]
So by the implicit function theorem there is a unique real-analytic function
\[
h=h(a)
\]
with
\[
h(a_*)=h_*,
\qquad
\partial_h\Psi_a(h(a))=0.
\]
Setting
\[
\gamma(a):=\Psi_a(h(a))
\]
gives the asserted local parametrization of $\mathcal D$.

Now pass to the local normalized nonmixed $x$-coordinate and write
\[
\alpha:=x(a),
\qquad
\tau(\alpha):=x(h(a)),
\qquad
V(\alpha):=D_\alpha(\tau(\alpha)).
\]
By Remark~\ref{rem:source-determinant-reformulation-Gammaa},
\[
\gamma(a)=\Psi_a(h(a))=V(\alpha).
\]
Since $(a_*,\gamma_*,h_*)\in\mathcal S_{\mathrm{RH}}$, one has
\[
V(\alpha_*)=\mathcal Z(\alpha_*),
\qquad
\alpha_*:=x(a_*).
\]

By Proposition~\ref{prop:transport-equations-repeated-source-critical-branch},
at a rooted noncollapse point one has
\[
V'(\alpha_*)=0,
\qquad
\tau'(\alpha_*)=0.
\]
Therefore
\[
\gamma'(a_*)
=
V'(\alpha_*)\,x'(a_*)
=
0,
\]
and
\[
x'(h_*)\,h'(a_*)
=
\tau'(\alpha_*)\,x'(a_*)
=
0,
\]
hence
\[
h'(a_*)=0.
\]

For the transverse-crossing derivative, differentiate
\[
\gamma(a)-\mathcal Z(x(a))
=
V(\alpha)-\mathcal Z(\alpha)
\]
to get
\[
\frac{d}{da}\Bigl(\gamma(a)-\mathcal Z(x(a))\Bigr)
=
x'(a)\bigl(V'(\alpha)-\mathcal R(\alpha)\bigr).
\]
At $a=a_*$, since $V'(\alpha_*)=0$, this becomes
\[
\frac{d}{da}\Bigl(\gamma(a)-\mathcal Z(x(a))\Bigr)\Big|_{a=a_*}
=
-\mathcal R(\alpha_*)\,x'(a_*),
\]
which is nonzero by hypothesis. This proves transversality.

Finally, differentiate
\[
V'(\alpha)=\frac{2}{\tau(\alpha)-\alpha}\bigl(V(\alpha)-\mathcal Z(\alpha)\bigr)
\]
from
Proposition~\ref{prop:transport-equations-repeated-source-critical-branch}(1).
At the rooted point,
\[
V(\alpha_*)-\mathcal Z(\alpha_*)=0,
\qquad
\tau'(\alpha_*)=0,
\]
so
\[
V''(\alpha_*)
=
\frac{2}{\tau(\alpha_*)-\alpha_*}\bigl(V'(\alpha_*)-\mathcal R(\alpha_*)\bigr)
=
-\frac{2}{\tau(\alpha_*)-\alpha_*}\,\mathcal R(\alpha_*).
\]
Hence
\[
\gamma''(a_*)
=
V''(\alpha_*)\,x'(a_*)^2
=
-\frac{2\,x'(a_*)^2}{x(h_*)-x(a_*)}\,\mathcal R(x(a_*)),
\]
as claimed.
\end{proof}

\begin{remark}[What the discriminant/catastrophe route actually reduces to]
\label{rem:what-discriminant-catastrophe-route-actually-reduces-to}
The discriminant reformulation sharpens the geometry, but it also shows the
limit of purely local catastrophe arguments on this route.

Indeed, by
Proposition~\ref{prop:discriminant-reduction-moving-shear},
the sheared discriminant on a nonmixed patch is already one-dimensional:
it is the graph of the repeated-source critical locus
\[
\mathfrak D(a,h)=0
\]
in the $\gamma$-direction. And by
Proposition~\ref{prop:generic-rooted-discriminant-point-transverse-crossing},
a generic rooted point is not a tangential meeting of sheets, nor a local
fold/cusp obstruction for the RH-selected slice
\[
\gamma=\mathcal Z(x(a)).
\]
It is a \emph{transverse crossing} of a smooth discriminant curve by that
moving slice, with oriented crossing sign controlled by
\[
-\mathcal R(x(a_*)).
\]

Accordingly, the discriminant/catastrophe route does not reduce the adjacent
odd-zero problem to a local singularity classification in
$(a,\gamma,h)$-space. The remaining issue is global: to exclude the existence
of such a transverse crossing along the continued born branch before the
segment exits the arc, collapses affine-planarly, or acquires the next odd
zero of $\mathcal W_{\mathrm{src}}$.
\end{remark}
```

Why I think this is the right output from route 2:

- it gives a real structural simplification,
- it explains why “catastrophe” language is mostly misleading here,
- and it isolates the true remaining content: not local singularity theory, but a global no-crossing theorem for the moving slice against a smooth discriminant curve.

That is probably the cleanest honest result available from this line right now.
## User (2026-04-20 20:11:51)

Now, can you Study the discriminant surface

D={(a,γ,h):D
f
γ
	​

	​

(a,h)=0, ∂
h
	​

D
f
γ
	​

	​

(a,h)=0}

near an adjacent-two-odd-zero configuration, and show that the RH-selected slice γ=Z(a) cannot meet D in the pattern required to create the first rooted point.

## Assistant (2026-04-20 20:11:52)

I’m checking the current draft’s exact discriminant, transport, and adjacent-odd-zero setup before I try to push a theorem. The main question is whether there is a real local obstruction near the two-odd-zero chamber, or whether the best honest result is another no-go that says the remaining issue is global.

I can push this a bit, but the honest result is narrower than “full exclusion in the two-odd-zero case.” The discriminant analysis does give a real theorem: a first rooted point cannot be *created locally* at the moment the second adjacent odd zero first enters the active segment. If rooting happens at all, it has to occur later, after a genuine two-sign-change chamber of positive width has formed.

_Thought for 4m 50s_

Yes — but the honest result is a **local no-birth theorem at the adjacent-zero entrance**, not a full exclusion of all crossings in the two-odd-zero case.

After pushing the \((a,\gamma,h)\) discriminant picture against the current transport formulas, I think the strongest thing you can cleanly prove is:

> when the born repeated-source critical branch first reaches the **next adjacent odd zero** of \(\mathcal R\), the RH-selected slice
> \[
> \gamma=\mathcal Z(a)
> \]
> is still separated from the discriminant. So a first rooted point cannot be **created at that entrance event**.

That is real progress, but it still leaves open a later crossing deeper inside a genuine two-sign-change chamber.

The reason is simple. On the born branch,
\[
\mathcal D=\{(a,\gamma,h):D_{f_\gamma}(a,h)=0,\ \partial_hD_{f_\gamma}(a,h)=0\}
\]
is parametrized by
\[
(a,\gamma,h)=\bigl(a,V(a),\tau(a)\bigr),
\qquad
V(a):=D_a(\tau(a)),
\]
because on a nonmixed patch
\[
D_{f_\gamma}(a,h)=D_\lambda(a,h)\,(\Psi_a(h)-\gamma),
\]
so discriminant points are exactly critical points of \(\Psi_a\) with \(\gamma=\Psi_a(h)\). A rooted point is therefore exactly
\[
V(a)=\mathcal Z(a).
\]

Now suppose the born branch is continued until its target first hits the **next adjacent odd zero** \(q\) of \(\mathcal R\). At that boundary parameter \(a_0\), one still has a critical pair
\[
\tau(a_0)=q,
\qquad
D_{a_0}'(q)=0,
\]
and the segment \([a_0,q]\) still has exactly one sign change of \(\mathcal R\). Then
\[
V(a_0)-\mathcal Z(a_0)
=
(q-a_0)\int_0^1 (1-s)^2\,\mathcal R(a_0+s(q-a_0))\,ds.
\]
Because
\[
0=D_{a_0}'(q)=2\int_0^1 s(1-s)\,\mathcal R(a_0+s(q-a_0))\,ds
\]
and
\[
\frac{(1-s)^2}{s(1-s)}=\frac{1-s}{s}
\]
is strictly decreasing on \((0,1)\), the same weighted signed-mass argument already used in the one-sign-change chamber shows that this integral has the sign of the initial lobe and is therefore **nonzero**.

So the discriminant point
\[
P_0:=\bigl(a_0,V(a_0),q\bigr)\in\mathcal D
\]
does **not** lie on the RH slice
\[
\mathcal S_{\mathrm{RH}}:=\{(a,\gamma,h):\gamma=\mathcal Z(a)\}.
\]
By continuity, there is a whole neighborhood of \(P_0\) in which
\[
\mathcal D\cap \mathcal S_{\mathrm{RH}}=\varnothing.
\]

That gives the right local conclusion:

- the first rooted point is **not born at the adjacent-odd-zero entrance**;
- if a rooted point exists at all, it must occur only **after** the branch has moved a positive distance into a genuine two-sign-change chamber.

So this route does not yet kill the adjacent-two-odd-zero case globally. It refines it. The remaining issue is now:

\[
\boxed{
\text{Can the RH slice cross the discriminant deeper inside a genuine two-sign-change chamber?}
}
\]

That is now a global no-crossing problem, not a local catastrophe-at-entry problem. This matches the bottleneck you flagged in your note. 

Here is the exact latex block I would insert right after
`prop:first-rooting-requires-second-odd-zero-repeated-source`
and before
`prop:at-most-one-odd-zero-no-genuine-repeated-source-obstruction`:

```latex
\begin{proposition}[Adjacent-odd-zero entrance is not a rooted discriminant point]
\label{prop:adjacent-odd-zero-entrance-not-rooted-discriminant-point}
Let $J$ be an opened nonmixed arc, and work in a local normalized nonmixed
chart on which
\[
S(t)=t^2,
\qquad
\Gamma_a=D_a.
\]
Let
\[
\tau:(a_-,a_0]\to J
\]
be a real-analytic continuation of the born repeated-source critical branch,
so that
\[
\tau(a)>a,
\qquad
D_a'(\tau(a))=0
\qquad (a\in(a_-,a_0]).
\]
Assume:

\begin{enumerate}
\item for every $a\in(a_-,a_0)$, the segment
\[
[a,\tau(a)]
\]
contains exactly one sign change of $\mathcal R$;

\item the target endpoint reaches the next adjacent odd zero at $a=a_0$, i.e.
\[
q:=\tau(a_0)
\]
is an odd-order zero of $\mathcal R$, there are no odd zeros of $\mathcal R$
in $(\tau(a),q)$ for $a<a_0$ close to $a_0$, and the segment
\[
[a_0,q]
\]
still contains exactly one sign change of $\mathcal R$.
\end{enumerate}

Set
\[
V(a):=D_a(\tau(a)).
\]
Define the sheared discriminant and the RH-selected slice by
\[
\mathcal D:=
\left\{
(a,\gamma,h):a<h,\ D_{f_\gamma}(a,h)=0,\ \partial_h D_{f_\gamma}(a,h)=0
\right\},
\]
\[
\mathcal S_{\mathrm{RH}}
:=
\left\{
(a,\gamma,h):\gamma=\mathcal Z(a)
\right\}.
\]

Then
\[
P(a):=(a,V(a),\tau(a))\in\mathcal D
\qquad (a\in(a_-,a_0]),
\]
but at the adjacent-odd-zero entrance point one has
\[
V(a_0)-\mathcal Z(a_0)\neq 0.
\]
More precisely,
\[
V(a_0)-\mathcal Z(a_0)
=
(q-a_0)\int_0^1 (1-s)^2\,
\mathcal R(a_0+s(q-a_0))\,ds,
\]
and this quantity has the sign of the initial lobe of $\mathcal R$ on
$[a_0,q]$.

Consequently,
\[
P(a_0)\notin \mathcal S_{\mathrm{RH}},
\]
and there exists a neighborhood $N$ of $P(a_0)$ in
\[
J\times\mathbf R\times J
\]
such that
\[
N\cap\mathcal D\cap\mathcal S_{\mathrm{RH}}=\varnothing.
\]

In particular, a first rooted point cannot be created at the moment when the
next adjacent odd zero first enters the active segment. If rooting occurs at
all, it must occur only after the branch has moved into a genuine two-sign-change
chamber of positive width.
\end{proposition}

\begin{proof}
By Remark~\ref{rem:source-determinant-reformulation-Gammaa},
\[
\Psi_a(h)=\Gamma_{x(a)}(x(h)),
\]
and on a nonmixed patch
\[
D_{f_\gamma}(a,h)=D_\lambda(a,h)\bigl(\Psi_a(h)-\gamma\bigr).
\]
Since
\[
D_\lambda(a,h)\neq 0
\qquad (a<h),
\]
we have
\[
D_{f_\gamma}(a,h)=0,\ \partial_hD_{f_\gamma}(a,h)=0
\iff
\gamma=\Psi_a(h),\ \partial_h\Psi_a(h)=0.
\]
Along the continued born branch,
\[
\partial_h\Psi_a(\tau(a))=0,
\qquad
\Psi_a(\tau(a))=V(a),
\]
so indeed
\[
P(a)=(a,V(a),\tau(a))\in\mathcal D.
\]

At $a=a_0$, the integral formula for $D_a$ gives
\[
V(a_0)-\mathcal Z(a_0)
=
(q-a_0)\int_0^1 (1-s)^2\,
\mathcal R(a_0+s(q-a_0))\,ds.
\]
Also, since $D_{a_0}'(q)=0$,
\[
0
=
D_{a_0}'(q)
=
2\int_0^1 s(1-s)\,
\mathcal R(a_0+s(q-a_0))\,ds.
\]

Set
\[
R_0(s):=\mathcal R(a_0+s(q-a_0)),
\qquad
k_0(s):=(1-s)^2,
\qquad
k_1(s):=s(1-s).
\]
Then
\[
\frac{k_0(s)}{k_1(s)}=\frac{1-s}{s}
\]
is strictly decreasing on $(0,1)$. Because $R_0$ has exactly one sign change on
$(0,1)$, the same weighted signed-mass argument used in
Proposition~\ref{prop:single-sign-change-cannot-realize-first-loss} implies
that
\[
\int_0^1 k_0(s)R_0(s)\,ds
\]
has the sign of the initial lobe of $R_0$, hence is nonzero. Therefore
\[
V(a_0)-\mathcal Z(a_0)\neq 0.
\]

Thus
\[
P(a_0)=(a_0,V(a_0),q)\in\mathcal D
\quad\text{but}\quad
P(a_0)\notin\mathcal S_{\mathrm{RH}}.
\]
Since both $\mathcal D$ along the continued branch and the function
\[
(a,\gamma,h)\longmapsto \gamma-\mathcal Z(a)
\]
are continuous, there is a neighborhood $N$ of $P(a_0)$ on which
\[
\gamma-\mathcal Z(a)
\]
keeps the sign of
\[
V(a_0)-\mathcal Z(a_0).
\]
Hence
\[
N\cap\mathcal D\cap\mathcal S_{\mathrm{RH}}=\varnothing.
\]

The final statement is immediate: a rooted point is exactly a point of
\[
\mathcal D\cap\mathcal S_{\mathrm{RH}},
\]
so no such point can be born at the adjacent-odd-zero entrance.
\end{proof}

\begin{remark}[What the discriminant analysis still leaves open]
\label{rem:what-discriminant-adjacent-odd-zero-analysis-still-leaves-open}
Proposition~\ref{prop:adjacent-odd-zero-entrance-not-rooted-discriminant-point}
shows that the RH-selected slice does not create a first rooted point
\emph{locally} when the next adjacent odd zero first enters the active segment.
So the adjacent-two-odd-zero case is not a boundary-birth catastrophe for the
discriminant.

What remains open is genuinely global: whether, after entering a true
two-sign-change chamber of positive width, the RH-selected slice can later cross
the continued discriminant curve
\[
(a,\Psi_a(\tau(a)),\tau(a)).
\]
Thus the unresolved nonmixed bottleneck is a global no-crossing theorem inside
the adjacent-two-odd-zero chamber, not a local entry-point singularity.
\end{remark}
```

That’s the furthest I can honestly push this line right now. It sharpens the geometry, but it does **not** yet prove that the RH slice can never cross the discriminant in the adjacent-two-odd-zero case.

filenavlist5:1Your note sharpening the bottleneck to a first-root / adjacent-odd-zero exclusion theorem and proposing the discriminant route.
## User (2026-04-20 20:33:13)

From the attached, try attack 2:

## Assistant (2026-04-20 20:33:15)

I’m using the attached note as the spec and the current TeX as the target. I’m going to see whether route 2 can produce an actual theorem beyond “the discriminant is a curve”: ideally a no-first-crossing statement on the open window between adjacent-odd-zero entry and any hypothetical rooted point.

I’ve pushed route 2 against what the draft already proves. The honest payoff is a sharper no-go: the discriminant route can exclude a *boundary birth* of the first rooted point, but it still does not by itself exclude a later transverse crossing deeper in a genuine two-odd-zero chamber.

_Thought for 2m 35s_

I pushed route 2 as far as I think it honestly goes with the current package.

The result is useful, but it is not the full adjacent-two-odd-zero exclusion. What route 2 really gives is this:

on a nonmixed patch, the sheared discriminant is a **curve**, not a surface, and the RH-selected slice meets it at a generic rooted point by a **transverse crossing**, not by a local fold/cusp catastrophe. So the remaining obstruction is not a local singularity-theory one. It is a genuinely **global no-crossing** problem along the continued born branch. That matches the bottleneck identified in the attached note. turn10file1

The strongest clean theorem I think you can add from this route is the entry-point no-birth statement, phrased directly in discriminant language.

```latex
\begin{proposition}[Discriminant entry into the adjacent-two-odd-zero chamber is not a first rooted point]
\label{prop:discriminant-entry-not-first-root}
Let $J$ be an opened nonmixed arc, and work in a local normalized nonmixed
chart on which
\[
S(t)=t^2,
\qquad
\Gamma_a=D_a.
\]
Let
\[
\tau:(a_-,a_0]\to J
\]
be a real-analytic continuation of the born repeated-source critical branch,
so that
\[
\tau(a)>a,
\qquad
D_a'(\tau(a))=0
\qquad (a\in(a_-,a_0]).
\]
Assume:

\begin{enumerate}
\item for every $a\in(a_-,a_0)$, the segment
\[
[a,\tau(a)]
\]
contains exactly one sign change of $\mathcal R$;

\item at $a=a_0$, the target first reaches the next adjacent odd zero
\[
q:=\tau(a_0)
\]
of $\mathcal R$, and the segment
\[
[a_0,q]
\]
still contains exactly one sign change of $\mathcal R$.
\end{enumerate}

Set
\[
V(a):=D_a(\tau(a)).
\]
Define the sheared discriminant and the RH-selected slice by
\[
\mathcal D:=
\left\{
(a,\gamma,h):a<h,\ D_{f_\gamma}(a,h)=0,\ \partial_h D_{f_\gamma}(a,h)=0
\right\},
\]
\[
\mathcal S_{\mathrm{RH}}
:=
\left\{
(a,\gamma,h):\gamma=\mathcal Z(a)
\right\}.
\]

Then
\[
P(a):=(a,V(a),\tau(a))\in\mathcal D
\qquad (a\in(a_-,a_0]),
\]
but
\[
P(a_0)\notin \mathcal S_{\mathrm{RH}}.
\]
More precisely,
\[
V(a_0)-\mathcal Z(a_0)
=
(q-a_0)\int_0^1 (1-s)^2\,
\mathcal R(a_0+s(q-a_0))\,ds,
\]
and this quantity has the sign of the initial lobe of $\mathcal R$ on
$[a_0,q]$, hence is nonzero.

Consequently, there exists a neighborhood $N$ of $P(a_0)$ in
\[
J\times\mathbf R\times J
\]
such that
\[
N\cap\mathcal D\cap\mathcal S_{\mathrm{RH}}=\varnothing.
\]

In particular, a first rooted point cannot be created at the instant when the
continued born branch first enters the adjacent-two-odd-zero regime.
If rooting occurs at all, it must occur only later, after the branch has moved
into a genuine two-sign-change chamber of positive width.
\end{proposition}

\begin{proof}
On a nonmixed patch one has
\[
D_{f_\gamma}(a,h)=D_\lambda(a,h)\bigl(\Psi_a(h)-\gamma\bigr),
\]
with
\[
D_\lambda(a,h)\neq 0
\qquad (a<h).
\]
Hence
\[
D_{f_\gamma}(a,h)=0,\ \partial_hD_{f_\gamma}(a,h)=0
\iff
\gamma=\Psi_a(h),\ \partial_h\Psi_a(h)=0.
\]
Along the repeated-source critical branch,
\[
\partial_h\Psi_a(\tau(a))=0,
\qquad
\Psi_a(\tau(a))=V(a),
\]
so indeed
\[
P(a)=(a,V(a),\tau(a))\in\mathcal D.
\]

At $a=a_0$, the integral representation for $D_a$ gives
\[
V(a_0)-\mathcal Z(a_0)
=
(q-a_0)\int_0^1 (1-s)^2\,
\mathcal R(a_0+s(q-a_0))\,ds.
\]
Also, since $D_{a_0}'(q)=0$,
\[
0
=
2\int_0^1 s(1-s)\,\mathcal R(a_0+s(q-a_0))\,ds.
\]

Now
\[
\frac{(1-s)^2}{s(1-s)}=\frac{1-s}{s}
\]
is strictly decreasing on $(0,1)$. Because the segment $[a_0,q]$ carries
exactly one sign change of $\mathcal R$, the same weighted signed-mass
comparison used in the one-sign-change chamber implies that
\[
\int_0^1 (1-s)^2\,\mathcal R(a_0+s(q-a_0))\,ds
\]
has the sign of the initial lobe of $\mathcal R$, hence is nonzero. Therefore
\[
V(a_0)-\mathcal Z(a_0)\neq 0,
\]
so
\[
P(a_0)\notin\mathcal S_{\mathrm{RH}}.
\]

Continuity then gives a neighborhood $N$ of $P(a_0)$ on which
\[
\gamma-\mathcal Z(a)
\]
keeps the sign of
\[
V(a_0)-\mathcal Z(a_0),
\]
hence
\[
N\cap\mathcal D\cap\mathcal S_{\mathrm{RH}}=\varnothing.
\]
\end{proof}
```

Then I would add this remark right after it:

```latex
\begin{remark}[What route 2 actually reduces to]
\label{rem:route-two-reduces-to-global-no-crossing}
The discriminant analysis rules out a \emph{local birth} of the first rooted
point at the adjacent-odd-zero entrance, but it does not by itself exclude a
later crossing inside a genuine two-sign-change chamber.

Indeed, on a nonmixed patch the sheared discriminant is already
one-dimensional:
\[
\mathcal D
=
\{(a,\Psi_a(h),h):\ \mathfrak D(a,h)=0\},
\]
so the RH-selected condition
\[
\gamma=\mathcal Z(a)
\]
meets a discriminant \emph{curve}, not a catastrophe surface. At a generic
rooted point this meeting is transverse, with crossing sign controlled by
\[
-\mathcal R(a_*).
\]
Accordingly, route 2 does not reduce the problem to a local fold/cusp
classification in $(a,\gamma,h)$-space. The remaining issue is global:
to exclude any later transverse crossing of the continued discriminant curve by
the RH-selected slice before affine-planar collapse, branch exit, or completion
of the adjacent-odd-zero connection.
\end{remark}
```

What I think this buys you is a sharper division of labor.

It proves that the “first rooted point” cannot be born at the exact moment the next odd zero enters the active segment. So the adjacent-two-odd-zero case is not a boundary-entry catastrophe. But it also shows why route 2 alone is unlikely to finish the proof: once you are inside a genuine two-sign-change chamber, the remaining question is whether the scalar defect
\[
G(a):=\Psi_a(\sigma(a))-\mathcal Z(a)
\]
can later cross zero, and that is a global continuation problem, not a local discriminant-singularity problem. That is exactly the moving-shear / first-root bottleneck the attached note isolates. turn10file2

My honest verdict: route 2 gives a worthwhile theorem, but it does not close the adjacent-two-odd-zero case. It sharpens it to one final question:

\[
\boxed{
\text{Can the RH-selected slice cross the discriminant curve later inside the
open two-sign-change chamber?}
}
\]

That is where I would stop this route unless a new monotonicity or crossing-index invariant appears.
## User (2026-04-20 20:42:07)

ok attack further

## Assistant (2026-04-20 20:42:09)

I’m pushing past the entry-point no-go to see whether route 2 can get a genuine global obstruction inside the open two-sign-change chamber, rather than just saying “not born at the boundary.” The key question is whether the RH slice can have a first transverse crossing of the discriminant curve once that chamber exists.

I found a stronger continuation argument than the entry-point no-birth theorem. If you stay on the born branch with the anchor still on its original outer lobe, then the two-odd-zero chamber is actually no-crossing for the RH slice: a first root would force \(G'(a_*)=-\mathcal R(a_*)\), but the one-sided sign of \(G\) coming from the entry point forces the opposite derivative sign.

_Thought for 5m 26s_

Yes. I think there is a real further theorem here, and it is stronger than the “entry point is not rooted” statement.

The key observation is that once the branch has entered the **adjacent-two-odd-zero chamber**, the anchor \(a\) is still sitting on the same outer lobe where the branch was born. So \(\mathcal R(a)\) has a fixed sign there. But at any rooted point,
\[
G(a):=V(a)-\mathcal Z(a),\qquad V(a):=D_a(\tau(a)),
\]
satisfies
\[
G'(a)=\frac{2}{\tau(a)-a}G(a)-\mathcal R(a),
\]
hence at a root
\[
G(a_*)=0 \quad\Longrightarrow\quad G'(a_*)=-\mathcal R(a_*).
\]

That derivative sign is incompatible with a **first** zero of \(G\) coming after the entry point, because the sign of \(G\) at the entry point is already fixed by the one-sign-change theorem.

So route 2 can actually be pushed to a genuine no-crossing theorem on the standard adjacent-two-odd-zero chamber.

Here is the exact insertion I would add.

---

## Insert this immediately after
```latex
\begin{proposition}[First rooting requires a second odd zero along the born repeated-source branch]
\label{prop:first-rooting-requires-second-odd-zero-repeated-source}
...
\end{proposition}
```

and before
```latex
\begin{proposition}[At most one odd zero implies no genuine repeated-source obstruction]
\label{prop:at-most-one-odd-zero-no-genuine-repeated-source-obstruction}
```

```latex
\begin{proposition}[No first rooted crossing on the adjacent-two-odd-zero chamber]
\label{prop:no-first-rooted-crossing-adjacent-two-odd-zero-chamber}
Let $J$ be an opened nonmixed arc, and work in a local normalized nonmixed
chart on which
\[
S(t)=t^2,
\qquad
\Gamma_a=D_a.
\]
Let
\[
b<q
\]
be adjacent odd-order zeros of $\mathcal R$ on $J$, and let
\[
I=(a_1,a_0]\subset J
\]
with
\[
a_0<b.
\]
Assume that
\[
\tau:I\to J
\]
is a real-analytic continuation of the born repeated-source critical branch
such that
\[
\tau(a)>a,
\qquad
D_a'(\tau(a))=0
\qquad (a\in I),
\]
and:

\begin{enumerate}
\item
\[
\tau(a_0)=q;
\]

\item for every $a\in I$ with $a<a_0$, the segment
\[
[a,\tau(a)]
\]
contains exactly the two sign changes of $\mathcal R$ at $b$ and $q$;

\item the anchor stays on the birth-side outer lobe, i.e.
\[
[a_1,a_0]\subset J\cap(-\infty,b),
\]
so $\mathcal R$ has fixed sign on $I$.
\end{enumerate}

Set
\[
V(a):=D_a(\tau(a)),
\qquad
G(a):=V(a)-\mathcal Z(a),
\]
and let
\[
\epsilon:=\operatorname{sgn}\bigl(\mathcal R|_I\bigr)\in\{\pm1\}.
\]
Then
\[
\epsilon\,G(a)>0
\qquad (a\in I).
\]
In particular,
\[
G(a)\neq 0
\qquad (a\in I),
\]
so the RH-selected slice
\[
\gamma=\mathcal Z(a)
\]
does not cross the continued discriminant curve
\[
(a,\gamma,h)=\bigl(a,V(a),\tau(a)\bigr)
\]
anywhere inside this adjacent-two-odd-zero chamber.

Equivalently, the born repeated-source branch cannot realize a genuine
repeated-source obstruction while the active segment contains exactly the two
adjacent odd zeros $b,q$ and the anchor remains on the original birth-side
outer lobe.
\end{proposition}

\begin{proof}
At the entry point $a=a_0$, one has
\[
\tau(a_0)=q,
\]
and the segment
\[
[a_0,q]
\]
contains exactly one sign change of $\mathcal R$, namely at $b$.
Therefore
Proposition~\ref{prop:single-sign-change-cannot-realize-first-loss} applies to
the critical pair $(a_0,q)$ and gives
\[
\operatorname{sgn}G(a_0)=\epsilon,
\]
hence
\[
\epsilon\,G(a_0)>0.
\]

Assume for contradiction that $G$ vanishes somewhere on $I$. Let
\[
a_*:=\sup\{a\in I:\ G(a)=0\}.
\]
Then $a_*<a_0$, and by continuity and the choice of $a_*$,
\[
\epsilon\,G(a)>0
\qquad\text{for all }a\in(a_*,a_0].
\]
Since $G$ is real-analytic, this implies the one-sided derivative sign
condition
\[
\epsilon\,G'(a_*)\ge 0.
\]

On the other hand, by
Proposition~\ref{prop:transport-equations-repeated-source-critical-branch}(1),
\[
G'(a)
=
\frac{2}{\tau(a)-a}\,G(a)-\mathcal R(a).
\]
Evaluating at the root $a=a_*$ gives
\[
G'(a_*)=-\mathcal R(a_*).
\]
Because $a_*\in I$ and $\mathcal R$ has fixed sign $\epsilon$ on $I$, one has
\[
\epsilon\,G'(a_*)
=
-\epsilon\,\mathcal R(a_*)
=
-|\mathcal R(a_*)|
<0,
\]
contradicting
\[
\epsilon\,G'(a_*)\ge 0.
\]

Therefore $G$ has no zero on $I$, and since
\[
\epsilon\,G(a_0)>0,
\]
its sign on all of $I$ is $\epsilon$. This proves the claim.
\end{proof}

\begin{corollary}[Adjacent-two-odd-zero chamber is no-crossing for the RH-selected slice]
\label{cor:adjacent-two-odd-zero-chamber-no-crossing}
In the setting of
Proposition~\ref{prop:no-first-rooted-crossing-adjacent-two-odd-zero-chamber},
define the discriminant curve
\[
\mathcal C:=\{(a,V(a),\tau(a)):a\in I\}
\subset \mathcal D
\]
and the RH-selected slice
\[
\mathcal S_{\mathrm{RH}}
:=
\{(a,\gamma,h):\gamma=\mathcal Z(a)\}.
\]
Then
\[
\mathcal C\cap \mathcal S_{\mathrm{RH}}=\varnothing.
\]
So the adjacent-two-odd-zero configuration does not produce a first rooted
point by any interior crossing of the RH-selected slice with the continued
discriminant curve.
\end{corollary}

\begin{proof}
Along the continued critical branch,
\[
V(a)=\Psi_a(\sigma(a)),
\]
so
\[
(a,V(a),\tau(a))\in\mathcal D.
\]
A crossing with the RH-selected slice is exactly the condition
\[
V(a)=\mathcal Z(a),
\]
i.e.
\[
G(a)=0.
\]
Apply
Proposition~\ref{prop:no-first-rooted-crossing-adjacent-two-odd-zero-chamber}.
\end{proof}

\begin{remark}[What this upgrades in the route-2 picture]
\label{rem:route-two-upgraded-no-crossing}
The earlier discriminant analysis showed only that a rooted point is not born
\emph{at the entry} into the adjacent-two-odd-zero chamber.  Proposition~
\ref{prop:no-first-rooted-crossing-adjacent-two-odd-zero-chamber} is stronger:
as long as the continued born branch remains in the standard adjacent-two-odd-zero
configuration---two sign changes on the active segment, with the anchor still
on the original birth-side outer lobe---the RH-selected slice cannot cross the
discriminant curve anywhere in that chamber.

So on this route the first genuinely unresolved configuration is pushed one step
further out: any putative first rooted point would have to occur only after the
branch leaves this standard adjacent-two-odd-zero chamber, for example by
branch exit, affine-planar collapse, or the entrance of a further odd zero of
$\mathcal R$ (equivalently of $\mathcal W_{\mathrm{src}}$).
\end{remark}
```

---

This is the important mathematical content:

- at the chamber entry \(a_0\), \(G(a_0)\) already has the sign of the birth-side lobe;
- on the whole adjacent-two-odd-zero chamber, the anchor-side sign of \(\mathcal R(a)\) stays fixed;
- a first root \(a_*\) would force
  \[
  G'(a_*)=-\mathcal R(a_*),
  \]
  whose sign is exactly the **wrong** one for a first zero coming from the entry sign.

So this is a genuine global no-crossing theorem for route 2, not just a local entry-point no-go.

The caveat is the assumption that the branch stays in the standard chamber with the anchor on the original birth-side outer lobe. Under that hypothesis, I think this argument is clean and closes the adjacent-two-odd-zero chamber itself.

If you want, I can now do one more pass and give the exact follow-up edits to `rem:minimal-unresolved-configuration-repeated-source`, `rem:what-remains-nonmixed-after-R-zero-bridge`, and the compact summary so they reflect this stronger reduction.
## User (2026-04-20 20:48:55)

and you attack this further?

## Assistant (2026-04-20 20:49:38)

_Stopped thinking_

## User (2026-04-20 20:49:55)

can you attack this: The caveat is the assumption that the branch stays in the standard chamber with the anchor on the original birth-side outer lobe. Under that hypothesis

## Assistant (2026-04-20 20:49:56)

I’m focusing on that caveat directly: whether the “anchor stays on the original birth-side outer lobe” hypothesis is actually needed, or whether it can be weakened to something more intrinsic along the continued branch.

I can sharpen that caveat. The proof does not really need “outer lobe” as a geometric phrase; what it actually uses is just that the anchor-side value \(\mathcal R(a)\) keeps a fixed sign on the continuation interval. That is a cleaner and weaker hypothesis.

_Thought for 1m 39s_

Yes.

The best honest push here is: you can weaken the caveat substantially.

You do **not** need the geometric phrase “anchor stays on the original birth-side outer lobe.” What the argument actually uses is only this:

\[
\text{the anchor-side value }\mathcal R(a)\text{ keeps a fixed sign along the continued branch.}
\]

That is a cleaner and weaker hypothesis, and it gives a real theorem.

So I would insert the following **immediately after**
```latex
\begin{proposition}[First rooting requires a second odd zero along the born repeated-source branch]
\label{prop:first-rooting-requires-second-odd-zero-repeated-source}
...
\end{proposition}
```

and **before**
```latex
\begin{proposition}[At most one odd zero implies no genuine repeated-source obstruction]
\label{prop:at-most-one-odd-zero-no-genuine-repeated-source-obstruction}
```

```latex
\begin{proposition}[Anchor-sign preservation excludes first rooting]
\label{prop:anchor-sign-preservation-excludes-first-rooting}
Let $J$ be an opened nonmixed arc, and work in a local normalized nonmixed
chart on which
\[
S(t)=t^2,
\qquad
\Gamma_a=D_a.
\]
Let
\[
I=(a_1,a_0]\subset J
\]
and let
\[
\tau:I\to J
\]
be a real-analytic continuation of the repeated-source critical branch such that
\[
\tau(a)>a,
\qquad
D_a'(\tau(a))=0
\qquad (a\in I).
\]
Set
\[
V(a):=D_a(\tau(a)),
\qquad
G(a):=V(a)-\mathcal Z(a).
\]

Assume that there exists $\epsilon\in\{\pm1\}$ such that
\[
\epsilon\,\mathcal R(a)>0
\qquad\text{for every }a\in I,
\]
and
\[
\epsilon\,G(a_0)>0.
\]
Then
\[
\epsilon\,G(a)>0
\qquad\text{for every }a\in I.
\]
In particular,
\[
G(a)\neq 0
\qquad (a\in I),
\]
so the branch cannot realize the rigidified first-loss condition anywhere on
$I$.
\end{proposition}

\begin{proof}
Assume for contradiction that $G$ vanishes somewhere on $I$. Since $G$ is
continuous, the set
\[
Z:=\{a\in I:G(a)=0\}
\]
is closed in $I$ and nonempty. Let
\[
a_*:=\sup Z.
\]
Then $a_*<a_0$, and by the choice of $a_*$ one has
\[
\epsilon\,G(a)>0
\qquad\text{for all }a\in(a_*,a_0].
\]
Hence the one-sided derivative at $a_*$ satisfies
\[
\epsilon\,G'(a_*)\ge 0.
\]

On the other hand, by
Proposition~\ref{prop:transport-equations-repeated-source-critical-branch}(1),
\[
G'(a)
=
V'(a)-\mathcal R(a)
=
\frac{2}{\tau(a)-a}\,G(a)-\mathcal R(a).
\]
Evaluating at the root $a=a_*$ gives
\[
G'(a_*)=-\mathcal R(a_*).
\]
Since $\epsilon\,\mathcal R(a_*)>0$ by hypothesis, this yields
\[
\epsilon\,G'(a_*)
=
-\epsilon\,\mathcal R(a_*)
<0,
\]
contradicting
\[
\epsilon\,G'(a_*)\ge 0.
\]
Therefore $G$ has no zero on $I$. Since
\[
\epsilon\,G(a_0)>0,
\]
the sign of $G$ on all of $I$ is $\epsilon$.
\end{proof}

\begin{corollary}[No crossing on the adjacent-two-odd-zero chamber under anchor-sign preservation]
\label{cor:no-crossing-adjacent-two-odd-zero-under-anchor-sign-preservation}
Let $b<q$ be adjacent odd-order zeros of $\mathcal R$ on an opened nonmixed
arc $J$. Work in a local normalized nonmixed chart, and let
\[
I=(a_1,a_0]\subset J,
\qquad
\tau:I\to J
\]
be a real-analytic continuation of the born repeated-source critical branch such
that
\[
\tau(a)>a,
\qquad
D_a'(\tau(a))=0
\qquad (a\in I),
\]
with
\[
\tau(a_0)=q.
\]
Assume that for every $a\in I$ with $a<a_0$, the segment
\[
[a,\tau(a)]
\]
contains exactly the two sign changes of $\mathcal R$ at $b$ and $q$, and that
\[
\mathcal R(a)
\]
has fixed sign on $I$.

Then
\[
\operatorname{sgn}\bigl(G(a)\bigr)=\operatorname{sgn}\bigl(\mathcal R(a)\bigr)
\qquad (a\in I),
\]
hence
\[
G(a)\neq 0
\qquad (a\in I).
\]
Equivalently, the RH-selected slice
\[
\gamma=\mathcal Z(a)
\]
does not cross the continued discriminant curve
\[
(a,\gamma,h)=\bigl(a,V(a),\tau(a)\bigr)
\]
anywhere on $I$.
\end{corollary}

\begin{proof}
At the entry point $a=a_0$, one has
\[
\tau(a_0)=q,
\]
and the segment
\[
[a_0,q]
\]
contains exactly one sign change of $\mathcal R$, namely at $b$. Therefore
Proposition~\ref{prop:single-sign-change-cannot-realize-first-loss} applied to
the critical pair $(a_0,q)$ gives
\[
\operatorname{sgn}\bigl(G(a_0)\bigr)
=
\operatorname{sgn}\bigl(\mathcal R(a_0)\bigr).
\]
Now apply
Proposition~\ref{prop:anchor-sign-preservation-excludes-first-rooting}
with
\[
\epsilon:=\operatorname{sgn}\bigl(\mathcal R|_I\bigr).
\]
\end{proof}

\begin{remark}[What this really removes from the previous caveat]
\label{rem:what-anchor-sign-preservation-removes}
The preceding corollary shows that the geometric hypothesis
“the anchor stays on the original birth-side outer lobe” was stronger than
necessary.  The proof uses only the intrinsic condition that the anchor-side
value
\[
\mathcal R(a)
\]
keep a fixed sign along the continuation interval.

Accordingly, the remaining unresolved possibility on this route is narrower:
a first rooted point could occur only after the anchor itself crosses an
odd-order zero of $\mathcal R$, or after the branch exits the arc, or after
affine-planar collapse.  Thus the true residual obstruction is no longer the
standard adjacent-two-odd-zero chamber itself, but the transition in which the
anchor-side sign of $\mathcal R$ changes along the continued branch.
\end{remark}
```

I would also update the “minimal unresolved configuration” remark to reflect this stronger reduction.

Replace:
```latex
In particular, the minimal genuinely live scenario is a repeated-source
critical branch born at one odd zero of $\mathcal R$ and continuing until the
segment acquires the next odd zero. Thus the sharpened source-coupling target
is an adjacent-odd-zero connection exclusion theorem on genuine opened
nonmixed arcs.
```

with:
```latex
In particular, the minimal genuinely live scenario is now narrower: a
repeated-source critical branch born at one odd zero of $\mathcal R$,
continuing past acquisition of the next adjacent odd zero, and then surviving
until the anchor itself reaches an odd zero of $\mathcal R$ (or the branch
exits the arc, or affine-planar collapse occurs).  By
Corollary~\ref{cor:no-crossing-adjacent-two-odd-zero-under-anchor-sign-preservation},
no rooted crossing can occur as long as the anchor-side sign of $\mathcal R$
is preserved.  Thus the sharpened source-coupling target is an
anchor-sign-change / odd-zero-connection exclusion theorem on genuine opened
nonmixed arcs.
```

That is the furthest honest push I see from this exact line. It does not remove the caveat completely, but it turns it from a geometric one into a precise dynamical one:

\[
\boxed{
\text{the only remaining issue is what happens when the anchor crosses an odd zero.}
}
\]

That is a better bottleneck than “outer lobe.”
## User (2026-04-20 21:00:50)

can you holistically give me all latex insertions I should make (repeat them to me here)

## Assistant (2026-04-20 21:01:00)

I’m checking the latest TeX so I can give you one consolidated, non-overlapping set of edits, rather than repeating blocks that the draft may already partially contain.

_Thought for 1m 20s_

Yes. I checked the latest draft holistically. Most of the earlier source-determinant, discriminant-curve, transport, and first-fold material is already in. The only edits I would still make are the ones that sharpen the remaining caveat from “outer lobe” to the intrinsic condition “the anchor-side value \(\mathcal R(a)\) keeps a fixed sign,” which is the clean route-2 upgrade you were pushing toward. That matches the current bottleneck in the attached notes. turn11file6

So here is the consolidated set of edits I would make now.

---

## 1) Insert this immediately after
```latex
\begin{proposition}[Discriminant entry into the adjacent-two-odd-zero chamber is not a first rooted point]
\label{prop:discriminant-entry-not-first-root}
...
\end{proposition}
```

and before the current remark
```latex
\begin{remark}[What route 2 actually reduces to]
\label{rem:route-two-reduces-to-global-no-crossing}
```

```latex
\begin{proposition}[Anchor-sign preservation excludes first rooting]
\label{prop:anchor-sign-preservation-excludes-first-rooting}
Let $J$ be an opened nonmixed arc, and work in a local normalized nonmixed
chart on which
\[
S(t)=t^2,
\qquad
\Gamma_a=D_a.
\]
Let
\[
I=(a_1,a_0]\subset J
\]
and let
\[
\tau:I\to J
\]
be a real-analytic continuation of the repeated-source critical branch such that
\[
\tau(a)>a,
\qquad
D_a'(\tau(a))=0
\qquad (a\in I).
\]
Set
\[
V(a):=D_a(\tau(a)),
\qquad
G(a):=V(a)-\mathcal Z(a).
\]

Assume that there exists $\epsilon\in\{\pm1\}$ such that
\[
\epsilon\,\mathcal R(a)>0
\qquad\text{for every }a\in I,
\]
and
\[
\epsilon\,G(a_0)>0.
\]
Then
\[
\epsilon\,G(a)>0
\qquad\text{for every }a\in I.
\]
In particular,
\[
G(a)\neq 0
\qquad (a\in I),
\]
so the branch cannot realize the rigidified first-loss condition anywhere on
$I$.
\end{proposition}

\begin{proof}
Assume for contradiction that $G$ vanishes somewhere on $I$. Since $G$ is
continuous, the set
\[
Z:=\{a\in I:G(a)=0\}
\]
is closed in $I$ and nonempty. Let
\[
a_*:=\sup Z.
\]
Then $a_*<a_0$, and by the choice of $a_*$ one has
\[
\epsilon\,G(a)>0
\qquad\text{for all }a\in(a_*,a_0].
\]
Hence the one-sided derivative at $a_*$ satisfies
\[
\epsilon\,G'(a_*)\ge 0.
\]

On the other hand, by
Proposition~\ref{prop:rooted-defect-transport-stationary-first-root},
\[
G'(a)=\frac{2}{\tau(a)-a}\,G(a)-\mathcal R(a).
\]
Evaluating at the root $a=a_*$ gives
\[
G'(a_*)=-\mathcal R(a_*).
\]
Since $\epsilon\,\mathcal R(a_*)>0$ by hypothesis, this yields
\[
\epsilon\,G'(a_*)
=
-\epsilon\,\mathcal R(a_*)
<0,
\]
contradicting
\[
\epsilon\,G'(a_*)\ge 0.
\]
Therefore $G$ has no zero on $I$. Since
\[
\epsilon\,G(a_0)>0,
\]
the sign of $G$ on all of $I$ is $\epsilon$.
\end{proof}

\begin{corollary}[No crossing on the adjacent-two-odd-zero chamber under anchor-sign preservation]
\label{cor:no-crossing-adjacent-two-odd-zero-under-anchor-sign-preservation}
Let $b<q$ be adjacent odd-order zeros of $\mathcal R$ on an opened nonmixed
arc $J$. Work in a local normalized nonmixed chart, and let
\[
I=(a_1,a_0]\subset J,
\qquad
\tau:I\to J
\]
be a real-analytic continuation of the born repeated-source critical branch such
that
\[
\tau(a)>a,
\qquad
D_a'(\tau(a))=0
\qquad (a\in I),
\]
with
\[
\tau(a_0)=q.
\]
Assume that for every $a\in I$ with $a<a_0$, the segment
\[
[a,\tau(a)]
\]
contains exactly the two sign changes of $\mathcal R$ at $b$ and $q$, and that
\[
\mathcal R(a)
\]
has fixed sign on $I$.

Then
\[
\operatorname{sgn}\bigl(G(a)\bigr)=\operatorname{sgn}\bigl(\mathcal R(a)\bigr)
\qquad (a\in I),
\]
hence
\[
G(a)\neq 0
\qquad (a\in I).
\]
Equivalently, the RH-selected slice
\[
\gamma=\mathcal Z(a)
\]
does not cross the continued discriminant curve
\[
(a,\gamma,h)=\bigl(a,V(a),\tau(a)\bigr)
\]
anywhere on $I$.
\end{corollary}

\begin{proof}
At the entry point $a=a_0$, one has
\[
\tau(a_0)=q,
\]
and the segment
\[
[a_0,q]
\]
contains exactly one sign change of $\mathcal R$, namely at $b$. Therefore
Proposition~\ref{prop:single-sign-change-cannot-realize-first-loss} applied to
the critical pair $(a_0,q)$ gives
\[
\operatorname{sgn}\bigl(G(a_0)\bigr)
=
\operatorname{sgn}\bigl(\mathcal R(a_0)\bigr).
\]
Now apply
Proposition~\ref{prop:anchor-sign-preservation-excludes-first-rooting}
with
\[
\epsilon:=\operatorname{sgn}\bigl(\mathcal R|_I\bigr).
\]
\end{proof}

\begin{remark}[What this really removes from the previous caveat]
\label{rem:what-anchor-sign-preservation-removes}
The preceding corollary shows that the geometric hypothesis
“the anchor stays on the original birth-side outer lobe” was stronger than
necessary. The proof uses only the intrinsic condition that the anchor-side
value
\[
\mathcal R(a)
\]
keep a fixed sign along the continuation interval.

Accordingly, the remaining unresolved possibility on this route is narrower:
a first rooted point could occur only after the anchor itself crosses an
odd-order zero of $\mathcal R$, or after the branch exits the arc, or after
affine-planar collapse. Thus the true residual obstruction is no longer the
standard adjacent-two-odd-zero chamber itself, but the transition in which the
anchor-side sign of $\mathcal R$ changes along the continued branch.
\end{remark}
```

---

## 2) Replace the current remark
```latex
\begin{remark}[What route 2 actually reduces to]
\label{rem:route-two-reduces-to-global-no-crossing}
...
\end{remark}
```

with this stronger version:

```latex
\begin{remark}[What route 2 now reduces to]
\label{rem:route-two-reduces-to-global-no-crossing}
The discriminant analysis first showed that a rooted point is not born
\emph{at the entry} into the adjacent-two-odd-zero chamber.  The stronger
anchor-sign argument shows more: as long as the continued born branch remains
in a continuation interval on which the anchor-side value
\[
\mathcal R(a)
\]
keeps a fixed sign, the RH-selected slice
\[
\gamma=\mathcal Z(a)
\]
cannot cross the discriminant curve at all.

Thus route 2 no longer leaves open the standard adjacent-two-odd-zero chamber
itself.  What remains is narrower and genuinely global: whether a rooted
crossing can occur only after the anchor passes through an odd-order zero of
$\mathcal R$ (equivalently of $\mathcal W_{\mathrm{src}}$), or after branch
exit, or after affine-planar collapse.

So the residual route-2 problem is not a local catastrophe and not even a
generic two-odd-zero chamber crossing problem.  It is an
\emph{anchor-sign-change crossing problem} for the continued discriminant curve.
\end{remark}
```

---

## 3) Replace the current remark
```latex
\begin{remark}[Minimal unresolved configuration on this route]
\label{rem:minimal-unresolved-configuration-repeated-source}
...
\end{remark}
```

with:

```latex
\begin{remark}[Minimal unresolved configuration on this route]
\label{rem:minimal-unresolved-configuration-repeated-source}
Proposition~\ref{prop:at-most-one-odd-zero-no-genuine-repeated-source-obstruction}
shows that the positive-kernel / source-determinant route has already ruled out
every opened nonmixed arc carrying at most one odd zero of $\mathcal R$.
Moreover,
Corollary~\ref{cor:no-crossing-adjacent-two-odd-zero-under-anchor-sign-preservation}
shows that even after the segment acquires the next adjacent odd zero, no
rooted crossing can occur as long as the anchor-side value $\mathcal R(a)$
keeps a fixed sign along the continued branch.

Accordingly, the first genuinely unresolved configuration is now narrower:
a repeated-source critical branch born at one odd zero of $\mathcal R$,
continued past acquisition of the next adjacent odd zero, and then surviving
until the anchor itself reaches an odd zero of $\mathcal R$ (or until the
branch exits the arc, or affine-planar collapse occurs).  Thus the sharpened
source-coupling target is an anchor-sign-change / odd-zero-connection
exclusion theorem on genuine opened nonmixed arcs.
\end{remark}
```

---

## 4) In
```latex
\begin{remark}[What remains on the nonmixed $4$-point side]
\label{rem:what-remains-nonmixed-after-R-zero-bridge}
```

replace the final paragraph beginning

```latex
Moreover, once one follows an analytic continuation of the born branch beyond
the local birth chamber, the rooted defect
```

through the end of the remark with:

```latex
Moreover, once one follows an analytic continuation of the born branch beyond
the local birth chamber, the rooted defect
\[
G(a)=D_a(\tau(a))-\mathcal Z(a)
\]
satisfies an exact first-order transport equation, and any nondegenerate first
root is forced to be a stationary point of the target map and a local fold of
the moving-shear equation
\[
A_a(x)-\mathcal Z(a)\,B_a(x)=0
\]
(Propositions~\ref{prop:rooted-defect-transport-stationary-first-root}
and~\ref{prop:nondegenerate-rooted-point-is-moving-shear-fold}).
The discriminant analysis rules out birth of such a rooted point at entry into
the adjacent-two-odd-zero chamber
(Proposition~\ref{prop:discriminant-entry-not-first-root}),
and the anchor-sign argument shows more: no rooted crossing can occur on any
continuation interval on which the anchor-side value
\[
\mathcal R(a)
\]
keeps a fixed sign
(Proposition~\ref{prop:anchor-sign-preservation-excludes-first-rooting} and
Corollary~\ref{cor:no-crossing-adjacent-two-odd-zero-under-anchor-sign-preservation}).

So the sharpened remaining nonmixed burden is no longer merely an adjacent-odd-zero
first-fold problem in the abstract.  It is a source-coupling /
\emph{anchor-sign-change first-fold exclusion theorem} for the moving shear
\[
f_a=u_5-\mathcal Z(a)\lambda,
\]
equivalently for odd zeros of
\[
\mathcal W_{\mathrm{src}},
\]
on genuine opened nonmixed arcs.
```

---

## 5) In
```latex
\begin{remark}[Compact status summary for the nonmixed $4$-point branch]
\label{rem:compact-status-summary-nonmixed-four-point}
```

replace the paragraph beginning

```latex
Moreover, the positive-kernel / source-determinant route now excludes every
opened nonmixed arc carrying at most one odd zero of
```

through the end of the remark with:

```latex
Moreover, the positive-kernel / source-determinant route now excludes every
opened nonmixed arc carrying at most one odd zero of
$\mathcal W_{\mathrm{src}}$:
on any chamber where the repeated-source segment sees exactly one sign change of
$\mathcal R$, the born critical branch is simple, has strictly negative slope,
and cannot realize the rigidified first-loss condition. Beyond that chamber, the
discriminant analysis rules out birth of a rooted point at entry into the
adjacent-two-odd-zero chamber, and the anchor-sign argument rules out any
crossing on a continuation interval where the anchor-side value
\[
\mathcal R(a)
\]
keeps a fixed sign. The rooted defect along the continued branch satisfies an
exact transport equation, and any nondegenerate first root is forced to be a
stationary point of the target map and a local fold of the moving-shear
equation
\[
A_a(x)-\mathcal Z(a)\,B_a(x)=0.
\]
So any genuine repeated-source obstruction would have to connect adjacent odd
zeros of $\mathcal W_{\mathrm{src}}$, survive entry into the two-odd-zero
chamber, and persist until an anchor-sign change or equivalent global escape.
Thus the genuinely remaining nonmixed burden is the source-coupling /
anchor-sign-change first-fold exclusion problem for odd zeros of
$\mathcal W_{\mathrm{src}}$ on opened nonmixed arcs.
```

---

That is the full set I would still make in the latest draft.

Everything else from this route that we discussed earlier is already in.
## User (2026-04-21 01:09:33)

ok what's the next attack then?

## Assistant (2026-04-21 01:10:39)

_Stopped thinking_
